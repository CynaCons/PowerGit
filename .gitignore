# PowerGit (branch powergit). The upstream Git Extensions tree lives on the
# master branch and in the ../gitextensions-ref worktree, never here.

# build outputs
bin/
obj/
/dist/
/src/engine/**/bin/publish/
/frontend/src-tauri/binaries/
artifacts/
*.binlog

# editors / OS
.vs/
.idea/
*.user
*.suo
*.DS_Store*
Thumbs.db

# local-only
/.vscode/
/.gitattributes
/frontend/.engine-token

# AppImage matrix / longevity outputs (docker/appimage-check/run-matrix.sh --out)
/docker/appimage-check/out*/
