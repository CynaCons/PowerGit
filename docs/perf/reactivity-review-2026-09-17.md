# Reactivity review — 2026-09-17

Owner: "I have a feeling like it's slow, sluggish, lagging. Not very smooth and reactive."

Method: five read-only finders, one dimension each (render cascade from App, grid and canvas, bottom panel and diff, engine latency on the interaction path, paint/compositor/shell), 46 raw findings, 39 distinct after dedup by file and line, the 24 highest-ranked each handed to a skeptic prompted to refute it — 13 confirmed, 11 refuted, 15 never verified. The confirmed list, with the skeptics' reasons, impact estimates and fixes, is the appendix. The plan is iteration v0.18.18.

## The amplifier

`frontend/src/App.tsx:365` passes `onSelect={(i) => setSelectedSha(…)}` inline. `RevisionGrid.tsx:284-299` puts it in the deps of `clickRow` and `contextRow`, which are props of every `RevisionRow`; `memo(RevisionRow)` (v0.18.16) therefore never holds across an App render, and ~60 rows (36 visible + overscan) re-render with their RefChips instead of the two whose selection changed.

App renders: twice per click (urgent and deferred pass), twice every 10 s (the status poll flips `refreshing` true/false even when the status answer is unchanged, `useRepoState.ts:48/73`), twice per action, once per splitter pointermove (`useChromeLayout.ts:50`), every 400 ms during a fetch (the job poll), per frame of the command-rail width transition (`CommandRail.tsx:88`). Five of the thirteen confirmed findings name this as what makes them expensive.

## Four independent costs

| # | What | Where | Symptom |
|---|---|---|---|
| 1 | Shiki tokenises the whole diff synchronously on the main thread on every arrival, no cache: 0.1 ms/line measured (40 ms at 360 lines, 760 ms at 7,200) plus ~300 ms first-run grammar compile per language | `DiffView.tsx:29-53`, `highlight.ts:203-222` | click latency on the Diff tab |
| 2 | Every in-app action refreshes twice: the watcher echo is deferred past the mute, never dropped, so the full sweep (3,000-row log, refs, four status processes) runs again 1.2 s later; `useOperationActions.test.ts:234` pins three page-0 fetches for one merge | `useRepoState.ts:166-188` | a stutter after every action |
| 3 | Scroll: react-virtual's `flushSync` render on every 28 px range change (SyncLane), the canvas bitmap reset (`canvas.width =`) and fully redrawn per step, and a non-passive wheel listener on the scroller that makes the compositor wait for the main thread on every wheel event | `RevisionGrid.tsx:174, 223-238, 165` | scroll jank, hover (32 ms redraw) |
| 4 | The bottom-panel splitter sets App state per pointermove (whole tree per frame, one emotion rule per pixel height); any reload whose page 0 changed resets the layout worker (every loaded row re-laid-out, ~12 MB structured-cloned for 10k rows) | `useChromeLayout.ts:46-51`, `BottomPanel.tsx:372`, `useHistory.ts:139-150` | panel resize stutter, a stutter after a refresh that changed anything |

Also: `GitHost.Queries.cs:371` — `RenamedFrom` (v0.18.15) spawns a whole-commit `diff-tree` on every per-file diff, serially before the `show`.

## Ruled out

`GetStatus`'s four git processes every 10 s (measured cheap in the 2026-09-16 audit); `BottomPanel` not being memo'd (its props are identity-stable; the deferred pass is the design); the 3,000-row JSON parse on window focus (real but bounded); `EngineProvider`'s fresh context value (no memo'd consumer on the hot path reads it).

## Second pass

The graph's own interactions and the bottom panel's resize got their own pass the same day — see the second part of this document (v0.18.18 task E; tasks F–I).

## Confirmed findings (13)

Each was read by a finder and then by a skeptic prompted to refute it; the skeptic's reason, impact estimate and fix are recorded as it gave them.

### 1. Inline onSelect in App gives RevisionGrid a new clickRow/contextRow every App render, defeating memo(RevisionRow) for all ~60 visible rows

- Where: `frontend/src/App.tsx:365` — render-cascade, symptom: click-latency, impact claimed high, skeptic says medium
- Mechanism: HistoryPane gets `onSelect={(i) => setSelectedSha(rows[i]?.rev.id ?? null)}` — a new closure per App render. RevisionGrid derives `clickRow = useCallback(..., [onSelect, rows, onHighlightRoot])` (RevisionGrid.tsx:284-292) and `contextRow = useCallback(..., [onSelect])` (:293-299) and passes them to every RevisionRow (:429-430). RevisionRow is memo'd (RevisionRow.tsx:42) but its onClick/onContextMenu props change identity on every App render, so every row in the virtual window (≈36 visible + 24 overscan at ROW_HEIGHT=28) re-renders — each row also rebuilds RefChips (memo defeated by the inline `onExpand={() => onExpand(row.rev.id)}` and `onRefContextMenu` lambdas, RevisionRow.tsx:90-92) with its MUI SvgIcon glyphs. This happens on both click passes (finding 2), on every `refreshing` flip (finding 6), on every status/refs/stash update and on every splitter pointermove (finding 10). RevisionGrid already keeps the menu callbacks in refs (RevisionGrid.tsx:69-74) precisely to avoid this, but not onSelect.
- Evidence: App.tsx:365 `onSelect={(i) => setSelectedSha(rows[i]?.rev.id ?? null)}`; RevisionGrid.tsx:284-292 `const clickRow = useCallback((index, e) => { onSelect(index) ... }, [onSelect, rows, onHighlightRoot])`; RevisionRow.tsx:42 `export const RevisionRow = memo(...)`, :70 `onClick={(e) => onClick(index, e)}`; RevisionRow.tsx:90 `onExpand={() => onExpand(row.rev.id)}`.
- Skeptic: Confirmed: App.tsx:365 passes an inline `onSelect` lambda, HistoryPane.tsx:119 forwards it unchanged (HistoryPane is not memo'd), and RevisionGrid.tsx:284-299 puts `onSelect` in the deps of both `clickRow` and `contextRow`, which go to every RevisionRow at :429-430; RevisionRow.tsx:42 is memo'd so a new onClick identity defeats it for the whole virtual window (overscan 12, RevisionGrid.tsx:179). The trigger is real on hot interactions: a click calls `setSelectedSha` in App (App.tsx:67), and the splitter drag calls `setBottomHeight` on every pointermove (useChromeLayout.ts:50), so App re-renders and all ~50-60 visible rows re-render instead of the 2 whose `selected` flag changed. The RefChips half is also true (RevisionRow.tsx:90-92 inline lambdas), though the per-row cost is bounded: RefChips memoizes orderRefs/foldRefs (RefChips.tsx:55-60) and Glyph SvgIcons only exist on rows carrying refs, so a row re-render is mostly element creation + reconcile with no DOM writes.
- Fix: Simpler than proposed: only the App side is needed. Add `selectRow: (i: number) => setSelectedSha(rows[i]?.rev.id ?? null)` to the existing `chrome` useStable bag (App.tsx:146, useStable.ts is the latest-ref pattern and reads `rows` at call time, not render time) and pass `onSelect={chrome.selectRow}`. `onHighlightRoot={history.setHighlightRoot}` is already a stable setter and `rows` only changes on real data changes, so `clickRow`/`contextRow` become stable without touching RevisionGrid. The RefChips inline-lambda part becomes moot for unchanged rows once RevisionRow's memo holds; leave it unless a profiler shows RefChips renders on the two rows that legitimately re-render.
- Measurement: Extend frontend/src/components/RevisionGrid.test.ts (it already has "does not rescan unchanged rows when a parent rerender changes selection" at line 42): vi.mock RevisionRow's module or wrap it with a render counter, render RevisionGrid with 60 rows, rerender with a new `onSelect` lambda and a different `selected`, and assert the row render count is 2 not 60. Alternatively, in a dev build open React DevTools Profiler, enable "Highlight updates", click a row: every visible row flashes today; after the fix only two do. The end-to-end number is the click → Commit tab median in tests/perf/interaction.spec.ts:106, which will move by only a few ms since the diff fetch dominates.

### 2. Every in-app action triggers a second full refresh 1.2 s later: the SSE echo is deferred, never dropped

- Where: `frontend/src/hooks/useRepoState.ts:176` — engine-latency, symptom: periodic-stutter, impact claimed high, skeptic says medium
- Mechanism: The watcher bumps ChangeVersion when the action writes .git (index/HEAD/refs); the engine's /events loop ticks every 500 ms (Program.cs:1216) so the echo lands while the action's own refresh() is in flight. onmessage computes `echoDelay = max(muteUntil - now + 100, inFlight > 0 ? 1200 : 0)` and arms a timer that ALWAYS calls `refresh(scope)` (line 185); there is no check that our own refresh already observed this version. So a commit/checkout/reset does: engine call -> refresh #1 (git log -n3000 + for-each-ref + 4 status processes + stash list) -> 1.2 s later refresh #2 with the same scope. mergeReload usually reports `unchanged`, but the engine still spawns ~8 git processes and the client still downloads and JSON.parses the 3000-row page on the main thread, and setRefreshing(true/false) re-renders App twice. A commit-dialog stage (engine.stage, not under refresh, inFlight==0) also produces an SSE-driven `refresh({status:true})` 400 ms later: 4 more git processes for a status the POST already returned.
- Evidence: const echoDelay = Math.max(muteUntil.current - Date.now() + 100, inFlight.current > 0 ? ECHO_MS + 500 : 0)
...
timer = window.setTimeout(() => {
  const scope: RefreshScope = pendingKind === "status" ? { status: true } : { revisions: true, refs: true, status: true }
  pendingKind = "none"
  void refresh(scope).catch(() => undefined)
}, Math.max(400, echoDelay))
- Skeptic: Confirmed in code: frontend/src/hooks/useRepoState.ts:166-188 only dedupes an identical payload and skips the initial snapshot; every other event arms a timer that unconditionally calls refresh(scope), and the comment at 171-175 says the echo is "deferred past the mute instead of dropped (v0.14.1)". GitHost.Watch.cs:76-89 bumps ChangeVersion on any HEAD/refs/index write regardless of writer and Program.cs:1211-1223 streams it every 500 ms, so every in-app commit/checkout/reset produces a second refresh 0.4-1.2 s later; the existing test useOperationActions.test.ts:234-256 pins it by asserting three page-0 fetches for one merge where the third "brings the same rows". The echo is not free: no server cache on /revisions, /refs, /status (Program.cs:211, 677, 689), the client re-parses a 3000-row page including commit bodies (types.ts:30), setRefs at useRepoState.ts:60 has no equality guard so App re-renders and RevisionGrid.tsx:61 rebuilds tagSet, re-rendering every visible RefChips, and StatusBar.tsx:104 flashes "refreshing…" a second time.
- Fix: The proposed version-stamp fix is sound and the right shape; keep it but note two details. Sample ChangeVersion at request START in the group filter (Program.cs:163) for every response under /repos/{repo}, including the POST /revisions read and the mutation POSTs that already return status, and in useRepoState keep a `seenVersion` ref updated with max(header) by every client call, then in onmessage `if (Number(e.data) <= seenVersion.current) return` before arming the timer. Failure mode is safe: a late FileSystemWatcher delivery yields a higher version than any response saw and simply refreshes as today. Cheap complementary win: guard setRefs with a structural refTreeEquals like statusEquals so an echo (or the 10 s status poll and the focus sweep, which stay) cannot re-render the grid's ref chips when nothing moved. Impact is medium, not high: the echo lands 1.2 s after the action, reads bypass the write gate (Program.cs:180-183) so it never blocks the next mutation, and mergeReload skips setState on unchanged rows; the visible cost is a second "refreshing…" flash, ~8 git processes of engine CPU, and one JSON.parse of a multi-MB page that can be a single 20-50 ms long task on a big repository, felt only if the user is scrolling at that moment.
- Measurement: Run `npx vitest run src/hooks/useOperationActions.test.ts` from frontend/: the merge timeline test asserts `calls.toHaveLength(3)` at line ~250, and the third page-0 request is the echo (change `advance(1300)` to `advance(300)` and it drops to 2, proving the timer is the source). Live in under 5 minutes: open any repo in the app, open the app log (reportTransition("refresh", ...) lines in useRepoState.ts:52/75) or DevTools Network filtered to `revisions?max=3000`, make one commit from the Commit tab: two `refresh N start scope={revisions,refs,status}` ids / two page-0 requests about 1.2 s apart and the `status-refreshing` testid appears twice; a Performance recording shows the second network+JSON.parse+React commit cluster 1.2 s after the commit with no user input.

### 3. Scroll: every visible-range change runs a flushSync React render inside the scroll handler and then rebuilds and redraws the entire graph canvas

- Where: `frontend/src/components/RevisionGrid.tsx:188` — grid-canvas, symptom: scroll-jank, impact claimed high, skeptic says medium
- Mechanism: @tanstack/react-virtual 3.14 notifies with `sync=true` while scrolling and calls `flushSync(rerender)` (node_modules/@tanstack/react-virtual/dist/esm/index.js:78-79); the range changes every 28 px of scroll, i.e. on most frames of a wheel or drag scroll. Each of those renders re-runs the whole RevisionGrid body (80 virtual items mapped, GraphOptionsBar re-rendered, see separate finding), `geometry` recomputes because `virtualItems` is a new array (lines 183-194), and since the render is on the SyncLane React flushes the passive canvas effect synchronously at the end of the commit: the canvas bitmap is reset (line 228-229), the whole 60-80 row band is re-stroked by drawRows and the canvas's `top` inline style moves (line 408), so the compositor gets a brand new 1-2 Mpx texture every frame. The browser's own style/layout/paint for the newly mounted rows comes on top of that, all inside one frame budget.
- Evidence: RevisionGrid.tsx:174-180 `useVirtualizer({ count: rows.length, getScrollElement: () => parentRef.current, estimateSize: () => ROW_HEIGHT, getItemKey, overscan: 12 })`; 183 `const virtualItems = virtualizer.getVirtualItems()`; 188-194 `const geometry = useMemo(() => {...gridGeometry(virtualItems, bandOf)}, [virtualItems, virtualizer])`; 405-410 `<canvas ... style={{ top: geometry.top, height: geometry.height, width }} />`; react-virtual index.js:78 `if (useFlushSync && sync) { flushSync(rerender); }`.
- Skeptic: The mechanism is confirmed, not refuted: virtual-core 3.17.8 calls `maybeNotify` from the native scroll listener with `isScrolling=true` and it is memoised on `[isScrolling, startIndex, endIndex]` (virtual-core index.js:350-372, 419-436), so every 28 px range change reaches react-virtual index.js:78 `flushSync(rerender)` (useFlushSync defaults true; RevisionGrid.tsx:174-180 passes no override). That SyncLane commit re-renders the grid body, `geometry` recomputes because `virtualItems` is a fresh array (RevisionGrid.tsx:183-194), and the passive effect at 223-238 is flushed synchronously, resetting the bitmap (228-229) and calling `drawRows` over ~56 bands (30 visible + 12+12 overscan + 2 neighbours, gridGeometry.ts:20-31). But "high" is overstated by the repo's own numbers: docs/perf/audit-2026-09-16.md:54-58,69 measured this exact 6 s wheel scroll at 10k rows (dev build) as 0 long tasks on two repos (21, max 77 ms on vscode), ~24-31 ms script per range-change render, with the profiler's top self time being React dev JSX and `graphWidth` (since memoised at RevisionGrid.tsx:137) and `drawRows` not in the top four; the hover path, which does the identical grid re-render + full-band redraw, is 32-34 ms dev (~8-12 ms prod, audit line 105) and was rated "fine". RevisionRow is `memo` (RevisionRow.tsx:42) with stable callbacks, so only 1-2 rows mount per step. The "1-2 Mpx texture" is the top end: the graph column is capped at 35% of the body (RevisionGrid.tsx:138-142), so a typical 150-300 px x 1568 px canvas is 0.25-0.5 Mpx at dpr 1.
- Fix: The proposed stationary-canvas + native rAF scroll listener rewrite is disproportionate. Two small changes get most of the win: (1) pass `useFlushSync: false` to `useVirtualizer` at RevisionGrid.tsx:174-180 so the scroll-driven rerender takes the continuous lane instead of SyncLane: React then batches several scroll events into one render and defers the passive canvas effect to after paint, so the redraw leaves the scroll handler and coalesces to at most once per frame; overscan 12 (336 px) covers the one-frame lag for wheel scrolling. (2) In the canvas effect (228-231) only assign `canvas.width`/`canvas.height` when the value actually changes (assigning the same value still discards the bitmap); `drawRows` already `clearRect`s (draw.ts:76), so this removes a realloc per step at zero risk. Also memoise `GraphOptionsBar` (RevisionGrid.tsx:451-456, separate finding) so it stops re-rendering per step. Only if a real WebView2 Performance trace still shows drawRows plus texture upload dominating scroll frames after that is the viewport-sized canvas worth doing.
- Measurement: Run `npm run test:perf -- -g "wheel scroll"` (frontend/tests/perf/interaction.spec.ts:140-154, instrumented by frontend/scripts/perf/metrics.mjs which counts a `.graph-canvas` clearRect as one redraw): the annotation's `redraws` equals the number of wheel steps (~74), proving one full-band redraw per range change, and `framesOver32`/`frameMaxMs` give the cost; change the loop to `page.mouse.wheel(0, 30)` every 16 ms to emulate smooth scrolling and re-run with and without `useFlushSync: false` to see the redraw count and frames-over-32 drop. Alternatively, in the app with Chrome DevTools Performance panel (WebView2 is Chromium), record a 3 s wheel scroll over the grid and look for the `scroll` event task containing `flushSync` > `commitRoot` > `drawRows` on every scroll event.

### 4. Shiki tokenises the entire diff synchronously on the main thread after every diff arrival (measured ~0.1 ms/line: 40 ms for 360 lines, 230 ms for 1800, 760 ms for 7200), and never caches the result

- Where: `frontend/src/components/DiffView.tsx:42` — bottom-panel-diff, symptom: click-latency, impact claimed high, skeptic says high
- Mechanism: useDiffTokens' effect joins every hunk line of the diff (not the visible window) and calls tokenizeLines; highlight.ts:218 runs `highlighter.codeToTokensBase(code, ...)` which is a synchronous TextMate scan with the JS regex engine, only gated by MAX_HIGHLIGHT_CHARS = 400_000 / MAX_HIGHLIGHT_LINES = 20_000 (highlight.ts:18-19), while the engine caps a diff at 1,000,000 chars. It runs in the microtask right after the render that showed the plain diff, so the sequence on a click is: diff appears -> main thread frozen for the tokenisation -> setTokens -> DiffView re-renders every mounted row with new token spans. Benchmarked with the installed shiki 4.4.3 + engine-javascript in Node (same V8 as WebView2): steady state tsx 362 lines = 37 ms, 1806 lines = 231 ms, 7221 lines = 759 ms; C# 1476 lines = 173 ms (first run per grammar ~2x slower). With the 'Full file' context option diffs are whole files, so thousand-line inputs are ordinary. The result lives in DiffView state keyed by `${mode}|${path}|${text}`, so remounting DiffView (finding 3, tab switches) or returning to a previously viewed file re-tokenises from scratch; the virtualised path (>200 lines) tokenises all rows although only ~73 are in the DOM.
- Evidence: DiffView.tsx:31-42 `const raw = text.split("\n") ... lines.forEach((l, i) => { if (l.kind === "other") return; indexes.push(i); code.push(raw[i].slice(1)) }) ... void tokenizeLines(code.join("\n"), lang, mode).then((result) => {`; highlight.ts:218 `const themed = highlighter.codeToTokensBase(code, { lang, theme: mode === "dark" ? DARK_THEME : THEME })`; highlight.ts:18-19 `const MAX_HIGHLIGHT_CHARS = 400_000 // ~400 KB` / `const MAX_HIGHLIGHT_LINES = 20_000`; highlight.ts:12-13 comment already admits 'TextMate tokenizing is synchronous per line; running it over a huge file would stall the UI thread'.
- Skeptic: Confirmed by reading and by reproduction. frontend/src/components/DiffView.tsx:29-53 runs the effect on every `${mode}|${path}|${text}` change, joins every non-header hunk line (not the virtual window) and calls tokenizeLines; frontend/src/highlight.ts:203-222 awaits an already-resolved highlighter promise and then calls `highlighter.codeToTokensBase` synchronously on the whole string, gated only by 400_000 chars / 20_000 lines (highlight.ts:18-19), while the engine caps diffs at 1,000,000 chars (src/engine/PowerGit.Engine/GitHost.Queries.cs:471) and DiffOptionsBar.tsx:104 offers "Full file". The result is only kept in per-instance useState (DiffView.tsx:27), so a remount or a return to the same file re-tokenises; there is no module-level cache. My own Node run with the installed shiki 4.4.3 + engine-javascript: DiffView.tsx 362 lines = 389 ms first run (regex compile) / ~50 ms steady, CommitDialog.tsx 420 lines = 126 ms, GitHost.Queries.cs 1476 lines = 270-290 ms, 5x concatenations 244 / 650 / 1400 ms, i.e. the claim's numbers are if anything conservative.
- Fix: Two cheap steps before any chunking: (1) a module-level LRU in highlight.ts keyed by `${lang}|${mode}|${code}` (or a hash) so remounts, tab switches and revisits are free; (2) switch to `createJavaScriptRawEngine` + `@shikijs/langs-precompiled` (shiki 4 supports it with the pure-JS engine, no WASM) which removes the ~300 ms first-run regex compile per grammar that dominates the first click on each language. Then for size: tokenise only the rows VirtualLines reports as visible plus a margin, carrying shiki's `grammarState` (`codeToTokensBase(chunk, { grammarState })` / `getLastGrammarState`) forward, and schedule the remaining chunks with setTimeout(0)/requestIdleCallback so a diff of 5000 lines never blocks a frame for more than ~30 ms. A Web Worker is not needed and would complicate the Tauri/WebKitGTK chunk loading.
- Measurement: Fastest (under 1 minute, no app): run the ESM bench at the scratchpad path C:/Users/cynak/AppData/Local/Temp/claude/C--dev-public-repo-powergit/224babf1-da0e-4bae-bea3-de03995b9232/scratchpad/bench.mjs copied into frontend/ (`node .bench-tmp.mjs`), which times `codeToTokensBase` on DiffView.tsx, CommitDialog.tsx and GitHost.Queries.cs with the installed shiki. In-app: run frontend/tests/e2e/diff-latency.spec.ts after setting the context select to "Full file" on a commit touching src/engine/PowerGit.Engine/GitHost.Queries.cs, or record a DevTools Performance trace of that click and look for the long task whose stack ends in `codeToTokensBase` right after the DiffView commit; a temporary `console.time("tokenize")` around highlight.ts:218 gives the same number.

### 5. Every refresh — including the 10 s status-only poll and every watcher event — flips `refreshing` true/false and lands its pieces in separate tasks, re-rendering the whole App tree 2–6 times even when nothing changed

- Where: `frontend/src/hooks/useRepoState.ts:48` — render-cascade, symptom: periodic-stutter, impact claimed medium, skeptic says low
- Mechanism: `refresh()` calls `setRefreshing(true)` synchronously (:48), then `client.refs().then(setRefs)`, `client.status().then(...)`, `client.stashes().then(setStashes)` and `reloadHistory()` resolve in separate network macrotasks (each its own render), then `setRefreshing(false)` in the finally (:73). The STATUS_POLL_MS=10 000 interval (:213-215) and every SSE change event (:180-188) go through the same function, so even a poll whose answer statusEquals() discards (statusEquals.ts:11 does the right thing) still produces two full App renders (true → false). `refreshing` is read only by StatusBar (App.tsx:401), but it lives in App state, so each flip re-renders App and the whole un-memoized fan-out: HistoryPane/RevisionGrid + all visible rows (finding 3), BottomPanel + DiffView (finding 2), CommandRail (finding 9), AppDialogs, JobPanel, StatusBar, GitConsole. On top, `reportTransition` (:52, :75) does `report()` (snapshot copy of the ring + listeners) + a console echo + an immediate Tauri IPC `invoke("log_frontend")` (diagnostics.ts:158-163) twice per refresh — 2 IPC round trips every 10 s in steady state.
- Evidence: useRepoState.ts:47-48 `inFlight.current++; setRefreshing(true)`; :59-68 four independent `.then(setX)` chains; :73 `setRefreshing(false)`; :213-215 `window.setInterval(() => { if (document.visibilityState === "visible" && !quiet()) void refresh({ status: true }) }, STATUS_POLL_MS)`; :52 `reportTransition("refresh", ...)`; diagnostics.ts:161-162 `window.clearTimeout(flushTimer); void flush()` → `invoke("log_frontend", { lines })`.
- Skeptic: The mechanism is real but the magnitude is overstated. useRepoState.ts:48 and :73 flip `refreshing` (App-owned state, destructured at App.tsx:69) on every refresh, including the 10 s status poll at :213-215, so steady state is exactly two App renders per 10 s, not 2-6: the poll's `.then(setStatus)` (bails via statusEquals), the Promise.all continuation and the `finally` are microtasks of the same fetch-response macrotask, so React batches them into one commit. The fan-out is real because App.tsx:365 passes an inline `onSelect` lambda, which is a dep of `clickRow`/`contextRow` at RevisionGrid.tsx:284-298, so every App render breaks RevisionRow's memo for all ~30 visible rows; but rows are plain divs, the canvas draw effect (RevisionGrid.tsx ~line 238) is gated on rows/geometry/selected/hovered and does not fire, rows/selected memos are stable, and the only DOM change is the "refreshing…" caption mounting/unmounting in StatusBar.tsx:104-112. The two `invoke("log_frontend")` calls per poll (diagnostics.ts:158-163) are a tiny async IPC each, not a stutter source. Net: a few ms of reconciliation twice every 10 s, invisible when idle, at most one dropped frame per 10 s if it lands mid-scroll; the existing scroll test (tests/perf/interaction.spec.ts:152, longTaskMaxMs <= 50 over 6 s with the poll live) already bounds it under 50 ms.
- Fix: Two small changes, no external store or batching needed: (1) in useRepoState.refresh, skip `setRefreshing` for status-only scopes (`if (!(s.status && !s.revisions && !s.refs && !s.stashes)) setRefreshing(true)`), or arm it via a 300 ms timer cleared in `finally`; that removes both App renders for the 10 s poll and the "refreshing…" flash in the status bar. (2) Wrap App.tsx:365 `onSelect` in useCallback over a `rowsRef` (or pass `setSelectedSha` + `rows` down) so RevisionRow's memo holds across every App render, which also helps every other App-level state change, not just refreshes. Leave reportTransition alone: two tiny IPC posts per 10 s are noise; the four-way result batching is unnecessary for the poll (already one commit) and only matters for full refreshes, where data actually changed.
- Measurement: Add `console.count("RevisionRow")` at the top of the RevisionRow body (frontend/src/components/RevisionRow.tsx:42) and open the dev app on any repo, then leave it idle for 30 s: the count jumps by ~30 (visible rows) every 10 s, twice per poll, alongside the `[powergit] refresh: N start/end` console echoes; React DevTools Profiler over the same 30 s shows six App commits of a few ms each. To bound the cost, run `npx playwright test tests/perf/interaction.spec.ts -g "wheel scroll"` a few times: the poll fires inside the 6 s window in most runs and the `longTaskMaxMs <= 50` assertion at interaction.spec.ts:152 still passes.

### 6. The 10 s status poll toggles `refreshing` true then false on every tick, forcing two App renders (and the full grid/compass cascade) even when status is unchanged

- Where: `frontend/src/hooks/useRepoState.ts:213` — grid-canvas, symptom: periodic-stutter, impact claimed medium, skeptic says low
- Mechanism: `refresh()` always calls `setRefreshing(true)` (line 48) and `setRefreshing(false)` in its finally (line 73). The safety-net poll at 213-215 calls `refresh({ status: true })` every STATUS_POLL_MS = 10_000 while visible; `status` itself is guarded by `statusEquals` (line 61) but `refreshing` is not, so each tick produces two App re-renders. Because App re-renders rebuild `onSelect`, the compass element, `chrome`, etc., each tick re-renders HistoryPane, RevisionGrid, every mounted RevisionRow (finding 4), GraphOptionsBar and GraphCompass (3 MUI Tooltips/IconButtons with function-valued sx) with nothing on screen changing. The full-sweep refresh on window focus additionally does `setRefs(newTree)` which produces new `tagNames`/`remoteNames` arrays (App.tsx:91, useRepoState.ts:250-253) and therefore a new `tagSet`, invalidating every RefChips `ctx`/`ordered`/`fold` memo.
- Evidence: useRepoState.ts:48 `setRefreshing(true)`; 73 `setRefreshing(false)`; 213-215 `const poll = window.setInterval(() => { if (document.visibilityState === "visible" && !quiet()) void refresh({ status: true }).catch(() => undefined) }, STATUS_POLL_MS)`; 10 `const STATUS_POLL_MS = 10_000`.
- Skeptic: The mechanism is real and needs no user interaction: useRepoState.ts:48/73 flip `refreshing` unconditionally inside `refresh()`, the poll at 213-215 calls `refresh({ status: true })` every 10 s while the tab is visible and `quiet()` (204) only blocks it within 700 ms of another refresh, so an idle app gets two App commits per tick (the statusEquals bail-out at line 61 protects `status` only). The cascade also holds: App.tsx:365 passes an inline `onSelect` lambda, HistoryPane and RevisionGrid are not memo'd, RevisionGrid.tsx:281-298 rebuilds `clickRow`/`contextRow` from `onSelect`, and those are props of the memo'd RevisionRow (RevisionRow.tsx:42), so every mounted row (viewport + overscan 12, RevisionGrid.tsx:179) re-renders twice per tick; `nav` from useGraphNav.ts:149 is a fresh object so GraphCompass re-renders too. Severity is overstated though: the canvas draw effect (RevisionGrid.tsx:223-238) has unchanged deps so no redraw, RefChips memos survive because `tagNames` derives from `refs` which a status-only poll never touches, and the audit's own click-to-highlight number (docs/perf/audit-2026-09-16.md:59, 62-65 ms median dev build, including the same App+grid+row render plus canvas redraw and event dispatch) bounds a tick at a few ms in production, well under a frame, and never a long task. The focus-sweep `setRefs` part is a one-off on a user action (focus) whose git cost (/refs 130 ms-1 s, audit line 102) dwarfs the RefChips re-memo. Note the toggle is also what shows "refreshing…" in the StatusBar (App.tsx:401, StatusBar.tsx:104), so it is not purely wasted work.
- Fix: Two one-line changes are enough and simpler than an external store: (1) in App.tsx wrap `onSelect` in useCallback on [rows, setSelectedSha] (rows keep identity across an unchanged refresh via historyMerge), so RevisionRow's memo holds and the cascade stops at RevisionGrid's function body; (2) in useRepoState.refresh gate the indicator on a user-visible scope, e.g. `const visible = s.revisions || s.refs; if (visible) setRefreshing(true)` and mirror it in finally, so the 10 s status poll neither re-renders App nor flashes "refreshing…" in the status bar every tick. Memoising tagNames/remoteNames by content is optional and only affects the focus sweep.
- Measurement: Open the Vite dev app on any repo with React DevTools Profiler recording, leave the mouse alone for 25 s: expect two App commits per 10 s each listing ~50 RevisionRow renders with "props changed: onClick, onContextMenu", and read their durations (expect single-digit to low-tens of ms, no long task). Alternative without DevTools: add `console.count("RevisionRow")` at the top of RevisionRow.tsx:42, watch the count jump by 2 x (visible rows + 24) every 10 s with no interaction, then revert.

### 7. Bottom-panel splitter drag sets App state on every pointermove, re-rendering the whole App (grid, panel, diff rows, status bar) per frame; the files-width SplitHandle does the same to the whole DiffTab

- Where: `frontend/src/hooks/useChromeLayout.ts:50` — bottom-panel-diff, symptom: other, impact claimed medium, skeptic says medium
- Mechanism: `onDividerMove` calls `setBottomHeight(...)` on each pointermove; the state lives in useChromeLayout inside App, so every coalesced pointer frame during the drag renders App and every non-memo child (HistoryPane -> RevisionGrid with a new height, BottomPanel -> DiffTab (finding 4's splits) -> DiffView rows -> DiffOptionsBar, StatusBar, NavRail, OperationBanner...), then the browser relayouts both the grid and the panel. The inner SplitHandle (SplitHandle.tsx:51 `onChange(next)` -> BottomPanel.tsx:347 `onChange: setFilesWidth`) likewise re-renders BottomPanel + DiffTab + CompactFileList + all diff rows per pointermove, and `getContainerWidth: () => panelRef.current?.clientWidth` forces a layout read per move. The commit window already does this correctly: CommitWindowFrame.tsx:99-105 writes the paper's style directly in onPointerMove ('no React render per pointer move; the diff underneath can be thousands of rows') and commits once on release.
- Evidence: useChromeLayout.ts:46-51 `const onDividerMove = (e) => { if (!dragState.current) return; const dy = (dragState.current.startY - e.clientY) / zoom; const max = (contentRef.current?.clientHeight ?? 600) - 140; setBottomHeight(Math.min(Math.max(120, dragState.current.startH + dy), Math.max(120, max))) }`; SplitHandle.tsx:51 `onChange(next)`; BottomPanel.tsx:346-347 `getContainerWidth: () => panelRef.current?.clientWidth ?? filesWidth, onChange: setFilesWidth`; CommitWindowFrame.tsx:80-83 comment `Dragging a grip resizes the paper directly (no React render per pointer move; ...) and hands the final rect to the owner on release`.
- Skeptic: The mechanism is real and fires on every pointermove: useChromeLayout.ts:46-51 calls setBottomHeight per move, the hook lives in App (App.tsx:121), and none of HistoryPane, BottomPanel, RevisionGrid, DiffTab, DiffView, StatusBar, GitConsole, AppDialogs are memo'd (only RepoTree.tsx:421, CommandBar.tsx:174, RevisionRow.tsx:42 are). Worse than the claim states: App passes `onSelect={(i) => setSelectedSha(...)}` inline (App.tsx:363), so RevisionGrid's `clickRow`/`contextRow` useCallbacks (RevisionGrid.tsx:283-298, deps include onSelect) get new identities each App render and every visible RevisionRow's memo breaks too; DiffView (DiffView.tsx:341) rebuilds all up-to-200 plain rows per render, and SplitHandle.tsx:51 -> BottomPanel.tsx:347 `onChange: setFilesWidth` re-renders BottomPanel+DiffTab+DiffView per move with a clientWidth layout read each time. One caveat the reviewer misses: RevisionGrid itself would still re-render per drag frame even after the fix, because a live height change re-fires react-virtual's ResizeObserver; what the fix removes is the App hook body, the row-memo breakage, the diff rows, the status bar and the dialogs tree.
- Fix: Reviewer's fix is right for the height: during drag write `contentRef.current.style.setProperty("--pg-bottom-height", px)` (BottomPanel and FileHistoryView size from the var, App.tsx:379 / FileHistoryView.tsx:346) and call setBottomHeight only in onDividerUp/cancel; same for SplitHandle (inline width via a ref on the files column, onChange on release, cache clientWidth in onPointerDown). Two cheaper additions that also pay off outside the drag: wrap App's inline `onSelect`, `onRetry`, `onOpenRepo`, `onRecover` (App.tsx:363-368) in useStable/useCallback so RevisionRow's memo survives any App render, and memo BottomPanel (its remaining props are already stable per the comment at BottomPanel.tsx:41) so a status-bar note or job tick no longer re-renders the diff.
- Measurement: Add `console.count("App render")` at the top of App() and `console.count("DiffView render")` in DiffView, open the heavy fixture used by tests/perf/interaction.spec.ts (10k rows, Diff tab open on a 200-line file), then in the Playwright/DevTools console run a `page.mouse.down/move(…, {steps: 60})/up` over `[data-testid="panel-splitter"]`: expect ~60 App and DiffView counts (one per frame) and, in the Performance panel, each pointermove task at or above the 32 ms hover→redraw median already recorded in interaction.spec.ts:33 (i.e. below 30 fps during the drag). After the fix the counts should be 1 each.

### 8. Graph canvas is reallocated and fully redrawn on every row hover (and every selection / 28 px scroll step)

- Where: `frontend/src/components/RevisionGrid.tsx:223` — css-compositor-shell, symptom: hover, impact claimed medium, skeptic says low
- Mechanism: The draw effect has `hovered` and `selected` in its deps, and every run first does `canvas.width = Math.ceil(width * dpr); canvas.height = Math.ceil(geometry.height * dpr)` (lines 228-229). Assigning width/height resets the bitmap even when the value is unchanged, so each run reallocates a (graph width x overscanned band) x dpr^2 backing store (roughly 440x1400 px at 125% DPI, ~2.4 MB), then `drawRows` (graph/draw.ts:65-187) re-clips, re-strokes every lane segment and re-draws every node of ~40 bands, plus 12 `getComputedStyle(...).getPropertyValue` reads (draw.ts:85-96), only to paint one hover band (draw.ts:125-128). Each RevisionRow `onMouseEnter` -> `setHovered` (line 300, 431) is a discrete event, so React renders RevisionGrid synchronously, commits, and flushes this effect before the browser can paint the hover: the whole redraw sits on the input task. Moving the pointer down the grid crosses a row every 28 px, i.e. dozens of full canvas redraws per second.
- Evidence: useEffect(() => { ... canvas.width = Math.ceil(width * dpr); canvas.height = Math.ceil(geometry.height * dpr); ... drawRows(ctx, rows, geometry, width + graphScroll, selected, hovered, ancestry, graphOptions) }, [rows, geometry, selected, hovered, width, graphScroll, ancestry, graphOptions])  and  const hoverRow = useCallback((index: number) => setHovered(index), [])
- Skeptic: The mechanism is real: RevisionGrid.tsx:65 keeps `hovered` in state, every RevisionRow mouseenter calls setHovered (RevisionRow.tsx:72 -> RevisionGrid.tsx:300/431), and the draw effect at RevisionGrid.tsx:223-238 lists `hovered` and `selected` in its deps, unconditionally reassigns canvas.width/height (228-229, which resets the bitmap even at the same value) and calls drawRows, which clears and re-strokes every band (draw.ts:76-186) just to paint the one hover band (draw.ts:125-128). The repo's own audit confirms it fires: docs/agents/memories/perf-grid-render.md records a hover -> canvas-redraw median of 32-34 ms in the dev build and tests/perf/interaction.spec.ts:78-103 budgets it at 50 ms. Two details of the claim are wrong, though: mouseenter is a ContinuousEventPriority event in React, so the render and the passive effect run in Scheduler tasks after the event dispatch, not synchronously on the input task, and the visible hover feedback on the text cells is pure CSS :hover (styles/app.css:106-110), so only the graph-column band lags; the user gets hover feedback on the next frame regardless.
- Fix: The resize guard is correct and trivial (compare against the last pixel width/height in a ref, assign only on change; also assign style.width/height only on change). For the band, a smaller change than a new absolutely positioned div (which alters the stacking contract that app.css:83-105 and tests/e2e/selected-row-graph.spec.ts pin down) is to split the drawing into two layers: draw lanes and nodes into an OffscreenCanvas/hidden canvas in an effect keyed on [rows, geometry, width, graphScroll, ancestry, graphOptions], and make the visible canvas's effect on [selected, hovered] do clearRect + two fillRects + one drawImage of the offscreen layer. That keeps the band below the lanes inside the canvas, keeps the pixel-sampling e2e test valid, and drops a hover from a full drawRows to one blit. Also note the "hovered" class added at RevisionRow.tsx:66 has no CSS rule, so `hovered` need not reach the rows at all; the RevisionGrid re-render itself is cheap because RevisionRow is memo'd with stable callbacks.
- Measurement: Run frontend/tests/perf/interaction.spec.ts, test "hovering rows repaints the graph within budget" (or `npm run perf:audit -- --repo <repo> --scenarios hover`, scripts/perf/metrics.mjs wraps CanvasRenderingContext2D.clearRect on .graph-canvas); the annotation prints "hover -> canvas redraw median N ms". Take the number before and after the resize guard; a second run with `hovered` temporarily removed from the effect deps at RevisionGrid.tsx:238 shows the redraw count drop to zero for hovers, which isolates the cost attributable to this claim. Alternatively a DevTools Performance recording while sweeping the pointer over the grid shows one Scheduler task per row crossing containing drawRows, with the CSS :hover paint already committed in the preceding frame.

### 9. Dragging the bottom-panel splitter sets App state on every pointermove, re-rendering the entire tree per move and making emotion insert a new CSS rule per pixel height

- Where: `frontend/src/hooks/useChromeLayout.ts:49` — render-cascade, symptom: panel-open, impact claimed medium, skeptic says medium
- Mechanism: `onDividerMove` calls `setBottomHeight(...)` on every pointermove (:49-54). `bottomHeight` is App state, so each move renders App and its full un-memoized fan-out (all visible RevisionRows via finding 3, BottomPanel + DiffView via finding 2, CommandRail via finding 9, AppDialogs...). BottomPanel then renders `<Paper sx={{ height, ... }}>` (BottomPanel.tsx:372): sx with a changing numeric value serializes to a new emotion class name and inserts a new stylesheet rule for every distinct pixel height, so a 200 px drag inserts ~200 rules and thrashes style recalc. The grid body's ResizeObserver (RevisionGrid.tsx:126-133) and the virtualizer's rect observer also fire per move, adding RevisionGrid renders and canvas redraws (finding 5).
- Evidence: useChromeLayout.ts:49-54 `const onDividerMove = (e) => { if (!dragState.current) return; const dy = ...; setBottomHeight(Math.min(Math.max(120, dragState.current.startH + dy), Math.max(120, max))) }`; PanelSplitter.tsx:13 `onPointerMove={splitter.onDividerMove}`; BottomPanel.tsx:370-373 `<Paper data-testid="bottom-panel" sx={{ height, flexShrink: 0, display: "flex", flexDirection: "column", overflow: "hidden" }}>`.
- Skeptic: The mechanism is real: useChromeLayout.ts:46-51 calls setBottomHeight on every pointermove and App.tsx:121-122 consumes it, so App renders per move; HistoryPane (HistoryPane.tsx:50), BottomPanel (BottomPanel.tsx:98), RevisionGrid, StatusBar, AppDialogs are all plain functions, not memo'd. The RevisionRow memo (RevisionRow.tsx:42) is defeated too, because App.tsx:365 passes `onSelect={(i) => ...}` as a fresh inline arrow, which is a dependency of `clickRow`/`contextRow` (RevisionGrid.tsx:284-299), so every visible row gets new onClick/onContextMenu props each move. BottomPanel.tsx:372 does put the numeric `height` in sx, so emotion hashes a new class per distinct value (fractional under zoom since dy is divided by zoom at :48), though insertRule is microseconds each and Chromium frame-aligns pointermove so it is bounded at ~1 render/frame, not one per pixel. Note the grid must genuinely re-layout during a drag (the body height changes, so the virtualizer/canvas work is inherent); the avoidable cost is the App + ~40 RevisionRow + BottomPanel/DiffView JS render per frame, which is the same fan-out the owner already reported as laggy per click (App.tsx:110-112 comment).
- Fix: Two independent small changes. (1) App.tsx:365: wrap onSelect in useCallback keyed on rows (or route through the existing useStable) so RevisionRow's memo actually holds across every App render, not just drags; this is the largest win and also fixes the per-click fan-out. (2) In useChromeLayout, during the drag write `panelRef.current.style.height` (or a CSS variable on contentRef) from onDividerMove and call setBottomHeight once in onDividerUp/Cancel; render BottomPanel's height via `style={{ height }}` instead of sx. rAF throttling is unnecessary since pointermove is already frame-coalesced; wrapping BottomPanel in memo is optional once (2) removes the per-move App render.
- Measurement: Run the existing e2e `bottom panel splitter resizes` (frontend/tests/e2e/shell.spec.ts:236) with `steps: 200` under `page.tracing` or, faster, open the demo build in Chrome with React DevTools Profiler recording, drag the splitter for ~2 s, and confirm the commit count ≈ pointermove count and that each commit lists every visible RevisionRow plus BottomPanel/DiffView as rendered (reason: props onClick/onContextMenu changed). In the Performance panel, count "Recalculate Style" events and frames >16 ms during the drag; then apply the useCallback on onSelect and re-record to see the rows drop out of each commit.

### 10. GraphOptionsBar (MUI Select, FormControl, 3 ToggleButtons, Tooltip, Dividers, Typography) is not memo'd and re-renders on every RevisionGrid render, i.e. every hover row crossing and every scroll range change

- Where: `frontend/src/components/RevisionGrid.tsx:451` — grid-canvas, symptom: hover, impact claimed medium, skeptic says low
- Mechanism: RevisionGrid re-renders for local state (hovered, graphScroll, widths, bodyWidth) and for every virtualizer range change. It renders `<GraphOptionsBar rows={rows} selectedAuthor={selectedAuthor} highlightRoot={rootRow} onExitHighlight={exitHighlight} />` each time; GraphOptionsBar is a plain function (GraphOptionsBar.tsx:50) so it always re-renders, running ~12 MUI components through useThemeProps and emotion's sx serialisation (its root `sx` is a new object literal with conditional values every render, lines 64-88), plus MUI Select's Input/Popover machinery. All four props are already referentially stable across hover/scroll renders (rows unchanged, string, useMemo'd rootRow, useCallback'd exitHighlight), so the work is pure waste. GraphCompass and headerExtra do not have this problem because they arrive as pre-built elements from HistoryPane and React bails out on the same element reference.
- Evidence: RevisionGrid.tsx:451-456 `<GraphOptionsBar rows={rows} selectedAuthor={selectedAuthor} highlightRoot={rootRow} onExitHighlight={exitHighlight} />`; GraphOptionsBar.tsx:50 `export function GraphOptionsBar({ rows, selectedAuthor, highlightRoot, onExitHighlight }: Props) {` (no memo); 126-139 `<FormControl size="small" ...><Select ... sx={{ fontSize: 12, "& .MuiSelect-select": { py: 0.5 } }}>`.
- Skeptic: The mechanism is real: RevisionGrid.tsx:300/431 calls setHovered on every row mouseenter and :446 on graph scroll, so the grid re-renders, and GraphOptionsBar.tsx:50 is a plain function (no memo) rendered at RevisionGrid.tsx:451 with four props that are indeed stable (rows array, string, useMemo rootRow :90, useCallback exitHighlight :101). Each such render walks Box, Tooltip, FormControl, Select (SelectInput/InputBase/OutlinedInput, though the Menu/Modal returns null while closed), 3 ToggleButtons, 2 Dividers, 3-4 Typography and re-serialises the new sx literal at :64-88, even when collapsed (the inner Box is display:none at :97, still rendered). However the cost is a fraction of a millisecond to ~1.5 ms in production against the already-audited 8-12 ms production hover cost (docs/perf/audit-2026-09-16.md:105,114, "under a frame in production ... not because the user feels it"), and neither hover nor scroll profile names any GraphOptionsBar/MUI function in its top self-time list (audit :69). Waste, yes; sluggishness, no.
- Fix: The proposed fix is correct and sufficient: `export const GraphOptionsBar = memo(function GraphOptionsBar(...) {...})` in frontend/src/components/GraphOptionsBar.tsx:50; the props at RevisionGrid.tsx:451-456 are already referentially stable so React will bail out on hover/scroll renders. useFloatingBar and useGraphOptions state inside the bar still re-renders it when its own state changes, which is the desired behaviour. Hoisting the root sx into two constants is optional and marginal since emotion already caches by serialised string; skip it. Also note memo will not help the one hover path that matters (canvas redraw + RevisionRow re-renders), so do not expect a visible change.
- Measurement: Add `console.count("GraphOptionsBar render")` as the first line of the function body at GraphOptionsBar.tsx:51, open the dev app (or run `npm run test:perf -- tests/perf/interaction.spec.ts -g hovering` in frontend/ which hovers 40 rows) and confirm the count climbs once per row crossed; then in React DevTools Profiler (Chrome, dev build) record a hover sweep over ~20 rows and read GraphOptionsBar's per-commit render duration in the flame chart, which will be roughly 0.5-2 ms in dev next to a 30 ms commit. Repeat with the memo wrapper and it should show "Did not render".

### 11. Every file click in the Diff tab spawns a second whole-commit diff-tree (RenamedFrom) before the diff

- Where: `src/engine/PowerGit.Engine/GitHost.Queries.cs:371` — engine-latency, symptom: click-latency, impact claimed medium, skeptic says medium
- Mechanism: BottomPanel.tsx:280-281 calls engine.diff(commitId, file) per file selection. GetDiff calls RenamedFrom, which runs `diff-tree --root -r --name-status -M <id>` over the whole commit (rename detection again) merely to learn the old path, then `show -- path old`. ListFiles (line 342) had that old path in `parts[1]` and threw it away (`parts[^1]`), so the client cannot pass it. On a 500-file commit each file click is two serial processes with rename detection twice.
- Evidence: string? renamedFrom = RenamedFrom(root, id, path);
...
CommandResult names = Run(root, "-c", "core.quotepath=false", "diff-tree", "--root", "-r", "--no-commit-id", "--name-status", "-M", "--diff-merges=first-parent", id);
- Skeptic: GitHost.Queries.cs:371 calls RenamedFrom unconditionally on every GetDiff, and RenamedFrom (lines 387-406) runs the identical whole-commit `diff-tree --root -r --name-status -M --diff-merges=first-parent <id>` that ListFiles already ran at line 326, serially before the `show` at line 377; there is no cache in GetDiff, in RenamedFrom, or in the /commits/{id}/diff endpoint (Program.cs:307-316). BottomPanel.tsx:280-281 does call engine.diff on each file selection after the first (the first file's diff arrives via changes() and is skipped by the diffFor key at 276-277), so every subsequent file click pays one extra git process spawn plus tree diff with rename detection. ListFiles line 342 indeed discards parts[1] (the old path) and FileChangeDto (Dtos.cs:64) has no OldPath, so the client has nothing to pass.
- Fix: The proposed fix is sound; a simpler engine-only variant needs no client change: memoize RenamedFrom per (root, id) with a small LRU (commit ids are immutable, so the name-status map never goes stale), or have ListFiles/GetChanges populate that map as a side effect so per-file clicks on the current commit skip the diff-tree entirely. Either way `show -- newpath oldpath` stays byte-identical to the current output. Only run the diff-tree lazily when the map has no entry for the commit.
- Measurement: Open the app's Git console (every git child goes through GitHost.RunLogged and is recorded with elapsed ms, GitHost.cs:210-227): select a commit with many files, click a second file in the Diff tab, and read the two consecutive entries per click, `diff-tree ... -M <id>` then `show ... -- <path>`; the diff-tree entry's ms is the pure overhead. For a repeatable number, add a Stopwatch around line 371 in a scratch xunit test in src/engine/PowerGit.Engine.Tests/QueryTests.cs calling GetDiff 20 times on a fixture commit and compare with the RenamedFrom call commented out.

### 12. Command-rail width transition re-lays out the whole content column and cascades through three ResizeObservers for 120 ms

- Where: `frontend/src/components/CommandRail.tsx:88` — css-compositor-shell, symptom: panel-open, impact claimed medium, skeptic says low
- Mechanism: The rail is a flex sibling of the content column with `width: expanded ? 188 : 48` and `transition: "width 120ms"`. Animating width forces a full layout of the grid, bottom panel and ref tree on every frame of the transition (~7 frames), and each frame fires the ResizeObservers on the content area: useChromeLayout.ts:32-35 (`setContentHeight` + `setContentWidth` -> two App re-renders), RevisionGrid.tsx:130 (`setBodyWidth` -> new `budget` for every visible row -> all RevisionRow memo checks fail and re-render, plus the canvas realloc/redraw because `width`/`autoWidth` change), and the virtualizer's own rect observer (another grid render). Every rail toggle therefore costs ~7 x (App render + full layout + canvas redraw) instead of one.
- Evidence: sx={{ width: expanded ? 188 : 48, transition: "width 120ms", flexShrink: 0, ... }}   //  useChromeLayout.ts:32-35  const ro = new ResizeObserver((entries) => { setContentHeight(entries[0].contentRect.height); setContentWidth(entries[0].contentRect.width) })
- Skeptic: The mechanism is real: CommandRail.tsx:87-89 animates `width` on a flexShrink:0 sibling, and App.tsx:313 puts `contentRef` on the flex:1 column next to it, so every transition frame shrinks/grows that column and fires the ResizeObservers at useChromeLayout.ts:32-35 and RevisionGrid.tsx:126-132; the new `bodyWidth` changes `budget` (RevisionGrid.tsx:148, gridGeometry.ts:44 has no hysteresis) which is a prop of every memo'd RevisionRow (RevisionRow.tsx:42), so all visible rows re-render per frame. But the cost is overstated: the two setStates in the RO callback are batched into one App render under React 19 (and setContentHeight bails out because the height does not change), and the canvas realloc/redraw (RevisionGrid.tsx:223-238) only re-runs when `width` changes, which needs `widths.graph` unset AND naturalWidth above the 35% cap (line 138-142), i.e. only on deep histories. Net: ~7 frames of App + grid + ~40 row renders on a rare, localStorage-persisted toggle; a possible 120 ms of jank once per session, not a sluggish UI.
- Fix: Simplest: drop `transition: "width 120ms"` at CommandRail.tsx:88 and fade only the label opacity inside RailItem; the rail then snaps and the content column resizes once (one RO cascade, which is unavoidable and also what happens on every window resize). Merging the two useChromeLayout setStates buys nothing (already batched, and the height setState already bails out). If a width animation is wanted, wrap the rail in a fixed 48px slot with `position: relative; overflow: visible` and animate the inner nav's width so it overlays the content instead of pushing it.
- Measurement: Add `console.count("grid-bodyWidth")` inside the ResizeObserver callback at RevisionGrid.tsx:130 (and `console.count("row")` at the top of RevisionRow.tsx:42), run `npm run dev`, open a repo, click `[data-testid=rail-toggle]` once: expect ~6-8 bodyWidth counts and (visible rows x that number) row counts within 120 ms; with the transition removed, expect 1. Alternatively, record the click in the DevTools Performance panel and count the React commit tasks following each ResizeObserver callback between the click and +120 ms.

### 13. Any refresh whose first page changed resets the layout worker: all loaded revisions are re-laid-out and every GraphRow (with its segment strings) is structured-cloned back to the main thread, then every downstream index rebuilds

- Where: `frontend/src/hooks/useHistory.ts:145` — grid-canvas, symptom: periodic-stutter, impact claimed medium, skeptic says medium
- Mechanism: The layout effect treats the new list as an append only if `revisions[0] === prev[0]` and the old last element is still in place (139-143). After a commit, a fetch that moved a branch on a top row, or any change to page 0, mergeReload produces a new object for at least the first revision (historyMerge.ts:40-54 builds a fresh Revision whenever refs/message differ), so the check fails and the effect posts `{ reset: true, revisions }` with the whole 10-12k list (150). The worker relays out everything and posts back every row (layout.worker.ts:15-16); each row carries ~5-30 RowSegments with three 40-81 char SHA strings, so the reply is tens of MB that the main thread must deserialise, then `setLiveGraphRows(rows)` (95) gives a brand-new `rows` identity: App's withArtificialRows copy, `extendAncestry` full walk (RevisionGrid.tsx:84-89), `graphWidth` scan (137), graphNav's `indexOf` (two Maps over all rows), a new `getItemKey` so the virtualizer rebuilds its 12k measurements, and every mounted RevisionRow re-renders because the row objects are new. This is a one-shot stall of the order of 100 ms+ on a 10k history after each action or external git write.
- Evidence: useHistory.ts:139-151 `const isAppend = prev.length > 0 && revisions.length > prev.length && revisions[0] === prev[0] && revisions[prev.length - 1] === prev[prev.length - 1] ... else { resetSeq.current = seq; post({ seq, reset: true, revisions }) }`; 93-96 `setLiveGraphRows((prev) => (reset ? rows : [...prev.slice(0, from), ...rows]))`; layout.worker.ts:13-16 `if (reset) layouter = createLayouter(); ... postMessage({ seq, reset, from, rows })`.
- Skeptic: The mechanism is real and fires on every reload that changes anything: useHistory.ts:139-143 only takes the append path when `revisions[0] === prev[0]` AND the list grew, but a reload puts new commits at index 0 (mergeReload, historyMerge.ts:68-76 builds `fresh` first), and a moved ref anywhere in page 0 keeps the length equal, so a reload can only ever produce `unchanged` (line 274) or a full `{reset: true, revisions}` post (line 150) — the "append" case the historyMerge.ts:5-11 comment describes never happens for a reload. I measured it with a throwaway vitest (deleted): a 10k synthetic layout (39k segments, 40-char ids) serialises to 11.8 MB, deserialises in ~73 ms on V8, layout takes ~88 ms in the worker, and the downstream markAncestry/graphNav index/scan add ~14 ms on the main thread, so the ~100 ms one-shot main-thread stall claim holds and scales linearly toward ~1 s if the user has scrolled to the 100k HARD_CEILING. The symptom label is wrong though: it is not periodic — status polls and focus refreshes that change nothing return `unchanged` and skip setState — it is one stall per real history change (commit, fetch that moved a page-0 ref, external write), landing asynchronously after the engine's ~300 ms reply, so it reads as a slightly delayed graph update rather than a stutter on the scroll/hover/select hot paths.
- Fix: The proposed "relayout from the first differing index" gains nothing for the common case, because the difference is at index 0 (a new commit lands on top) and lane assignment is top-down, so the worker would still relay out everything. Cheaper, simpler options: (1) keep the worker's previous rows and have it post back only rows whose lane/color/hasRefs/isHead/segments actually differ from last time (most rows below a new top commit are unchanged), with the main thread reusing the old GraphRow objects for the rest — this keeps row identity (RevisionRow memo hits, virtualizer keys) and cuts the reply to a few KB; (2) independently, drop the redundant strings from the transferred RowSegment (`id` is `${childId}:${parentId}` and can be derived on demand, and childId equals the row's own rev.id for most segments), which roughly halves the 11.8 MB payload. Either is a contained change in layout.worker.ts plus the `handle` callback at useHistory.ts:93-96.
- Measurement: Wrap the `handle` callback at frontend/src/hooks/useHistory.ts:93 in `console.time("layout-reply")`/`console.timeEnd` and, in Tauri devtools Performance panel, record a commit (or `git commit --allow-empty` from a terminal) on a 10k-commit repo with autofill complete: the reply shows as a single long "Message" task on the main thread (deserialise + setLiveGraphRows render) of ~100 ms, and no such task appears on a focus/poll refresh that changes nothing. Offline equivalent (under a minute): a vitest in src/graph that calls layoutGraph(syntheticHistory(10_000)) then node:v8 serialize/deserialize on the rows and prints byteLength and ms — I got 11.8 MB, 37 ms serialise, 73 ms deserialise, 88 ms layout.

## Refuted (11)

- **EngineProvider builds a new context value on every App render, invalidating every useEngine() consumer regardless of memo()** (`frontend/src/engine/context.tsx:18`) — The fresh `{ base, repo }` literal at frontend/src/engine/context.tsx:18 is real, but it has no measurable effect on any current interaction: the only memo'd components are CommandBar, RefChips, RepoTree and RevisionRow (grep `memo(` in src/components), and of those only CommandBar calls useEngine() via useCommandItems (commandItems.tsx:58). CommandBar is not mounted in the default "rail" layout (theme/barLayout.ts:5; App.tsx:242-266 renders CommandRail, which is not memo'd), and even in "top" layout its memo is already defeated every App render by the `jobs` prop, because useJobs.ts:230 returns a fresh object literal (App.tsx:261 passes it through). BottomPanel (BottomPanel.tsx:98), DiffTab, CommitFileTree, CommitFileLists and pendingRows are plain non-memo'd functions that re-render on every App render anyway, so the context adds zero renders on the click path, and useDeferredValue at App.tsx:138 still splits the render into urgent/deferred lanes regardless of context identity. The fix is correct hygiene but a prerequisite, not a cause of click latency.
- **BottomPanel is not memo'd, so useDeferredValue(current) still renders the whole panel (DiffView, file list, tree) in the urgent click pass and again in the deferred pass** (`frontend/src/components/BottomPanel.tsx:98`) — The mechanism is real but the "high / sluggish" impact is not: BottomPanel.tsx:98 is unmemoized and its props are identity-stable (useStable in useGridMenus.ts:17 and useStable.ts:13-24, chrome.selectRef at App.tsx:146), so on a click the urgent pass does re-execute the panel with the OLD `deferredCurrent` (App.tsx:137, 375). But that pass has no state change: DiffView memoizes parse and tokens on diff.text (DiffView.tsx:214-216), only the active tab is mounted (default tab 0 = the 118-line CommitInfo, not DiffTab, BottomPanel.tsx:393-436), and the re-render is pure reconciliation with zero DOM writes, i.e. low single-digit ms in production. The commit that introduced this exact design (b025a5124) measured the whole click at ~25 ms to class / <50 ms to paint in production with no long tasks, and tests/e2e/perf.spec.ts:71-106 asserts it; the 140-221 ms "click → Commit tab" number in tests/perf/interaction.spec.ts:34 is the deferred pass plus async engine loads on a 10k-row dev build, which memo() cannot remove because the two extra renders at BottomPanel.tsx:236-255 are BottomPanel-local setState and re-render the panel regardless.
- **Row hover is React state that re-renders RevisionGrid and fully redraws the graph canvas on every mouseenter — only to paint a hover band that CSS could draw** (`frontend/src/components/RevisionGrid.tsx:300`) — The mechanism is real as read: RevisionGrid.tsx:65/300/354 keep hover in state, the canvas effect at :223-238 lists `hovered` in its deps, reassigns canvas.width/height every run (:228-229) and drawRows (draw.ts:76-187) re-reads 14+ custom properties via getComputedStyle and re-strokes every visible band. But "sluggish / high" is contradicted by the repo's own instrumented measurement: docs/perf/audit-2026-09-16.md line 57/108/114 measured hover -> canvas redraw at 32-34 ms median in the DEV build on 10k rows (est. 8-12 ms production) and rated it "Confirmed but cheap ... under a frame in production; listed because the ranking asked for it, not because the user feels it" — and that was BEFORE commit 75bf51af9 memoised RevisionRow/RefChips (only the two rows whose `hovered` boolean flips re-render now, RevisionRow.tsx:12-13,42) and before graphWidth was useMemo'd (RevisionGrid.tsx:137). The "several full redraws per frame" amplification also misreads the browser: Chromium/WebView2 rAF-aligns mousemove, and mouseenter is derived from that single per-frame hit-test, so a sweep fires at most one setHovered per frame regardless of how many 28 px rows the pointer skipped. A regression guard already exists at tests/perf/interaction.spec.ts:78-104 (budget 50 ms dev).
- **Hover and selection are canvas-effect deps: every row crossing and every click resets the canvas bitmap and redraws the whole visible band** (`frontend/src/components/RevisionGrid.tsx:238`) — The mechanism is real (RevisionGrid.tsx:65/300/354 set `hovered`, it is a dep of the canvas effect at :238, and :228-229 reassign canvas.width/height before drawRows), but the repo already measured exactly this path: docs/perf/audit-2026-09-16.md lines 57, 105 and 114 record hover -> canvas redraw at 32-34 ms median in the Vite dev build (about 8-12 ms production), one redraw per hover, "Confirmed but cheap ... under a frame in production; listed because the ranking asked for it, not because the user feels it", and most of that time was React re-rendering ~45 non-memoised rows, which v0.18.16 fixed (RevisionRow.tsx:42 is memo'd, RevisionGrid.tsx:69-74/300 keep the callbacks stable). The bitmap is at most ~56 bands x 28 px x graph width (RevisionGrid.tsx:139 caps it at 35% of the body) x DPR, and drawRows clears and repaints every band anyway (draw.ts:73), so the width reset adds only a backing-store reallocation, not a second raster. The "twice per key repeat" claim misreads :201-215: scrollToIndex only runs when the SHA changed and the row is outside the viewport, so in-view arrow repeats produce one redraw; out-of-view ones need the geometry redraw regardless.
- **GetStatus is 4 serial git processes including a full-index `ls-files -v -z`, run every 10 s and inside every stage/reset/stash POST** (`src/engine/PowerGit.Engine/GitHost.Queries.cs:691`) — The mechanism exists as described (GitHost.Queries.cs:691 status -uall, :731 rev-list via GetAheadBehind at :978, :733 rev-parse @{upstream}, :738 ApplyFileFlags -> GitHost.Files.cs:406 ls-files -v -z; run inside Mutate for POST /stage etc. at Program.cs:192/399-431 and on the 10 s poll at useRepoState.ts:213), but the repo's own harness audit already measured it and cleared it: docs/perf/audit-2026-09-16.md:94 and :111 record the whole GetStatus at 300-400 ms flat from 675 to 13,500 status lines, with ls-files -v at 36-45 ms even on vscode's 18,930 tracked files and rev-list/rev-parse ~40-45 ms each, i.e. process-spawn cost, not index size ("Confirmed, but it is not what hurts"). It cannot produce periodic UI stutter: the git work runs in the C# engine process, not on the WebView main thread, and since v0.18.13 an unchanged poll result keeps the previous status reference (statusEquals, useRepoState.ts:61) so no React re-render happens; the 3.5 s/poll freeze the audit saw was the unvirtualized list re-render, since fixed (PLAN.md:818). The GIT_OPTIONAL_LOCKS=0 claim misreads git: it only suppresses writing the refreshed index, the lstat-vs-cached-stat comparison still short-circuits unchanged files, so only racily-clean entries are re-hashed, not the full tree.
- **refs and stashes are replaced wholesale on every full refresh (window focus, every action, every SSE refs event) with no equality check, cascading into RepoTree, every grid row's RefChips and the dialogs' quote context** (`frontend/src/hooks/useRepoState.ts:60`) — The cascade is real (useRepoState.ts:60/66 store fresh objects; RepoTree.tsx:421 and RevisionRow.tsx:42/RefChips.tsx:42 are memo'd but receive a new tree/tagSet via App.tsx:91 and RevisionGrid.tsx:61), but it fires only once per focus, action or genuine refs change: the sole periodic trigger, the 10 s poll at useRepoState.ts:213-215, is status-only and status already has statusEquals, so there is no periodic source for a stutter. Measured on a 2,500-ref tree (heavy fixture size) the whole client-side re-derivation is ~4 ms (buildTree x3 3.6 ms, name memos 0.4 ms, 40 RefChips 0.05 ms, graphRefCounts 0.01 ms) plus a React commit of ~80 virtualized rows, i.e. about one frame, and AppDialogs.tsx:62 rebuilds tagNames unmemo'd every render anyway. The dominant cost of a focus sweep is the engine's /refs call (~1.5 s on 2,500 refs per tests/perf/heavy.spec.ts:57) and the history reload, which the identity check does not touch.
- **The default command rail is not memo'd, and the memo'd CommandBar is defeated by the `jobs` object that useJobs rebuilds every render — 16 RailItems with MUI Tooltip/ButtonBase/Menu re-render on every App render** (`frontend/src/components/CommandRail.tsx:34`) — The mechanism is real but the cost is not: CommandRail.tsx:34 is a plain function, useJobs.ts:230 returns a fresh object, and even a memo'd CommandRail would still re-render because EngineProvider (engine/context.tsx:18) passes a new `{ base, repo }` context value on every App render and useCommandItems reads it (commandItems.tsx:58). However the rail only re-renders when App does (row selection via `selectedSha`, App.tsx:68-85; refresh), not on hover (hover state is inside RevisionGrid per docs/perf/audit-2026-09-16.md:105), and status polls are identity-guarded (hooks/statusEquals.ts:10) so a quiet 10 s poll renders nothing. A re-render of 16 RailItems is 16 × (Tooltip + ButtonBase + a closed Menu whose Modal returns null at node_modules/@mui/material/Modal/Modal.js:190 after its hooks) — a few ms in dev, ~1-3 ms in production, against a measured 140-221 ms click → Commit-tab path (tests/perf/interaction.spec.ts:33), i.e. low single-digit percent, well under anything a user feels.
- **Selecting another file tears DiffView down and remounts it (blank flash, ~2,800 DOM nodes destroyed and recreated, tokens recomputed) instead of keeping the previous diff stale as the commit path does** (`frontend/src/components/BottomPanel.tsx:279`) — The unmount does happen: BottomPanel.tsx:279 sets {kind:"loading"} on a file change and DiffPane.tsx:53/66-68 then renders null (busy is useDelayed(pending, 200) at BottomPanel.tsx:338, so no spinner for the first 200 ms). But the latency argument does not hold: the new file has different text, so parseGutterLines (DiffView.tsx:205, memo on diff.text) and useDiffTokens (DiffView.tsx:207, keyed on text/path) must rerun either way, and rows keyed by index (DiffView.tsx:342) would have every gutter number, text node and token span patched in place, which is not cheaper than a fresh mount of at most 200 plain rows (VIRTUALIZE_MIN_LINES, DiffView.tsx:60, the cap introduced precisely because the 800-line teardown was the measured slow case). Unmounting is one removeChild plus fiber cleanup of ~200 rows, single-digit ms; the click-to-diff time is dominated by the engine round trip, as tests/e2e/diff-latency.spec.ts documents (~400 ms dev / ~200 ms prod, mostly network). What remains is a cosmetic blank during the round trip, not sluggishness.
- **The Diff tab splits and scans the whole diff text on every render (three full split("\n") passes in useDiffLineSelection) and parses it twice on arrival** (`frontend/src/hooks/useDiffLineSelection.ts:143`) — The mechanism does fire: useDiffLineSelection.ts:143-148 computes rows/selectable/eligibility unmemoized, and nothing in the chain is memo'd (DiffTab, DiffPane, DiffView are plain function components; BottomPanel.tsx:340-348 passes setFilesWidth as onChange so every SplitHandle pointermove re-renders the panel and the hook). But the magnitude does not support "sluggish": measured under vitest with the repo's own functions, all three passes cost 0.11 ms on an 800-line/59 KB diff and 1.85 ms at the engine cap (GitHost.Queries.cs:471 MaxDiffChars = 1 MB, ~16k lines); the DiffView.tsx:28 key concat adds 0.03 ms / 0.8 ms; the duplicate parseGutterLines (BottomPanel.tsx:180 vs DiffView.tsx:205) adds 0.5 ms / 2 ms once per new diff, not per render. Every one of those renders already re-runs DiffView's renderLine over the virtualized window (VirtualLines rows with per-token spans), which is where the frame budget actually goes, so at worst-case size the splits are a sub-3 ms rider on a much larger re-render and at typical sizes they are ~100 µs; nothing here can produce perceptible click latency.
- **A pending-count change rebuilds the `rows` array, which makes RevisionGrid redo its O(n) whole-history work (ancestry marks, graph width, virtualizer keys, nav index) and re-render every visible row** (`frontend/src/App.tsx:76`) — The mechanism exists (App.tsx:76-80 returns a new array from artificial.ts:103 and RevisionGrid.tsx:84-89/137/173 are keyed on it, with ancestry.ts:71 falling back to a full walk), but the trigger is misread: the memo deps are the primitive counts, and useRepoState.ts:61 keeps the previous status object via statusEquals, so a save to an already-modified file, an SSE burst, or the 10 s poll on an unchanged tree rebuild nothing; only a count transition (first save of a clean file, stage/unstage, commit) does, and that transition must repaint the pending row anyway. At the 10k EAGER_CEILING the same passes (markAncestry + visible-row re-render + canvas redraw) are what the audit measured as "ancestry on" at 87-94 ms in the dev build (docs/perf/audit-2026-09-16.md:30), i.e. roughly one frame in production, once per transition, not periodic; graphNav's index (graphNav.ts:21-24) is lazy and only rebuilds if the user then presses a nav chord. Not a periodic stutter and not a sluggish-UI cause.
- **Refresh page is 3000 revisions with body/committer/emails the UI discards: multi-MB JSON.parse on the main thread after every action and every window focus** (`frontend/src/hooks/useHistory.ts:13`) — The chain is real: window focus calls the full-scope refresh (frontend/src/hooks/useRepoState.ts:203-208 -> :59 reloadHistory), which fetches PAGE=3000 (useHistory.ts:13,178,260) and parses via res.text()+JSON.parse (frontend/src/engine/client.ts:137-147), and the engine really emits %ae/%cn/%ce/%b per row (src/engine/PowerGit.Engine/GitHost.Queries.cs:130,179-191) that toRevision drops (historyMerge.ts:13-24). But the magnitude does not support "sluggish": measured on this 17.7k-commit repo, page 0 is 1.65 MB of JSON and TextDecoder+JSON.parse takes 7 ms in V8 (slim DTO: 776 KB, 3.5 ms), far under the 50 ms long-task threshold and invisible as click latency; the 200 ms git log is on the engine, off the main thread, and mergeReload's `unchanged` path (historyMerge.ts:70-84, useHistory.ts:273-277) skips setState so a focus refresh with no change triggers no render or layout at all. Also the -n400 variant of the log took the same ~190 ms (cost is --date-order/--decorate over all refs, not row count), so the small-page half of the fix buys nothing engine-side here.

## Unverified, low-ranked (15)

- [medium] drawRows recomputes lane topology per segment with six linear .find scans and issues 3-5 separate stroke() calls per segment on every redraw (`frontend/src/graph/draw.ts:142`)
- [medium] Every scroll frame of the diff re-runs renderLine for all ~73 mounted rows inside flushSync, with no per-row memo and 40 overscan rows (`frontend/src/components/VirtualLines.tsx:123`)
- [medium] Commit click: whole-commit `git show` patch just to cut out file 1, plus `%D` decoration load and a second diff-tree on fallback (`src/engine/PowerGit.Engine/GitHost.Queries.cs:430`)
- [medium] Startup autofill fetches 10k rows as 3 extra `git log --skip` pages, each re-walking the previous ones and copying the whole array (`frontend/src/hooks/useHistory.ts:284`)
- [medium] A second action waits for the previous action's whole refresh before its engine call starts (`frontend/src/hooks/useJobs.ts:84`)
- [medium] Non-passive wheel listener on the grid scroller makes every scroll gesture wait for the main thread (`frontend/src/components/RevisionGrid.tsx:165`)
- [medium] MUI ripples are enabled on every ButtonBase (toolbar, tabs, menu items, rail, tree checkboxes) (`frontend/src/theme/index.ts:81`)
- [low] Commit-dialog file click runs `ls-files --error-unmatch` before every worktree diff although status already says untracked (`src/engine/PowerGit.Engine/GitHost.Queries.cs:661`)
- [low] The 200 ms busy indicator dims the whole panel content with an opacity transition and forces two more full-subtree renders on any load slower than 200 ms (`frontend/src/components/BottomPanel.tsx:397`)
- [low] File tree rows are un-virtualised MUI Boxes with inline sx, re-serialised for every row on every panel render and all rendered at once for large directories (`frontend/src/components/CommitFileTree.tsx:282`)
- [low] Job polling rebuilds the jobs array every 400 ms during fetch/pull/push, re-rendering App each tick (`frontend/src/hooks/useJobs.ts:63`)
- [low] Each zoom step rebuilds the whole MUI theme and rewrites ~90 :root custom properties (`frontend/src/theme/AppThemeProvider.tsx:236`)
- [low] `transition: all 120ms` plus translucent box-shadow on the three floating pills animates layout and paint on every pointer enter/leave (`frontend/src/components/GraphOptionsBar.tsx:85`)
- [low] backdrop-filter blur on two floating icon buttons over the Diff-tab file list (`frontend/src/components/DiffTab.tsx:187`)
- [low] Engine handlers block thread-pool threads on sync-over-async git waits; /changes uses three threads per request (`src/engine/PowerGit.Engine/GitProcess.cs:40`)

# Second pass — the graph and the bottom-panel resize (2026-09-17, later the same day)

Owner: "The graph is what feels sluggish. Also when I resize the bottom panel it is jumping or rubber band jumps."

Four finders (click / keys / compass, hover and pointer, scroll and paging, panel resize), the first pass's confirmed list given as known so they looked for what was left, a skeptic per finding: 25 raw, 18 distinct, 10 confirmed, 8 refuted. Tasks F–I of v0.18.18.

## The resize jump — six hypotheses

H1 (screen px vs zoomed px) — REFUTED. useChromeLayout.ts:48 `const dy = (dragState.current.startY - e.clientY) / zoom` divides the visual-px pointer delta by the #root zoom (useZoom(), :13); startH (:43), the move-time clamp `contentRef.current?.clientHeight` (:49), the RO contentRect (:33) and the rendered `height` (BottomPanel.tsx:372) are all local px inside the zoomed subtree, as zoom.ts:11-14 documents. No mixed units; at 150 % the panel tracks the pointer 1:1. (Only nit: dy is fractional under non-integer zoom, so the Paper gets sub-pixel heights and one emotion class per fraction — already covered by finding 9.)

H2 (CSS transition on height/flex-basis) — REFUTED. BottomPanel.tsx:372 sx has no transition; the only transitions in the panel/splitter path are PanelSplitter.tsx:27 `transition: "background-color 120ms"` and BottomPanel.tsx:397 `transition: "opacity 120ms"` on the inner content box; MUI 9 Paper.js has no transition rule; app.css has no `transition` at all; FileHistoryView.tsx:346 is the same shape. Nothing animates toward the value.

H3 (clamp max moves under the pointer because the container's height changes mid-drag) — REFUTED. contentRef's Box (App.tsx:313, flex:1/minHeight:0) sits in a definite-height chain (#root calc(100vh/zoom) -> browse-shell height:100% -> flex:1 row -> stretched column), so the panel growing can overflow the column at App.tsx:335 but never enlarge the content box; the RO at :32-35 fires on window/zoom/console/banner changes only. The two clamp sources (RO contentRect float vs clientHeight integer) differ by < 1 px. GitConsole and StatusBar have fixed heights (GitConsole.tsx:86, StatusBar.tsx:63) and do not depend on the panel.

H4 (state-driven height lands late, then catches up) — CONFIRMED but already reported (findings 7/9). Timeline detail worth keeping for the fix: pointermove is rAF-aligned and dispatched inside BeginMainFrame; setBottomHeight is a ContinuousEventPriority update rendered in a Scheduler task that can only run after that frame commits, so even a cheap render puts the panel one frame behind the pointer; a render longer than the inter-frame gap delays the next BeginMainFrame, Chromium coalesces the moves, and the panel advances in multi-frame steps — the 'jumping'. The grid-body RO (RevisionGrid.tsx:130, setBodyWidth with an unchanged width) eagerly bails out, but react-virtual's rect observer calls `rerender()` (useReducer, no eager bailout) on every 28 px range change, adding a DefaultLane RevisionGrid render + canvas effect the following frame; overscan 12 hides it visually. Direct style writes in onDividerMove with a single setState on release remove both the latency and the fan-out.

H5 (splitter position derived from a layout read) — REFUTED. PanelSplitter is a flex sibling between HistoryPane and the Paper (App.tsx:373-379); its position follows from the panel height in the same layout pass, and the drag reads nothing back (delta from startY, :48). No one-frame feedback loop.

H6 (missing pointer capture) — REFUTED. `e.currentTarget.setPointerCapture(e.pointerId)` at :44; pointerup, pointercancel and lostpointercapture all route to onDividerUp (PanelSplitter.tsx:14-16). What is missing on the pointerdown is preventDefault (finding 1) and a button check (finding 4).

Also checked: text selection / body cursor during the drag — not handled (finding 1); stored height vs a smaller window — the panel keeps requestedBottom and the RO-fed clamp applies one frame late, un-clipped column overflows under the status bar for that frame, and re-growth snaps the panel back up (finding 3).

## Confirmed (10)

### 1. Page append: the worker reply (3.35 MB, ~12k nested objects per 3,000-row page) is deserialised on the main thread in one ~25-30 ms task, then everything downstream runs in the same task

- Where: `frontend/src/hooks/useHistory.ts:95` — scroll-paging, interaction: page-append, impact claimed high, skeptic says medium
- Mechanism: Timeline of one append. Task 1 (fetch resolves): JSON.parse of the page (~5-7 ms), page.map(toRevision), array spread, setRevisions + setLoadingTail(false) batched -> App render in which nothing on screen changes -> the layout effect (useHistory.ts:147) posts revisions.slice(prev.length): structured-clone serialisation of 3,000 Revision objects (~5 ms, measured 13 ms for a full structuredClone round trip). Task 2 (worker reply, 30-100 ms later): the browser deserialises {rows} — each GraphRow carries a full copy of its Revision (which the main thread already holds in revisionsRef) plus ~3.9 RowSegments with three 40-81 char strings each — measured 3.35 MB / 31.6 ms at 10k and 3.28 MB / 23 ms at 100k in Node's V8 (same engine as WebView2). Then setLiveGraphRows -> App render: withArtificialRows copy (0.5 ms @10k / 2.2 ms @100k), selected findIndex, navTargets index build (2.7 / 52 ms, finding 2), extendAncestry (2.5-5 / 37-60 ms, finding 3), graphWidth (0.6 / 6 ms), getMeasurements rebuild (0.3 / 5 ms), 60-row reconcile, commit, then the canvas effect: firstParentOf Map rebuild (2.2 / 30 ms, finding 4) + drawRows (~5-10 ms prod). Total ≈ 45-55 ms at 10k rows and ~180 ms near the 100k HARD_CEILING, in one task — that is the second of the two long tasks the 2026-09-16 audit saw. Nothing defers it: onNearEnd fires when the last overscan row is within 60 of the end (RevisionGrid.tsx:220), i.e. 48 visible rows ≈ 1,344 px before the end, and `git log --skip=N -n 3000` answers 0.3-2 s later, so the task lands while the wheel is still turning; setLiveGraphRows is a plain setState, not a transition. Slimming the segment strings (the fix proposed for the reset finding) does NOT move this cost: a slim encoding at 2.10 MB still deserialised in 30 ms — object count, not bytes, is what the deserialiser pays for.
- Evidence: useHistory.ts:93-96 `const handle = ({ seq, reset, from, rows }: LayoutReply) => { if (seq < resetSeq.current) return; setLiveGraphRows((prev) => (reset ? rows : [...prev.slice(0, from), ...rows])) }`; layout.worker.ts:15-16 `const rows = layouter.append(revisions); (self as unknown as Worker).postMessage({ seq, reset, from, rows })`; layout.ts:105-112 pushes `{ rev, lane, color, hasRefs, isHead, segments }` with `rev` the full Revision; RevisionGrid.tsx:220 `if (onNearEnd && rows.length > 0 && end >= rows.length - 60) onNearEnd()`. Scratch vitest (deleted) on syntheticHistory: 10k rows: page reply 3.35 MB, deserialize 31.6 ms; slim (no id/childId strings) 2.10 MB, deserialize 30.3 ms; revisions page structuredClone 13.5 ms.
- Skeptic: Mechanism confirmed as read: layout.worker.ts:15-16 posts every GraphRow with its full `rev` (layout.ts:105-112) and string-laden RowSegments (types.ts:14-21), useHistory.ts:93-95 deserialises that reply on the main thread (Blink materialises `e.data` inside the onmessage task) and calls a plain setLiveGraphRows with no transition, and RevisionGrid.tsx:220 requests the page only 60 rows before the tail so it lands while the user is at the end. A throwaway vitest (deleted) on this repo's layouter reproduced the size: a 3,000-row page at skip 10k is 3.69 MB / 12,304 segments and V8-deserialises in 20-47 ms; the audit (docs/perf/audit-2026-09-16.md:116) already logged these appends as the only long tasks during boot. Two corrections: (a) the "everything in one task" timeline is wrong for React 19 — a worker `message` event is DefaultEventPriority, so the App render+commit runs in a separate Scheduler task and the canvas passive effect in a third, so the deserialise (~20-45 ms) is the biggest piece, not a 45-55 ms monolith; (b) "object count, not bytes" is contradicted by the same test: dropping `rev` alone (keeping the segment strings) halves the reply to 1.76 MB and the deserialise to ~10 ms, so the claim's statement that slimming does not move the cost is only true for the segment-string slimming, not for removing `rev`. Impact is medium, not high: it is one 2-3 frame hitch per 3,000-row page (three during boot autofill, then once per ~84,000 px of tail scrolling), landing at the same instant the new rows appear rather than as sustained sluggishness.
- Fix: Do the cheap half first: post rows without `rev` from the worker (layout.worker.ts:16, e.g. `rows.map(({ rev, ...r }) => r)`) and re-attach the Revision on the main thread in `handle` (useHistory.ts:93) from the revisions that were posted for that seq — keep a `Map<seq, Revision[]>` filled at useHistory.ts:147/150 (or slice lastSent.current) rather than indexing revisionsRef.current, which a reload may have replaced. Measured here that alone halves the reply (3.69 -> 1.76 MB) and the deserialise (~20 -> ~10 ms) and restores Revision identity between `revisions` and `rows`. The typed-array segment encoding is only worth it if the remaining ~10 ms still shows as a long task; the 500-row chunking stop-gap is unnecessary once the reply is under ~2 MB. Wrapping setLiveGraphRows in startTransition is fine but changes little since the update is already DefaultLane and renders in its own task; the scroll-time overlap is better addressed by raising the near-end lookahead at RevisionGrid.tsx:220 from 60 rows to a few hundred so the page is usually in before the wheel reaches the tail.
- Measurement: In frontend/src/hooks/useHistory.ts:116 change the worker handler to `worker.onmessage = (e) => { console.time("layout-reply"); const d = e.data; console.timeEnd("layout-reply"); handle(d) }` (Blink deserialises on the `.data` read, so the timer isolates the structured-clone cost), run `npm run dev` on a repo with >13k commits, scroll to the loaded tail and read the console: expect ~20-45 ms per 3,000-row page in dev; in the DevTools Performance panel the same page shows as a "Message" task of that length followed by a separate ~10-20 ms React task. Offline in under a minute: a vitest in src/graph that does `createLayouter()`, appends syntheticHistory(13_000).slice(0,10_000), then `serialize`/`deserialize` (node:v8) the rows returned by appending the next 3,000 — prints 3.69 MB and 20-47 ms, and 1.76 MB / ~10 ms with `rev` stripped.

### 2. Held ArrowDown/PageDown: one uncoalesced sync render per key repeat, and two grid renders + two canvas redraws per event once the selection reaches the viewport edge

- Where: `frontend/src/components/RevisionGrid.tsx:395` — click-keys-compass, interaction: keyboard, impact claimed high, skeptic says medium
- Mechanism: Frame timeline for a keydown at the bottom of the viewport: (1) keydown is a React discrete event, so `onSelect(next)` at :395 -> App `setSelectedSha` renders App + HistoryPane + RevisionGrid synchronously at the end of the event, and because the lane is SyncLane React flushes the passive effects in the same task. (2) The scroll effect at :201-215 reads `parent.scrollTop`/`clientHeight` (forces the style recalc the class changes just dirtied) and calls `virtualizer.scrollToIndex(selected, {align:'auto'})` (:213) -> virtual-core 3.17.8 `element.scrollTo({top})` + a rAF `scheduleScrollReconcile`. (3) The canvas effect at :223-238 then resets the bitmap and runs `drawRows` over ~56 bands with the OLD geometry (the virtualizer has not seen the scroll yet) - redraw #1. (4) The browser dispatches the async `scroll` event; virtual-core's handler (`_intendedScrollOffset` is set so the early-return is skipped) updates `scrollOffset`, `maybeNotify` sees a 1-row range change and react-virtual 3.14.10 runs `flushSync(rerender)` (index.js:78): RevisionGrid render #2 inside the scroll handler, one row unmounts/mounts (RefChips, measureElement -> RO.observe), and its SyncLane passive effects run again: new `geometry` -> canvas bitmap reset + full `drawRows` - redraw #2. (5) rAF: `reconcileScroll` reads scrollHeight/clientHeight; 150 ms after the last scroll event the isScrolling debounce triggers render #3 (no redraw). There is no `e.repeat` check at :366-371 and no rAF coalescing, so Windows auto-repeat (~30 events/s) queues one such task per event. The repo's own dev numbers are 62-65 ms click->highlight and 24-31 ms per range-change render, i.e. ~90 ms per key event in dev (3x the repeat interval: the keydown queue backs up and the selection keeps moving after the key is released) and ~20-25 ms in production (at the 33 ms budget; over it on a slower machine, or when the author changes and up to 60 rows re-render for `sameAuthor`). PageDown/PageUp repeat is worse: every event is a full range change that mounts ~60 rows in the flushSync render. The deferred BottomPanel render (App.tsx:137) is started after each event and interrupted by the next, so its time slices are wasted too.
- Evidence: RevisionGrid.tsx:366-371 `if (e.altKey || e.ctrlKey || e.metaKey) return ... const cur = selected < 0 ? 0 : selected` (no `e.repeat`); :394-395 `e.preventDefault()\n onSelect(next)`; :201-215 `useEffect(() => { ... if (top >= parent.scrollTop && bottom <= parent.scrollTop + parent.clientHeight) return } virtualizer.scrollToIndex(selected, { align: "auto" }) }, [selected, rows])`; :223-238 `canvas.width = Math.ceil(width * dpr) ... drawRows(ctx, rows, geometry, width + graphScroll, selected, hovered, ancestry, graphOptions)`; node_modules/@tanstack/react-virtual/dist/esm/index.js:78 `if (useFlushSync && sync) { flushSync(rerender); }`; node_modules/@tanstack/virtual-core/dist/esm/index.js:1110-1114 `this._intendedScrollOffset = offset + (adjustments ?? 0); this.options.scrollToFn(...)` and :412-436 the scroll handler that calls `this.maybeNotify()`; docs/perf/audit-2026-09-16.md:59 `20 selections: click -> row highlighted, median 62-65 ms` (dev). No keyboard-navigation perf test exists (tests/perf/interaction.spec.ts covers hover, click, wheel only).
- Skeptic: The mechanism reads as claimed: the grid's onKeyDown (RevisionGrid.tsx:355-396) has no `e.repeat` gate or rAF coalescing and calls `onSelect(next)` at :395 on every repeat, and App.tsx:365 turns that into `setSelectedSha` (useHistory.ts:43), a SyncLane render whose passive effects run in the same task. Once a held ArrowDown pins the selection at the viewport edge, every repeat has a new SHA that is off-screen, so the guard at :204/:211 no longer returns and :213 `scrollToIndex` runs; virtual-core 3.17.8 sets `_intendedScrollOffset` (index.js:1114) so its scroll handler skips the early return (:420) and calls `maybeNotify` (:436), which reaches react-virtual 3.14.10's `flushSync(rerender)` (index.js:78, `useFlushSync` not overridden at :174-180) — a second grid render and a second canvas reset + `drawRows` (:223-238) per repeat, plus one more render after the 150 ms `isScrollingResetDelay` (virtual-core :274-277, useScrollendEvent false) on release. The prior review's line 166 only refuted the in-viewport case; the edge case is the steady state of a held key. Impact is medium not high: the "~90 ms per event" is the dev build (audit-2026-09-16.md:59 click->highlight 62-65 ms, range-change 24-31 ms, both dev with Playwright attached); the audit's hover path with the same render+redraw shape was 32-34 ms dev / 8-12 ms prod, so a production repeat at 30 Hz costs roughly 20-30 ms of a 33 ms interval — a saturated main thread and a stalled deferred BottomPanel (App.tsx:137, prefetch at :146 is already throttled to 250 ms in commitCache.ts:82) rather than a runaway selection, except in dev or on a slow machine. Task B's `useFlushSync: false` already moves render #2 out of the scroll handler; the uncoalesced per-repeat sync render (part 1) is the residual nothing in A-D addresses.
- Fix: Keep part (1) exactly: in the onKeyDown at RevisionGrid.tsx:355, when `e.repeat` is true store `next` (computed from `pendingRef.current ?? selected`) and schedule a single `onSelect(pendingRef.current)` per requestAnimationFrame, cancelling on keyup/blur; first press stays immediate. Drop part (2)'s `virtualizer.scrollOffset = top` write: it relies on the private guard at virtual-core index.js:420 and leaves `scrollDirection`/`isScrolling` stale, and it duplicates task B — with `useFlushSync: false` the scroll-driven rerender leaves the scroll handler and lands in the continuous lane, and the pixel-size check on `canvas.width` (task B) makes redraw #2 a plain repaint, which is the bulk of the cost. If a single render per repeat is still wanted after B, the safe variant is to set `parent.scrollTop` directly in the keydown handler before `onSelect(next)` so the visibility check at :211 returns and `scrollToIndex`'s rAF reconcile loop (virtual-core :1160-1200) never starts; that still costs one batched grid render from the scroll event but no flushSync and no reconcile rAFs. For the test note that Playwright's `keyboard.down` does not auto-repeat: emit the repeats with CDP `Input.dispatchKeyEvent` `{type:'keyDown', autoRepeat:true}` every 33 ms for 1 s, then assert the selected row's index is unchanged two rAFs after keyUp and `__pgPerf.end().longTaskMaxMs <= 50`.
- Measurement: In frontend/, boot the dev app (or any tests/perf run) and in the console run `window.__pgPerf.start()` (scripts/perf/metrics.mjs counts every `.graph-canvas` clearRect as a redraw and records input events and long tasks), click a row, hold ArrowDown for 2 s so the selection reaches the bottom edge, release, then `window.__pgPerf.end()`: at the edge `redraws` is about 2x the number of keydown inputs (one per keydown task, one per flushSync scroll task) instead of 1x, and `longTasks` shows the per-event cost. To see the queue backing up, record the same hold in the Chrome DevTools Performance panel (WebView2 is Chromium; the Interactions track shows each keydown's input delay growing, and each keydown task is followed by a scroll task containing flushSync > commitRoot > drawRows). Under 5 minutes; no code change needed.

### 3. Column-resize drag applies visual clientX deltas to zoomed layout widths: the column edge outruns or lags the pointer by the zoom factor (SplitHandle has the same bug)

- Where: `frontend/src/components/RevisionGrid.tsx:252` — click-keys-compass, interaction: other, impact claimed medium, skeptic says low
- Mechanism: Zoom is CSS `zoom` on #root (theme/index.ts:91), so `--graph-width`/`--col-*` and `clampWidth` are in local (zoomed) px while `PointerEvent.clientX` is in visual px. `dragStart` records `startX = e.clientX` and on every pointermove sets `startWidth + ev.clientX - startX` with no division by zoom, so at 150 % a 10 px pointer move widens the column by 10 local px = 15 visual px (the handle runs ahead of the pointer and the column 'jumps' relative to the cursor); at 70 % it lags. The bottom-panel splitter already does it right (`useChromeLayout.ts:48` divides dy by `zoom`) and CommitWindowFrame divides by zoom (:49); the grid header handles and `SplitHandle.tsx:49` (`startWidth + (e.clientX - startX)`, the Diff/File-tree files column) do not. Not a selection cost, but it is a zoom-factor drag error the review asked to look for and it lives in the grid file.
- Evidence: RevisionGrid.tsx:247 `const startX = e.clientX`; :251-253 `const move = (ev: PointerEvent) => { setWidths((w) => ({ ...w, [key]: clampWidth(key, startWidth + ev.clientX - startX) })) }`; useChromeLayout.ts:48 `const dy = (dragState.current.startY - e.clientY) / zoom`; SplitHandle.tsx:42 `drag.current = { startX: e.clientX, startWidth: widthRef.current }` and :49 `const next = clamp(drag.current.startWidth + (e.clientX - drag.current.startX))`; theme/zoom.ts:14-16 comment `only pointer deltas (clientX/clientY are visual px) need dividing by the zoom`.
- Skeptic: Confirmed in code: theme/index.ts:92 puts CSS `zoom` on #root, and zoom.ts:14 states pointer deltas must be divided by it. RevisionGrid.tsx:247-252 computes `startWidth + ev.clientX - startX` (visual px delta) and writes the result as local-px CSS vars `--graph-width`/`--col-*` (:319-322); SplitHandle.tsx:42,49 does the same and DiffTab.tsx:112 applies it as a local-px `width`. Only useChromeLayout.ts:48 and CommitWindowFrame.tsx:49 divide by zoom; neither RevisionGrid nor SplitHandle imports useZoom/getZoom. However it is a zoom-gated tracking error (no-op at the default 100 %, drag still functional at other zooms), not a per-frame cost, so it does not make the grid feel sluggish.
- Fix: The proposed division is correct but simpler than stated: call `getZoom()` from theme/zoom inside the pointermove closure (`(ev.clientX - startX) / getZoom()`) instead of adding `useZoom()` to the component, so RevisionGrid's `dragStart` useCallback deps ([width, widths] at :264) and SplitHandle need no new hook/dep. Same one-line change in SplitHandle.tsx:49. The "write CSS var during drag, setWidths on pointerup" part is a separate optional perf hygiene item, not needed to fix the zoom error.
- Measurement: Copy the zoom-fixture pattern from tests/e2e/selected-row-graph.spec.ts:61-68 (addInitScript localStorage pg.zoom=1.5), then: read getComputedStyle(grid).getPropertyValue('--graph-width'), page.mouse.move/down on [data-testid=col-resize-graph], move +100 px, up, and read again. Current code yields +100 local px (edge lands 150 visual px from where it started, ahead of the cursor); correct code yields ~+67. Equivalent DevTools check: at 150 % zoom drag the graph header handle and watch the divider outrun the pointer.

### 4. navTargets builds a two-Map index over every loaded row synchronously inside App's render on each page append (2.7 ms at 10k rows, 52 ms at 100k)

- Where: `frontend/src/hooks/useGraphNav.ts:79` — scroll-paging, interaction: page-append, impact claimed medium, skeptic says medium
- Mechanism: `targets = useMemo(() => navTargets(rows, current, memory.current), [rows, current])` runs in App's render whenever `rows` changes identity, which every append does (`[...prev.slice(0, from), ...rows]` in useHistory, then withArtificialRows in App). navTargets calls headSha(rows) -> indexOf(rows), and indexOf is cached in a WeakMap keyed by the array, so a new array means a full rebuild: a bySha Map of N entries plus a children Map with an array per parent, over the whole history, not just the appended page. The 2026-09-17 skeptic recorded this index as 'lazy and only rebuilt if the user presses a nav chord' — that is wrong: the compass needs `targets` on every render, so the build is eager and on the append path. Measured with a fresh array per call: 2.74 ms at 10k rows, 52 ms at 100k (56 ms with pending rows).
- Evidence: useGraphNav.ts:79 `const targets = useMemo<NavTargets>(() => navTargets(rows, current, memory.current), [rows, current])`; graphNav.ts:205-206 `export function navTargets(rows, selected, memory) { const head = headSha(rows)`; graphNav.ts:65-67 `export function headSha(rows) { return indexOf(rows).head }`; graphNav.ts:21-40 `function indexOf(rows) { let idx = INDEX.get(rows); if (idx) return idx; const bySha = new Map(); const children = new Map(); ... for (const row of rows) { bySha.set(row.rev.id, row) ... children.set(p, [row.rev.id]) } ... INDEX.set(rows, idx)`; App.tsx:120 `const nav = useGraphNav({ rows, current, ... })`.
- Skeptic: Mechanism confirmed: useGraphNav.ts:79 memoises navTargets on [rows, current], navTargets calls headSha -> indexOf (graphNav.ts:65-67, 205-206), and indexOf's WeakMap is keyed by array identity (graphNav.ts:19-41), so every append (`[...prev.slice(0, from), ...rows]` at useHistory.ts:95, then withArtificialRows at App.tsx:76-80) rebuilds both Maps over the whole history inside App's render. Reproduced in a throwaway vitest (Node): ~1 ms at 3k rows, 4-9 ms at 10k, 8-20 ms at 30k, 45-125 ms at 100k, versus 0.2-13 ms for the append's own array copy, so the index is the largest single synchronous cost on the append path at 100k. It also reruns whenever the pending counts change (rows deps at App.tsx:79 include unstagedCount/stagedCount), i.e. on every file save in a dirty tree, not just on append. Caveat on feel: the eager load stops at EAGER_CEILING = 10_000 (useHistory.ts:14) where each of the four appends costs under 10 ms, so it is only felt on repositories above ~30k rows, when a page lands during scroll-to-end (onNearEnd, useHistory.ts:405-411) or the pending counts flip.
- Fix: The reviewer's incremental index is right but does not need the withArtificialRows identity fix: index `engineRows` (App.tsx:76, useHistory's array) instead of App's `rows`. Its prefix identity already survives appends (`[...prev.slice(0, from), ...rows]` keeps the same row objects, so `next[0] === prev[0] && next[prev.length-1] === prev[prev.length-1]` holds) and it is untouched by pending-count changes, which removes the per-file-save rebuild for free; artificial rows are already non-targets (navTargets returns early on `selected.artificial`, indexOf skips them, parents in the compass tooltips are real SHAs), so rowOf/firstChildOf/headSha over engineRows give the same answers. Then in indexOf keep a module-level `{ rows, idx }` for the last array and, on a prefix match, walk only `rows.slice(prev.length)` into the same Maps and INDEX.set(rows, idx); on a mismatch (reset, reload with changes) rebuild as today. Pass `engineRows` into useGraphNav as the index source (rows are still needed for `current`).
- Measurement: Add a temporary `src/graph/zz_navperf.test.ts` (vitest, `include: src/**/*.test.ts`) that builds N synthetic GraphRows (rev.id, rev.parents, isHead), calls `navTargets([...base], base[5])` five times per N in {3000, 10000, 30000, 100000} with a fresh array each time and logs performance.now() deltas: expect ~1 / ~4-9 / ~8-20 / ~45-125 ms. In the app: open a 100k-row repository, scroll to the end so onNearEnd appends a page, record in the Performance panel and look for `indexOf` under App's render in the flame chart at the moment the page lands, or wrap indexOf's loop in console.time("navIndex") and read the console.

### 5. extendAncestry is not incremental on appends: it copies the whole marks Map every time, and never takes the incremental path at all while pending rows exist because withArtificialRows fabricates new objects at the top of the list on every call

- Where: `frontend/src/graph/ancestry.ts:76` — scroll-paging, interaction: page-append, impact claimed medium, skeptic says low
- Mechanism: Two parts, same function. (a) The incremental branch starts with `const marks = new Map(previous.marks)` — an O(N) copy of every mark before walking the 3,000 new rows: 2.5 ms at 10k, 37 ms at 100k against 5.2 / 41.6 ms for the full walk, so 'incremental' saves almost nothing at scale. (b) The branch is only entered when `rows[i] === previousRows[i]` for the whole previous prefix; but App's `rows` is withArtificialRows(engineRows, ...) and that function builds fresh `artificial` rows and a fresh `headCopy` on every invocation (artificial.ts:99-103), so whenever the tree is dirty (any pending change — the normal state while working) rows[0..2] are new objects on every append and the full markAncestry walk runs: measured 60 ms at 100k rows, 2.8 ms at 10k. The result feeds the canvas effect in the same task (finding 1's timeline).
- Evidence: ancestry.ts:72 `for (let i = 0; i < previousRows.length; i++) if (rows[i] !== previousRows[i]) return markAncestry(rows, rootId)`; ancestry.ts:76 `const marks = new Map(previous.marks)`; artificial.ts:99-103 `const headCopy: GraphRow = { ...head, segments: [...] }; return [...rows.slice(0, headIndex), ...artificial, headCopy, ...rows.slice(headIndex + 1)]`; RevisionGrid.tsx:84-89 `const next = extendAncestry(previous?.rows, previous?.ancestry, rows, highlightRoot ?? undefined)`; App.tsx:76-80 `withArtificialRows(engineRows, status ? { unstagedCount, stagedCount } : null)`. Scratch vitest: 'extendAncestry with pending rows (+3000) = 60.06 ms (falls back to full walk: true)' at 100k, 2.84 ms at 10k.
- Skeptic: Both halves hold on reading: ancestry.ts:72 bails to markAncestry on any prefix identity change and ancestry.ts:76 copies the whole Map before the walk; App.tsx:76-79 recomputes withArtificialRows whenever engineRows changes (every append, since useHistory.ts:95 replaces the array), and artificial.ts:85-103 always builds fresh artificial rows and a fresh headCopy, so with a dirty tree rows[headIndex..headIndex+2] are new objects and the incremental branch never runs (my scratch vitest confirms fellBack=true). Measured best-of-5 in Node: at 100k rows full walk 14.8 ms, "incremental" 11.8 ms of which the Map copy alone is 10.1 ms, so the incremental path saves ~3 ms; at 10k everything is about 1 ms. It is real but it fires once per 3,000-row page on scroll-to-bottom, not per frame, and is imperceptible below ~50k rows, so the reviewer's 60 ms is the pessimistic end; the whole task also carries graphWidth (2 ms at 100k) and the canvas effect.
- Fix: Keep (b) but the simplest form is a last-call cache inside withArtificialRows (artificial.ts:45): if `head === lastHead && above === lastAbove && counts equal`, return the cached `artificial` rows and `headCopy` (on an append rows[headIndex] and rows[headIndex-1] keep identity, so it hits). That alone makes the prefix check at ancestry.ts:72 pass in the dirty case and also stops the two pending rows and HEAD re-rendering per append. For (a), mutating previous.marks in place at ancestry.ts:76 is safe (marks only ever gain entries; the previous Ancestry is only held by RevisionGrid's ancestryCache ref at RevisionGrid.tsx:83-87 and by the draw effect, which is fine with extra entries) and removes ~10 ms of the 12 ms at 100k; below 50k rows neither change is user-visible, so ship them as a cheap correctness-of-intent fix rather than a perf headline.
- Measurement: Drop a vitest file in frontend/src/graph (e.g. zz_ancestry_append.test.ts) that builds 100k synthetic GraphRows, runs withArtificialRows(prev, {unstagedCount:2, stagedCount:1}) and withArtificialRows(next) and times extendAncestry(prevDirty, markAncestry(prevDirty), nextDirty) vs. extendAncestry(prevEngine, anc, nextEngine) vs. `new Map(anc.marks)`; `npx vitest run src/graph/zz_ancestry_append.test.ts` prints 13.7 / 11.8 / 10.1 ms at 100k and ~1 ms at 10k, and `expect(nextDirty[0]).not.toBe(prevDirty[0])` proves the identity break. In the app: DevTools Performance panel, 100k-commit repo with one modified file, scroll to the bottom until a page loads, look for the markAncestry frame under the RevisionGrid useMemo in the append task.

### 6. Splitter pointerdown is not default-prevented and nothing sets user-select:none for the drag, so the native text-selection drag runs alongside the resize

- Where: `frontend/src/hooks/useChromeLayout.ts:42` — panel-resize, interaction: panel-resize, impact claimed medium, skeptic says low
- Mechanism: onDividerDown only records the start point and captures the pointer; it never calls e.preventDefault(), the handle is a plain div (user-select: auto) and no drag-time user-select/cursor is put on body. Pointer capture retargets pointer and compat mouse events to the handle, but the engine's selection controller is armed by the un-prevented mousedown and keeps extending the selection from the real hit-test under the cursor on every mousemove (Chromium: EventHandler::HandleMouseMoveOrLeaveEvent -> MouseEventManager::HandleMouseDraggedEvent(mev) -> SelectionController::UpdateSelectionForMouseDrag, which forces UpdateStyleAndLayout and a selection repaint). Frame timeline per pointermove: React handler (setBottomHeight) -> engine default action: hit test + forced layout + selection change -> paint invalidation of every grid row (or diff line, moving down) the range now covers. The grid rows are selectable (.grid-row has no user-select rule; only .diff-row-gutter and .author-disc have user-select:none, app.css:194/542) and DiffView is userSelect:'text' (DiffView.tsx:328), so dragging up sweeps a blue selection across the graph rows and leaves it selected after release. The same omission is in SplitHandle.tsx:41-45 (files/diff column), where the sweep runs across diff text.
- Evidence: useChromeLayout.ts:42-45 `const onDividerDown = (e) => { dragState.current = { startY: e.clientY, startH: bottomHeight }; e.currentTarget.setPointerCapture(e.pointerId) }` (no preventDefault, no button check). PanelSplitter.tsx:20-29 sx has cursor/bgcolor only. Contrast the repo's own column handle RevisionGrid.tsx:244-245 `if (e.button !== 0) return; e.preventDefault()` and CommitWindowFrame.tsx:85-93 `e.currentTarget.setPointerCapture(e.pointerId); e.preventDefault()` plus `userSelect: "none"` at :127. SplitHandle.tsx:41-45 `onPointerDown = (e) => { drag.current = {...}; setDragging(true); e.currentTarget.setPointerCapture(e.pointerId) }`.
- Skeptic: The code reads exactly as claimed: useChromeLayout.ts:42-45 records startY, calls setPointerCapture and nothing else (no preventDefault, no button check), PanelSplitter.tsx:20-29 styles only cursor/bgcolor, and SplitHandle.tsx:41-45 is the same shape; no body/#root user-select rule exists (app.css:194/542/581 cover only the diff gutter, author disc and compact file list, and .grid-row at app.css:28/77 sets none), while DiffView.tsx:328 explicitly sets userSelect:"text". The repo's own column handles (RevisionGrid.tsx:244-245) and CommitWindowFrame.tsx:85-93/127 already do preventDefault + userSelect:none, so the omission is real and inconsistent. Pointer capture retargets events but does not cancel the compat mousedown, so Chromium/WebView2 arms the selection controller and extends the selection from the real hit-test on every mousemove, sweeping a highlight across grid rows (dragging up) or diff text (dragging down) and leaving it selected after release. However, the per-move cost is a selection repaint over rows that the height change already repaints anyway (the setBottomHeight-per-pointermove relayout is task D), so this is a visible artifact and a small extra invalidation, not a source of sluggishness or jump on its own.
- Fix: Simplest sufficient fix: in onDividerDown and SplitHandle.onPointerDown add `if (e.button !== 0) return; e.preventDefault()` before setPointerCapture (matching RevisionGrid.tsx:244-245); preventing pointerdown suppresses the compat mousedown, so the selection drag never arms. The body userSelect/cursor belt-and-braces is optional; if wanted, add only `userSelect: "none"` to the two handles' sx (Chromium's CanMouseDownStartSelect returns false for a user-select:none mousedown target, and MUI Box lets you do it in one line) rather than mutating document.body styles that must be restored on cancel/lostpointercapture paths.
- Measurement: In the running app, drag the panel-splitter upward ~200px over graph rows, release, then in the WebView2/DevTools console run `getSelection().toString().length` (non-zero today, 0 after the fix); the same one-liner as a `page.evaluate` assertion appended to frontend/tests/e2e/shell.spec.ts:236 'bottom panel splitter resizes' makes it a permanent gate. To confirm it is not a perf cost, record the drag in the Performance panel: Paint/Layout per frame do not change measurably with the fix, only the selection highlight disappears.

### 7. useGraphNav returns a fresh object every render, so GraphCompass (3 MUI Tooltip+IconButton pairs with function-valued sx, a closed Menu) re-renders on every App render: twice per click, and on every poll/drag/job tick; a compass click adds a focus render plus 120 ms 'transition: all' repaints and steals keyboard focus from the grid

- Where: `frontend/src/hooks/useGraphNav.ts:149` — click-keys-compass, interaction: compass, impact claimed low, skeptic says low
- Mechanism: `useGraphNav` returns `{ targets, loadingTarget, goToParent, ... }` as a new literal each call; HistoryPane builds `compass={nav && <GraphCompass rows={rows} nav={nav} />}` (HistoryPane.tsx:128), so GraphCompass (not memo'd) re-renders whenever App does, not only when `targets` changes: the urgent click pass, the deferred `useDeferredValue` pass (same targets), and every other App render (status poll flips, splitter frames, job polls - the ones being fixed elsewhere). Each render walks three NavButtons (MUI Tooltip with ~15 hooks and a null-returning Popper, IconButton/ButtonBase with ripple + focus-visible hooks, an sx object with two `(t) => alpha(...)` callbacks serialised by emotion each time) plus Menu -> Popover -> Modal hooks; ~1 ms production, 3-4 ms dev, per render. A compass click additionally: mousedown focuses the button -> root `onFocus` -> `setFocused(true)` -> a third GraphCompass render whose new `expanded` value changes border/bgcolor/boxShadow/opacity on the root and all three buttons under `transition: "all 120ms ease"` (root :88, NavButton :302) - 120 ms of small-area repaints - and the MUI ripple. Since focus now sits on the button, subsequent ArrowUp/Down keydowns bubble from the button and never reach `.grid-body`'s onKeyDown (the compass is rendered outside it, RevisionGrid.tsx:457), so arrows are dead until the user clicks the grid. The long-press timer costs nothing on a plain click (armed only when `targets.parents.length > 1`, cleared on pointerup).
- Evidence: useGraphNav.ts:149 `return { targets, loadingTarget, goToParent, goToChild, goToHead, back, forward }`; HistoryPane.tsx:128 `compass={nav && <GraphCompass rows={rows} nav={nav} />}`; GraphCompass.tsx:40 `export function GraphCompass({ rows, nav }: Props)` (no memo); :70 `onFocus={() => setFocused(true)}`; :88 `transition: "all 120ms ease"`; :298 `bgcolor: lit ? (t) => alpha(t.palette.primary.main, 0.12) : ...`; :302 `transition: "all 120ms ease"`; :133-137 `onPointerDown={() => { cancelPress(); if (many) pressTimer.current = setTimeout(openList, LONG_PRESS_MS) }}`; App.tsx:137 `const deferredCurrent = useDeferredValue(current)` (the second pass).
- Skeptic: The sluggishness half is refuted: GraphCompass does re-render with every App render (useGraphNav.ts:149 fresh literal, GraphCompass.tsx:40 no memo), but its parents HistoryPane (HistoryPane.tsx:50) and RevisionGrid (RevisionGrid.tsx:44) are plain functions that re-render on the same App renders, so memo(GraphCompass)+useMemo(nav) trims ~1 ms from a subtree already costing tens of ms, and the drivers (poll, splitter, job ticks) are being fixed elsewhere; the "120 ms transition on click" is misread because `expanded = bar.expanded || focused || menuAt !== null` (GraphCompass.tsx:45) and bar.expanded is already true from onMouseEnter (floatingBar.ts:52) when the pointer clicks, so setFocused(true) changes nothing in the sx and no transition fires. The focus half is confirmed: `{compass}` is rendered at RevisionGrid.tsx:457 as a sibling of `.grid-body` (:351), plain ArrowUp/Down are handled only in that element's onKeyDown (:355; catalog.ts binds only Ctrl/Alt arrow chords), clickRow refocuses the grid only on a row click (:289), and select() in useGraphNav.ts:82-95 never calls focusGrid (src/hooks/focusGrid.ts, whose call sites are App.tsx:167, AppDialogs, useJobs, FileHistoryView), so after a compass click the arrows are dead until the grid is clicked. That is a keyboard-UX defect the user feels, not a performance cost; tests/e2e/graph-nav.spec.ts never presses a plain arrow after a compass click (line 137 clicks a row first), so it is uncovered.
- Fix: Drop the useMemo/memo/transition parts (no measurable win while RevisionGrid and HistoryPane re-render anyway; the transition does not fire on a click). Keep only the focus fix: add `onMouseDown={(e) => e.preventDefault()}` to the IconButton in NavButton (GraphCompass.tsx:282) so a mouse click never moves focus off `.grid-body` (Tab focus still works, MUI's ripple still starts, and the parents Menu's FocusTrap restores focus to the grid instead of the button). Add one e2e step in tests/e2e/graph-nav.spec.ts after `await parent.click()` (line 126): `await page.keyboard.press("ArrowDown")` and assert the selected row moved one down.
- Measurement: Focus: in tests/e2e/graph-nav.spec.ts insert `await page.keyboard.press("ArrowDown")` right after line 126 (`await parent.click()`) and expect the selected row to change; it fails today (focus is on graph-nav-parent, `document.activeElement.dataset.testid` reads "graph-nav-parent"). Perf: React DevTools Profiler in dev, click graph-nav-parent, read the commit's flame chart: GraphCompass self time (~1-3 ms dev) sits under a RevisionGrid/HistoryPane render that is itself tens of ms, and the Elements panel shows no style transition on the compass root (data-expanded already "true" from hover before the click).

### 8. The graph canvas backing store ignores the application zoom, so lanes and nodes are bitmap-upscaled (blurry) at every zoom step other than 100 %

- Where: `frontend/src/components/RevisionGrid.tsx:227` — scroll-paging, interaction: other, impact claimed low, skeptic says low
- Mechanism: The canvas is sized `width * devicePixelRatio` and shown at `width` CSS px, but it lives under `#root { zoom }` (theme/index.ts:92): at zoom 1.25 the element covers width × 1.25 × dpr device pixels while the bitmap has width × dpr, so the compositor resamples it by 1.25 (2.0 at 200 %); the 2 px lane strokes and 10 px nodes lose their edges exactly when the user zoomed in to see them better. CSS zoom does not change window.devicePixelRatio, so the code has no way to notice. Not a speed issue, but the graph is the thing the owner stares at and this is the one drawing surface the zoom does not scale crisply.
- Evidence: RevisionGrid.tsx:227-231 `const dpr = window.devicePixelRatio || 1; canvas.width = Math.ceil(width * dpr); canvas.height = Math.ceil(geometry.height * dpr); canvas.style.width = `${width}px`; ...`; :236 `ctx.setTransform(dpr, 0, 0, dpr, -graphScroll * dpr, 0)`; theme/index.ts:92 `"#root": { zoom, height: `calc(100vh / ${zoom})`, width: `calc(100vw / ${zoom})` }`; grep shows devicePixelRatio is read only here and getZoom() only by the diagnostic snapshot.
- Skeptic: Confirmed by reading the code: RevisionGrid.tsx:227-231 sizes the bitmap from `window.devicePixelRatio` alone and sets style.width/height in local px, while the canvas sits inside `#root { zoom }` (theme/index.ts:92), so at zoom z the element covers width*z*dpr device pixels against a width*dpr bitmap and the compositor bilinearly upscales it by z (1.25 to 2.0 on the ZOOM_STEPS ladder, zoom.ts:22). CSS `zoom` does not alter devicePixelRatio and the grep shows RevisionGrid never reads useZoom/getZoom; zoom.ts:12-14 even documents that the canvas "keeps working in ROW_HEIGHT units", which is true for geometry but not for the backing-store resolution. Not a speed or resize-jump issue, and invisible at the default 100 %, so impact stays low; but for anyone who zooms in, the 2 px lanes and 10 px nodes are the only zoomed surface that goes soft.
- Fix: The proposed fix is right and minimal: in RevisionGrid read `const zoom = useZoom()` (theme/zoom.ts:79), use `const scale = (window.devicePixelRatio || 1) * zoom` for canvas.width/height and for `ctx.setTransform(scale, 0, 0, scale, -graphScroll * scale, 0)`, keep style.width/height in local px, add `zoom` to the effect deps. Combine with the planned "assign canvas.width/height only when changed" guard. Do not reach for ResizeObserver device-pixel-content-box: it would give exact device pixels on Chromium but WebKitGTK does not support it, and useZoom is already the app's cross-engine source of truth.
- Measurement: Copy the addInitScript line from frontend/tests/e2e/shell-zoom.spec.ts:10 with "pg.zoom" = "2", open a repo, then `page.evaluate(() => { const c = document.querySelector('canvas'); return { bitmap: c.width, device: Math.round(c.getBoundingClientRect().width * devicePixelRatio) } })`: today bitmap is half of device (upscaled 2x); after the fix they match. Same check in WebView2 DevTools with `$0` on the canvas, or a 200 % screenshot next to the v0.18.17 visual baseline shows the soft lane edges.

### 9. The clearNote effect on selectedSha cannot bail out eagerly while the deferred pass is pending, so every selection change runs one extra synchronous App render inside the click/key task

- Where: `frontend/src/App.tsx:117` — click-keys-compass, interaction: click, impact claimed low, skeptic says low
- Mechanism: `useEffect(() => clearNote(), [clearNote, selectedSha])` calls `setNoteState(null)` in the passive-effect flush of the urgent render (flushed synchronously because the update lane is SyncLane). React's eager-state bailout requires `fiber.lanes === NoLanes`, but `useDeferredValue(current)` (App.tsx:137) has just merged a DeferredLane into App's fiber for the pending second pass, so the condition fails and a real SyncLane update is enqueued; `flushPassiveEffects` ends with `flushSyncWorkOnAllRoots`, so App's function body executes a second time in the same task (every hook - useEngineSession, useHistory, useRepoState, useGraphNav, useGitActions' closure rebuild, the ~30-element JSX tree) before React notices the state is unchanged and bails out of the children. Also fires for Alt+arrows, compass moves and jumpToRef. ~0.5-1 ms production, 3-5 ms dev, per selection - pure waste while `note` is already null (the common case).
- Evidence: App.tsx:115-117 `const notes = useStatusNote()\n const { clearNote } = notes\n useEffect(() => clearNote(), [clearNote, selectedSha])`; useStatusNote.ts:23-27 `const clearNote = useCallback(() => { if (timer.current) clearTimeout(timer.current); timer.current = null; setNoteState(null) }, [])`; App.tsx:137 `const deferredCurrent = useDeferredValue(current)`; React 19.2.8 `dispatchSetStateInternal`: eager bailout only when `fiber.lanes === NoLanes && (alternate === null || alternate.lanes === NoLanes)`; `updateDeferredValueImpl` does `currentlyRenderingFiber.lanes = mergeLanes(currentlyRenderingFiber.lanes, deferredLane)` when it returns the previous value.
- Skeptic: The mechanism is real and I reproduced it. App.tsx:117 fires `clearNote()` on every selectedSha change and useStatusNote.ts:26 calls `setNoteState(null)` unconditionally; a row click goes through the inline `onSelect` -> `setSelectedSha` (App.tsx:365, useHistory.ts:43), a discrete SyncLane update. In react-dom-client 19.2.8 the eager bailout in dispatchSetStateInternal (line 9144) needs `0 === fiber.lanes`, but updateDeferredValueImpl (line 8859) has just done `currentlyRenderingFiber.lanes |= deferredLane` for App.tsx:137, so a SyncLane update is enqueued; flushSpawnedWork flushes passive effects synchronously for sync lanes (line 18323 `pendingEffectsLanes & 3`) and flushPassiveEffects ends with `flushSyncWorkAcrossRoots_impl` (line 18475), so App's whole body runs again inside the click task before bailing out of the children. A throwaway vitest probe (useState + useDeferredValue + the same effect pattern, since deleted) counted 3 Probe renders per selection unguarded vs 2 with a hasNote ref guard. No other App effect sets state on selection (prefetchCommit only touches the cache; useGraphNav effects touch refs), so this is the sole source of the extra render.
- Fix: Proposed fix is correct; keep the useStatusNote ref-guard variant (set `hasNote.current = next !== null` in setNote, and in the NOTE_MS timeout; `if (!hasNote.current) return` at the top of clearNote). Note a functional updater `setNoteState(n => n === null ? n : null)` would NOT help, since the eager bailout is skipped on fiber.lanes before the reducer runs; only avoiding the dispatch avoids the render. The extra render is App's body only (children bail via bailoutOnAlreadyFinishedWork), so expect sub-millisecond gains in production; fold it into task A rather than treating it as its own item.
- Measurement: Temporarily add `console.count("App render")` as the first line of App() in frontend/src/App.tsx, run the dev app, click a graph row: the count rises by 3 per click today (urgent, extra sync, deferred) and by 2 after the guard. Equivalent 30-second unit check: a jsdom vitest with useState + useDeferredValue + `useEffect(() => setX(null), [sel])` counting renders per `act(() => setSel(...))` gives 3 vs 2 (that is what I ran). In React DevTools Profiler the same click shows two back-to-back App commits with identical output before the deferred one.

### 10. A reload that inserts rows at the top shifts the viewport by k rows: nothing compensates scrollTop, and the auto-scroll effect skips it because the selected SHA did not change

- Where: `frontend/src/hooks/historyMerge.ts:76` — scroll-paging, interaction: other, impact claimed low, skeptic says low
- Mechanism: Appends at the bottom do not move anything (keys are SHAs, `anchorTo` is 'start', total size grows below the fold — confirmed, no jump). But a refresh that finds new commits — a commit, a fetch that moved a page-0 ref, an external write — splices `fresh` in front of the kept tail, so every previously loaded row's index and `start` grow by k × 28 px while `scrollTop` stays; the rows in view get new translateY values and the content the user was reading jumps down by k rows. react-virtual only adjusts scroll for item *resizes* (resizeItem) or in anchorTo:'end' mode; RevisionGrid's effect at 201-215 returns early because `lastScrolledSha` still matches; and the browser's own scroll anchoring cannot be relied on with absolutely positioned, transform-translated rows that remount (overflow-anchor is unset, nothing sets a stable anchor). This is a distinct symptom from the reset's CPU cost (task D): a visible one-row jump after every commit while scrolled away from the top, k rows after a fetch.
- Evidence: historyMerge.ts:76 `next = [...fresh, ...old.slice(k + 1).filter((r) => !seen.has(r.id))]`; RevisionGrid.tsx:201-215 `if (!sha || sha === lastScrolledSha.current) return ... virtualizer.scrollToIndex(selected, { align: "auto" })` (only on a SHA change); RevisionGrid.tsx:174-180 `useVirtualizer({ count: rows.length, ..., getItemKey, overscan: 12 })` with virtual-core's default `anchorTo: "start"` (node_modules/@tanstack/virtual-core/dist/esm/index.js:271); RevisionRow.tsx:78 `transform: translateY(${start}px)`; app.css has no overflow-anchor rule.
- Skeptic: Confirmed on the code: mergeReload (frontend/src/hooks/historyMerge.ts:72-76) splices `fresh` in front of `old.slice(k+1)`, so a page-0 refresh that found new commits shifts every kept row's index by the number of new commits; RevisionGrid positions rows purely by index (`useVirtualizer({count, estimateSize: ROW_HEIGHT, getItemKey})` at RevisionGrid.tsx:174-180, `translateY(start)` at RevisionRow.tsx:78) and virtual-core only compensates scrollTop for item resizes or in anchorTo:'end' (node_modules/@tanstack/virtual-core/dist/esm/index.js:857-876; default anchorTo 'start' at :271). The selection is stored as a SHA (App.tsx:67,82; setSelectedSha only from user/jump paths), so after the reload `selected` changes index but the effect at RevisionGrid.tsx:201-215 returns at line 204 because `lastScrolledSha` still matches, and nothing else touches grid-body's scrollTop (only DiffView/VirtualLines/AppLogView do). Browser scroll anchoring will not cover it: all rows sit at layout offset 0 inside the relative container (RevisionGrid.tsx:399-404) and move only by transform, which Chromium's anchor offset computation ignores, and no overflow-anchor rule exists. So a reload that prepends k commits while the user is scrolled down leaves the content k rows lower under the eye. It is a rare, cosmetic jump (external/fetched commits while reading deep history), not sluggishness, so 'low' stands.
- Fix: Same shape as proposed, two corrections: (1) use `useLayoutEffect` on `rows` in RevisionGrid and compensate by the *start delta* of the anchor key, not `d * ROW_HEIGHT` — sizes are cached by SHA and an expanded row above the fold makes the per-row constant wrong: remember `{key, start}` of `virtualItems[0]` from the previous render (a ref), find the key's new index (Map from rows, or bounded scan around the old index), read `virtualizer.measurementsCache[newIndex].start` (or `newIndex * ROW_HEIGHT` if not measured yet) and do `parentRef.current.scrollTop += newStart - oldStart` when non-zero; (2) skip when `scrollTop === 0` or the key is gone (reset/filter change), and do not touch `lastScrolledSha`. Alternatively have useHistory expose `prepended` from mergeReload (`fresh.length - overlap`) and shift by that many measured starts — but the key-based delta is self-contained and handles the expanded row for free.
- Measurement: In the running window (no code change): open DevTools on the graph, run `const el=document.querySelector('[data-testid=grid-body]'); el.scrollTop=500*28; ` then note `el.scrollTop` and the subject text of the first row whose bounding top >= el.getBoundingClientRect().top; in a terminal `git commit --allow-empty -m probe` (or `git fetch` on a repo with new upstream commits); after the ~1 s reload re-read both: scrollTop is unchanged while the top row's subject is now the row that was one (k) above it — the jump. Same check as a Playwright step in frontend/tests/perf/interaction.spec.ts using repoFixture.ts to commit mid-test: assert the top-row text before/after the reload is equal (fails today, passes with the layout-effect compensation).

## Refuted (8)

- **drawRows rebuilds a SHA -> first-parent Map over the entire history on the first redraw after every rows change (2.2 ms at 10k, 30 ms at 100k), inside the canvas effect on the append task** (`frontend/src/graph/draw.ts:193`) — The code is read correctly: draw.ts:153-155 calls firstParentOf for every drawn segment whenever ancestry !== null, and draw.ts:192-201 caches by rows-array identity, which useHistory.ts:95 (`[...prev.slice(0, from), ...rows]`) and App.tsx:76-80 / artificial.ts:103 (new array on every append or pending-count change) invalidate. But the cost is not one the user feels: a scratch vitest (best of 5, 40-char ids) gave 0.8 ms at 10k and 11 ms at 100k for the Map build, once per 3000-row page (PAGE = 3000, useHistory.ts:13), never on the scroll/hover/select hot paths where the WeakMap hits. At 10k that is a sub-frame cost on a task that already spreads the whole rows array, runs findIndex (App.tsx:82) and, with a dirty tree, a full markAncestry walk because the fresh artificial rows break the rows[i] identity check at ancestry.ts:72; only at the 100k ceiling does it approach a dropped frame or two, and the same task's other O(n) work is of the same order. The draw.ts:189-191 comment ('linear scan over a virtualised window's rows only when the highlight is on') is wrong about both the range and the gating, which is worth fixing, but the impact is low, not medium.
- **Author mark fan-out: selecting a commit by a different author re-renders every mounted row by the old and the new author and re-shapes their author cells (font-weight 500 -> 600)** (`frontend/src/components/RevisionGrid.tsx:421`) — The mechanism is real as read (RevisionGrid.tsx:108-109, :421; RevisionRow.tsx:66; app.css:128-132 weight 600 over theme/index.ts:118 weight 500) but it fires only on a discrete click whose selected author differs from the previous one, and the cost is bounded by the ~56 mounted rows: a throwaway vitest (jsdom, dev React, 56 memo'd RevisionRow with stable props and cached authorIdentity, flipping sameAuthor on every row) measured 5.2 ms per flip vs 0.5 ms when nothing flips, so prod React in WebView2 is 1-2 ms of render plus a style recalc/relayout confined to author cells in fixed-px grid tracks (app.css:34 `var(--col-a)`), well under a frame and masked by the commit-detail fetch and Shiki tokenisation that the same click already triggers. In the owner's single-author repo it fires only when a bot commit is entered or left, and the reviewer's own text concedes "a few ms". Not a sluggishness the user can feel, and unrelated to the bottom-panel resize jump.
- **Three O(rows) scans run on every selection change (two findIndex over the same SHA, one countAuthor even when the pill is collapsed) - measured 0.2 ms each at 12k rows, ~2 ms each at the 100k hard ceiling** (`frontend/src/App.tsx:82`) — The mechanism does fire on a row click (App.tsx:82 and useHistory.ts:169 each run findIndex; GraphOptionsBar.tsx:55 re-runs countAuthor only when the selected author actually changes, since its memo deps are [rows, selectedAuthor]), but my scratch vitest confirms the reviewer's own numbers: at 12k rows findIndex is 0.22 ms and countAuthor 0.28 ms, so a click pays well under 1 ms total against a re-render that already redraws the canvas and the bottom panel; even at the 100k ceiling it is ~5 ms, below one frame and nothing a user can feel. The reviewer's premise that history.current is dead is also wrong: useGitActions.ts:16 declares `history: Pick<History, "current" | ...>` and :41 `const { current } = history` feeds createRef/tag/rebase dialogs at :145-169, so the proposed "delete selected/current from useHistory" would break those. The jumpToRef prefix scan (useHistory.ts:357, ~2 ms at 12k) runs once per ref jump / per extended page, not per click, so it is not on the selection hot path either.
- **RevisionRow hands RefChips two fresh closures on every render, so memo(RefChips) never holds and every row re-render drags its chips and glyph icons along** (`frontend/src/components/RevisionRow.tsx:90`) — The mechanism is real as read: RevisionGrid.tsx:429-434 passes stable useCallback handlers (setExpanded at :271, refContextRow at :301), and RevisionRow.tsx:90/92 wraps them in fresh arrow functions, so memo(RefChips) (RefChips.tsx:42) cannot bail on any RevisionRow render. But it only fires on rows that re-render, and on a click that is the two rows whose `selected` flips (RevisionGrid.tsx:419) plus the sameAuthor rows (:421), which is already its own finding; RevisionRow's other props (measureRow, clickRow, hoverRow, tagSet, budget) are stable, so hover/scroll do not fan out. For the typical row with zero refs, the RefChips re-render is three bailing useMemos (RefChips.tsx:55-60) and one `<span>` (:64) with no children, i.e. single-digit microseconds; only branch-tip rows pay for chip spans and SvgIcon glyphs, and keyed reconciliation produces no DOM writes. Even at 56 rows this is well under 1 ms per click, below anything the user could feel; it is a hygiene item, not a sluggishness cause.
- **Options pill animates layout properties (padding, border-width) with `transition: all` and pops its whole content in on pointer enter, so each crossing of the bottom-left corner costs 7 frames of layout + shadow paint** (`frontend/src/components/GraphOptionsBar.tsx:85`) — The code reads exactly as quoted: GraphOptionsBar.tsx:75/79-80 toggle border 0->1 and px/py, :85 `transition: "all 120ms ease"`, :97 flips the content Box display, and floatingBar.ts:54-55 set `inside` on enter/leave, so border-width and padding do interpolate for ~7 frames. But the pill is `position: absolute` (GraphOptionsBar.tsx:65) so each frame's layout is confined to its ~20-node subtree and the repaint is a ~600x36 px rect; that is well under 1 ms per frame on an otherwise idle main thread and happens once per crossing, which nobody can feel. The only felt cost of entering the pill is the sibling `.grid-body` onMouseLeave -> setHovered(-1) (RevisionGrid.tsx:349-354, pill mounted at :451) which tests/perf/interaction.spec.ts:33 already measures at 32-34 ms on 10k rows; that is the already-listed canvas bitmap reset, and it fires on leaving the body in any direction, not specifically because of the pill. The content pop-in is by design (floatingBar.ts:10 "keep the expanded body mounted and only hide it"). This is a polish item, not a reactivity cost.
- **A page append renders App twice: the setRevisions render changes nothing on screen, its only job is to post the page to the worker, and the postMessage serialisation runs inside that render's effect** (`frontend/src/hooks/useHistory.ts:215`) — The code reading is accurate: useHistory.ts:39 owns `revisions` as state, :215/:282/:343 set it, its only reader is the effect at :134-153 that posts to the worker, and the hook's return (:413-432) never exposes it, so the render it forces changes nothing on screen. But the cost is not something a user can feel: the worker post is a 3000-Revision slice (:147) that v8.serialize'd in 2 ms (792 KB, structuredClone 6 ms) in a throwaway node run here, and the no-change App render is the "few ms of reconciliation" the review already measured for the poll flip (and shrinks further once task A memoises RevisionRow), so the whole setRevisions task is ~5-10 ms, under a frame and under the 50 ms long-task line. The 150-400 ms per-append long task that tests/perf/interaction.spec.ts:62-63 documents is the reply side (worker relayout, multi-MB rows deserialise, setLiveGraphRows at :95 giving `rows` a new identity and cascading into withArtificialRows/extendAncestry/virtualizer keys), which this finding leaves untouched; the extra render is a rounding error on it.
- **The 'Loading more history…' tail is a flex child of .main, so it shrinks the scroller by its height when a page starts loading and grows it back when the page lands — two ResizeObserver-driven relayouts per page and a bottom edge that hides/reveals ~20 px of rows while the user sits at the end** (`frontend/src/components/RevisionGrid.tsx:458`) — The structure is as described (RevisionGrid.tsx:458-462 mounts .grid-tail as a stacked flex sibling after .grid-body; app.css:328-335 has no position:absolute; app.css:66-72 .grid-body is flex:1), and setLoadingTail(false) (useHistory.ts:231) does land in the same batched commit as setRevisions (:215), before the layout worker reply (:93-95) appends the rows. But the cost is not one the user can feel: the near-end trigger fires 60 rows (1680 px at ROW_HEIGHT 28) before the end (RevisionGrid.tsx:220) and a page is 3000 rows (useHistory.ts:13), so the tail toggles twice per 3000 rows, each toggle costing one ResizeObserver-driven RevisionGrid render plus a <=1-row range shift and a ~50-band canvas redraw, on a path that already renders per scroll event; the width observer at :130 sets an unchanged bodyWidth and bails out. For a user 'sitting at the end' nothing moves: scrollTop is kept and the 20 px bar simply covers the bottom of the last row, which is exactly what the proposed absolute overlay would also do, so the fix changes nothing visible in that case. The only real artifact is narrow: if the user reaches the shrunken max scrollTop while the fetch is in flight (fling past 1680 px in the few hundred ms a 3000-row page takes), the tail unmount clamps scrollTop by ~20 px and the rows nudge down once before the appended rows appear tens of ms later.
- **Hover follows scroll: Chromium re-hit-tests under a stationary pointer after each scroll step and fires mouseenter, so scrolling and keyboard navigation pay an extra RevisionGrid render + canvas redraw per step and the hover band hops rows** (`frontend/src/components/RevisionGrid.tsx:300`) — The code path is as quoted (RevisionGrid.tsx:300 hoverRow -> setHovered, :431, :238 `hovered` in the canvas-effect deps) and Chromium does re-hit-test after scroll, but the cost is bounded by the reviewer's own throttle (one synthetic mouseenter per ~100 ms or once at scroll end, versus a scroll render every step) and it is already inside the measured budget: tests/perf/interaction.spec.ts:143 parks the pointer at the grid-body centre for the whole 6 s wheel scroll, so docs/perf/audit-2026-09-16.md:54-56 (0 long tasks on two repos, longTaskMaxMs <= 50 guard) already includes every scroll-induced hover. After tasks A-D the extra render is two memo'd RevisionRows flipping a `hovered` boolean that no CSS rule uses (RevisionRow.tsx:66, app.css has no `.hovered` rule) plus a blit, roughly 1-2 ms per 100 ms of scrolling; the "band hops" is the same thing the browser does natively to the text cells via `.grid-row:hover` (app.css:106-110), and keyboard navigation only scrolls when the selection leaves the viewport (RevisionGrid.tsx:201-213), so it is one hop per page, not per key. Not a felt cost; leave it.