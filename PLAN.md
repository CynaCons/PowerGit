# PowerGit

**Goal:** Git Extensions Browse as React + Tauri, C# git engine sidecar, Windows + Linux. Dev on Windows. Ship `.zip` portable (Windows) and `.AppImage` (Linux).

**Philosophy:** Git Extensions is the *behavior* spec, not the visual spec. Material React chrome. Plan → design → code → e2e → update this file. After v0.3, each UI piece gets an owner design demo (on request).

**Verify:** `npm run test:e2e` once (no retries, stop at first fail, no screenshots). `test:visual` / Chrome shots only when the owner asks. See `AGENTS.md` → How we verify.

**Upstream pin:** `7f75cee29`

---

## Current Status

The current iteration is the last heading below that is not marked COMPLETE
(`powerplan get_current_iteration`). This header never names a version, so it
cannot go stale. Live dev: UI `http://127.0.0.1:1420` · engine `http://127.0.0.1:7733`
(bearer-token gated since v0.13.0).

---

## v0.0 — Fork (COMPLETE 2026-08-20)
- [x] Clone latest Git Extensions + submodules; GitHub fork synced

## v0.1 — Methodology (COMPLETE 2026-08-20)
- [x] PRD, AGENTS, CLAUDE, SRS, PowerSpawn/powerplan

## v0.2 — Engine sidecar (COMPLETE 2026-08-20)
- [x] `net10.0` Kestrel git host: health, open, current

## v0.3 — App scaffold (COMPLETE 2026-08-20)
- [x] Tauri + React Material shell, GE graph, hover fix

## v0.4 — Browse product

### v0.4.0 — Mini SHA-1 + live revision stream (COMPLETE)
- [x] Engine `GET /revisions` (`--topo-order --branches`, not `--all`/`--tags` so stale remotes don’t scatter the graph)
- [x] Graph from real `git log`; SHA-1 column (~7 chars)
- [x] E2e: sha-cell visible

### v0.4.1 — Bottom panel (COMPLETE)
- [x] Engine commit detail, name-status files, unified diff
- [x] Commit / Files / Diff tabs; file click loads diff
- [x] E2e: files tab visible

### v0.4.2 — Left panel repo-objects tree (COMPLETE)
- [x] Branches, Remotes, Tags, Submodules from engine
- [x] Current branch highlighted; click selects tip in graph

### v0.4.3 — Settings (COMPLETE)
- [x] Gear on navrail; `user.name`, `user.email`, `core.autocrlf`
- [x] E2e: settings dialog opens

### v0.4.4 — VS Code default tools (COMPLETE)
- [x] Auto-detect VS Code; settings Apply for editor/diff/merge

### v0.4.5 — Commit badge + overlay (COMPLETE)
- [x] Dirty count badge; unstaged/staged lists; stage click; commit needs staged+subject

### v0.4.6 — Navrail recents picker (COMPLETE)
- [x] History icon; card overlay (title, path, branch); persist recents.json

### v0.4.7 — Owner review punch list (2026-08-21) (COMPLETE)
Settings
- [x] Field labels clipped in the settings panel (titles unreadable)

Main Browse
- [x] Graph / main card too round — smaller Material radius, not pill-like
- [x] Bottom panel resizable (drag splitter vs the revision grid)

Left panel
- [x] Real tree, not a flat/fake list: hierarchy, expand/collapse, nested remotes and tags as tree nodes

Commit overlay
- [x] Current popup overlaps, clips, and does not match Git Extensions FormCommit. Rebuild the *layout* of the original commit window (unstaged | staged | diff | message). Material chrome only — do not restyle WinForms, do match the arrangement.

Bottom panel
- [x] Per-commit **file tree** of the repo at that revision (GE File tree), not only a flat change list
- [x] Files and diff in a **left–right split** (GE split). Tabs-only stacked view is not enough
- [x] Diff colors match Git Extensions (add / remove / header / hunk). Do not invent a new palette

Graph context menu
- [x] Right-click on a revision/ref is missing. Add the Git Extensions basics: Checkout Branch, Reset branch, Rebase. Behaviour identical to Git Extensions (`RevisionGridMenuCommands` / FormCheckoutBranch, FormResetCurrentBranch, FormRebase) — same guards, prompts, and git operations. Material menu chrome only.

### v0.4.8 — Owner review punch list #2 (2026-08-21) (COMPLETE)
> Owner feedback round 2: appbar/navrail integration, full ref graph with GE colors, real file tree with blob viewing, floating diff-options toolbar, commit-dialog selection overhaul with file context menu + gitignore preview, GE-style left tree, author highlight. Stash handling planned for v0.4.9.
- [x] v0.4.8-1: AppBar visually integrated with navrail (single continuous chrome)
- [x] v0.4.8-2: graph always shows all local branches + tags, GE lane colors
- [x] v0.4.8-3: bottom panel resize — full-width visible splitter
- [x] v0.4.8-4: File Tree tab = full repo tree at commit; open unchanged files via new engine blob endpoint
- [x] v0.4.8-5: floating diff-options bar (context lines / full file / ignore whitespace), compact-on-idle, in commit dialog + bottom panel diff; engine diff options
- [x] v0.4.8-6: commit dialog multi-select (shift/ctrl, no text selection), stage/unstage selection
- [x] v0.4.8-7: right-click menu on staged/unstaged lists: stage/unstage/delete/add-to-gitignore; gitignore preview dialog with match count
- [x] v0.4.8-8: left tree GE look — icons per ref type, bold current branch
- [x] v0.4.8-9: author highlight — selecting a commit tints other commits by same author
- [x] v0.4.8-10: left tree right-click menus per element type — branch (checkout/delete), remote (fetch/configure), tag (checkout/delete), submodule (open); engine ops behind them

### v0.4.9 — Stash handling (2026-08-21) (COMPLETE)
> Final 0.4.x iteration: GE-parity stash handling — engine stash ops, stashes visible in graph, topbar mini menu + manage dialog.
**Goal:** Stash handling at Git Extensions parity: stash/apply/pop/drop from the UI, stashes visible in the revision graph, FormStash-style management dialog.
- [x] v0.4.9-1: engine stash ops — GET /stashes, POST /stash, /stash/apply, /stash/drop + tests
- [x] v0.4.9-2: stashes as graph nodes (include refs/stash in revisions) + distinct ref chip
- [x] v0.4.9-3: topbar Stash mini menu + FormStash-style manage dialog (list, stash w/ message+options, apply/pop/drop)

### v0.4.10 — Visual polish, resolutions, Linux check (2026-08-21) (COMPLETE)
> Polish + infrastructure: menu typography, commit-dialog row unification, graph offset, GE app icon, multi-resolution visual test suite, Docker Ubuntu check, regular pushes.
**Goal:** Fix menu/dialog visual defects, take over the GE app icon, and establish resolution + Linux verification so layout bugs (e.g. fullscreen clipping) are caught automatically.
- [x] v0.4.10-1: right-click menus — app font + compact size (theme-level)
- [x] v0.4.10-2: commit dialog rows unified on CompactFileList (kill offset divergence)
- [x] v0.4.10-3: graph left padding inside rounded container
- [x] v0.4.10-4: GE app icon for Tauri build
- [x] v0.4.10-5: multi-resolution e2e suite incl. fullscreen clipping check
- [x] v0.4.10-6: Docker Ubuntu smoke (engine + UI + e2e headless)

## v0.5 — Welcome / home (parked) · Release track
Recent repos with diff stats, Open, Clone.

- [ ] GitHub Pages showcase site: new features + visuals screenshots
- [ ] Release skill for opencode and claude agents (.opencode/skills + .claude/skills): how to cut a release, update Pages visuals and main README
- [ ] Build and attach binaries to GitHub releases (Windows portable zip, Linux AppImage)

### v0.5.1 — Engine sidecar spawn (2026-08-21) (COMPLETE)
> Make shipped builds self-sufficient: bundle the engine as a Tauri sidecar spawned at startup, so end users don't need to run anything manually.
**Goal:** Engine auto-start inside the packaged app (sidecar), verified by a real release build smoke test.
- [x] v0.5.1-1: publish engine as self-contained single-file exe into src-tauri/binaries with target-triple name
- [x] v0.5.1-2: tauri externalBin + shell plugin + Rust startup spawn on :7733
- [x] v0.5.1-3: release build smoke — packaged app spawns engine, health ok, no manual steps

### v0.5.2 — Windows artifacts (2026-08-21) (COMPLETE)
> One-command Windows artifact: portable zip (app + sidecar engine) plus the NSIS installer, versioned from tauri.conf.json.
**Goal:** scripts/package-windows.ps1 produces distributable, self-sufficient Windows artifacts.
- [x] package-windows.ps1: portable zip + NSIS installer, zip smoke-verified self-sufficient

### v0.5.3 — Release pipeline (2026-08-21) (COMPLETE)
> Tag-triggered GitHub Actions workflow producing Windows zip/installer and Linux AppImage/deb, attached to a GitHub release.
**Goal:** Pushing a vX.Y.Z tag builds and attaches release binaries for both platforms.
- [x] release.yml: windows (zip+installer) + linux (AppImage+deb) jobs, sidecar sh script

### v0.5.4 — Pages showcase (2026-08-21) (COMPLETE)
> GitHub Pages showcase: captured screenshots, static site, auto-deploy workflow.
**Goal:** docs/site deployed to GitHub Pages on push, showing real feature screenshots from real builds.
- [x] showcase site + screenshots + pages.yml deploy workflow

### v0.5.5 — Release skill (2026-08-21) (COMPLETE)
> release skill registered for opencode (.opencode/skills) and claude (.claude/skills): preflight, version bump, artifacts, tag, notes, Pages refresh.
**Goal:** Any agent can cut a release by following the skill.
- [x] release skill in .opencode/skills and .claude/skills

## v0.6 — Showcase + cleanup

### v0.6.1 — README scope rewrite (2026-08-22) (COMPLETE)
> Rewrite README to state the fork's scope crisply: a new, modern, portable, cross-platform frontend for Git Extensions.
**Goal:** A visitor understands within seconds what PowerGit is, what it is not yet, and why it exists.
- [x] README: scope-first rewrite (modern/portable/cross-platform frontend for GitExtensions)

### v0.6.2 — React showcase site (2026-08-22) (COMPLETE)
> Replace the static HTML page with a Vite + React site: hero, feature cards, screenshot gallery.
**Goal:** docs/site replaced by a React webapp under website/, built in CI and deployed to Pages.
- [x] website/: Vite+React+MUI hero site (features, screens)
- [x] pages.yml builds website + demo bundle, deploys combined artifact

### v0.6.3 — Live demo embed (2026-08-22) (COMPLETE)
> Embed the real PowerGit frontend (built with a /demo/ base path) in an iframe — it renders the revision graph from its built-in synthetic history when no engine is present, so visitors interact with the actual UI, not a video.
**Goal:** Live interactive demo of the real UI on the Pages site.
- [x] demo: real frontend built at /demo/ base, iframe embed with sample-data notice

### v0.6.4 — Remaining cleanup sweep (2026-08-22) (COMPLETE)
> Sweep up everything left from the repo analysis: sync engine version constant, promote the live-demo probe to a proper script, refresh stale PLAN "Later" list and AGENTS commands, then a full packaged-build verification pass.
**Goal:** No known loose ends left in the repository outside explicitly backlogged items.
- [x] sync engineVersion constant with release version (drifted at 0.4.0)
- [x] check-live-demo.cjs -> npm run test:live, documented in release skill post-deploy step
- [x] refresh PLAN Later list + AGENTS commands (sidecar/packaging/stashes now shipped)
- [x] full verification: engine tests, e2e, resolution, unit, packaged-build smoke

## v0.7 — File Tree correctness + GE command bars

### v0.7.0 — File Tree correctness + GE-style command bars (planned) (2026-08-24) (COMPLETE)
**Goal:** Fix nested File Tree browsing (files invisible below depth 1), close the e2e coverage gap that missed it, and bring the topbar split-buttons and revision context menu closer to Git Extensions design fidelity.
- [x] Engine: normalize ListTree (GitHost.Queries.cs) so entry `name` is relative to the requested directory — strip the path prefix git ls-tree emits when `path` is passed; add engine test asserting nested fetch of docs/srs returns bare basenames
- [x] UI: fix CommitFileTree child path/label building to match normalized DTO; verify blob pane opens files at depth >= 2
- [x] E2E coverage: new spec that expands nested directories in the File Tree tab (e.g. frontend -> components) and asserts file rows render; must fail on the pre-fix engine
- [x] Topbar: Git Extensions-style split buttons (icon + dropdown caret) for Commit / Stash / Fetch / Pull / Push, with secondary actions (amend, force push variants, manage stashes) in the dropdown menus; match GE iconography and ordering
- [x] Revision context menu: expand toward GE parity (create branch/tag here, copy SHA, cherry-pick/revert placeholders as feasible); keep existing checkout/reset/rebase entries and testids stable
- [x] Verification: dotnet test src/engine/PowerGit.Engine.sln + npm run test:e2e green; smoke npm run dev once

## v0.8 — Large-repo responsiveness

### v0.8.0 — Large-repo responsiveness + owner feedback #3 (2026-08-24) (COMPLETE)
> Owner feedback 2026-08-24: (1) show a progress indicator in the top app bar during fetch/pull/push; (2) fonts on Ubuntu AppImage look low-quality and too grey; (3) not all branches are visible by default — want Git Extensions behaviour, not just checked-out branches; (4) repos may be very heavy — never block the UI on engine responses (e.g. branch list); data must load dynamically and live-refresh; (5) visuals must scale with large projects; (6) navrail is too wide; (7) overall: on large projects the app is unusable/too laggy.
Code findings behind the feedback: fetch/pull/push are synchronous blocking POSTs (GitHost.Operations.cs, RunTimed up to 300 s) with only a boolean `busy` flag; App.tsx refreshRepo awaits Promise.all(revisions+refs+status) before anything renders; layoutGraph re-runs fully over all rows synchronously on every revisions change (main thread); Inter/Fira Code load from Google Fonts CDN via a media="print" onload trick that yields fallback system fonts when offline/blocked (packaged AppImage) and theme greys (#737373 secondary text) are light for Linux rasterizers; navrail is fixed 64 px.
**Goal:** On heavy repositories PowerGit stays fluid: no UI blocking on engine calls, every long git operation shows progress in the top bar, all branches are visible by default (GE parity), and typography/contrast/navrail feel desktop-quality on Windows and Linux.
- [x] Engine job model for network ops: POST /fetch, /pull, /push return a jobId immediately and run detached (300 s cap kept); GET /jobs/{id} reports running/done/error + captured output (poll or SSE); existing sync behaviour covered by tests; + engine tests
- [x] Topbar progress indicator: centered LinearProgress + operation label (Fetching…/Pulling…/Pushing…) in the AppBar driven by job state; buttons stay enabled for unrelated actions; errors surface as today
- [x] Non-blocking data loading: render the shell immediately at boot; revisions / refs / status / stashes load independently (kill the Promise.all in refreshRepo) with per-panel skeletons; refreshes use stale-while-revalidate (keep showing old data until new arrives); no dialog or grid ever waits on the engine to become interactive
- [x] All branches visible by default (GE parity): engine /revisions includes every local AND remote-tracking branch tip + tags (+stash), passing explicit tips to git log so the -n cap can never hide a branch tip; left tree shows all branches expanded as today; owner override of the v0.4.0 stale-remotes decision
- [x] Graph layout off the main thread: move/extend layoutGraph to a Web Worker (or incremental append-only layout) so 10k+ commits never block interaction; avoid full re-layout on selection/hover; keep lane output byte-identical (unit tests + existing layout.test.ts must pass unchanged)
- [x] Large-scale render audit + perf budget: extend the 10k unit perf test into a real budget; e2e spec on a large synthetic history asserting scroll + row-select stay responsive (no multi-frame stalls); tune virtualizer overscan/memoization as needed
- [x] Self-hosted fonts + contrast: bundle Inter and Fira Code woff2 in frontend assets, drop the Google Fonts CDN link (offline AppImage currently falls back to poor system rasterization); darken text.secondary/divider greys in theme.ts so text stops looking washed-out on Linux; verify in packaged build
- [x] Navrail slimming: reduce fixed width 64 px → 48 px (icon buttons compact), keep tooltips and testids stable
- [x] Verification: dotnet test engine sln + npm run test:unit + npm run test:e2e green; smoke npm run dev once; manual large-repo check (owner-provided heavy repo if available)

## v0.9 — Linux AppImage hardening

### v0.9.0 — Linux AppImage runtime hardening (2026-08-24) (COMPLETE)
> Owner report from the GitHub-release AppImage on Ubuntu: "libgvfscommon.so: undefined symbol: g_task_set_static_name" (needs GLib ≥ 2.76), failed loads of libgvfsdbus.so / libdconfsettings.so / libgioremote-volume-monitor.so ("g_assertion_message_cmpint" undefined — same GLib family), "libcurl-gnutls.so: undefined symbol nghttp2_option_set_no_rc_9113_leading_and_trailing_ws_validation" (nghttp2 ≥ ~1.50), and an atk-bridge unknown-signature warning.
Root cause analysis: linuxdeploy bundles GIO modules (gvfs, dconf) and libcurl-gnutls that were built against newer GLib/nghttp2 than the versions they bind to at runtime (the host's system stack pulled in by webkit2gtk). Non-fatal but degrades native file dialogs (no gvfs), GTK settings persistence (no dconf), and pollutes stderr on first run.
**Goal:** The released AppImage launches on stock Ubuntu with zero GLib/GIO/curl module errors: bundled libraries must never conflict with the host system stack that webkit2gtk already guarantees.
- [x] Reproduce + inspect bundle: build the AppImage, list bundled gio/modules (gvfs, dconf), libcurl-gnutls and their GLib/nghttp2 versions; confirm the symbol-mismatch diagnosis against the owner's stderr report
- [x] Fix bundling: exclude the host-provided stack from the AppImage (GIO/gvfs/dconf modules, libcurl-gnutls + its nghttp2 chain — webkit2gtk deps already guarantee them on target distros); wire exclusion flags into the linux release job (release.yml / sidecar scripts) and document them
- [x] Regression guard: extend scripts/ubuntu-check.ps1 (Docker) and/or the release CI job to launch the AppImage headless and fail on any "undefined symbol" or "Failed to load module" stderr line; silence residual atk-bridge noise (NO_AT_BRIDGE=1) if still present

## v0.10 — Large-repo scalability for real

### v0.10.0 — Large-repo scalability for real (2026-08-24) (COMPLETE)
> Re-delivers what v0.8.0/v0.9.0 checked off but did not achieve. Root findings: /revisions passes every ref as argv (breaks >~900 refs on Windows, slow topo walk re-run on every refresh); history hard-capped at 800 with no way to load more; clicking a branch whose tip is not loaded silently does nothing; RepoTree renders thousands of un-virtualized rows expanded; refreshRepo re-fetches everything after every action; sync ops (checkout/reset/rebase) give zero progress feedback; "Fetch all remotes" races the single-flight job guard; no live refresh at all; AppImage guard is a static 3-symbol scan, never launches the app. Owner emphasis: repos may have SO MANY branches they cannot all be shown — there must always be a way to find and see them (search/filter + jump-loads-history).
**Goal:** On a repo with 100k commits and thousands of branches, PowerGit stays fluid AND every branch is findable: /revisions is argv-safe and paged, history loads incrementally with append-only layout, the left tree is virtualized with a ref filter, jumping to any ref loads history until that ref is visible, all mutating ops show progress, repo data live-refreshes on .git changes, and the AppImage guard actually launches the app.
- [x] v0.10.0-1: Engine /revisions argv-safe + paged — replace explicit-tips argv with --branches --remotes --tags (+ refs/stash, + HEAD) so ref count can never exceed the Windows 32K command-line limit; add skip/max paging params; engine tests for paging and for a repo with 2000+ refs (generated in-test) [agent: claude]
- [x] v0.10.0-2: Incremental history loading — UI loads revisions in pages (first page fast, background auto-fill up to a ceiling), layout worker keeps state and appends rows without re-laying-out the prefix (full-run and append runs must produce identical rows; unit test proves it); grid shows a subtle loading tail indicator [agent: claude]
- [x] v0.10.0-3: Every branch findable (owner requirement) — left tree gets a filter/search box over ALL refs (branches/remotes/tags, uncapped for-each-ref data); clicking any ref whose tip is not yet in the loaded graph loads further pages until the tip is visible (with progress + graceful ceiling message) instead of silently doing nothing [agent: claude]
- [x] v0.10.0-4: RepoTree virtualization — flatten visible nodes and render via the existing virtualizer so thousands of refs cost only visible rows; remotes/tags sections auto-collapse above a threshold; keep tree-row testids and context menus working [agent: claude]
- [x] v0.10.0-5: Targeted refresh + stable selection — selection keyed by SHA (survives refresh), per-action refresh scope (status-only after stage, status+revisions+refs after commit/checkout, etc.) instead of the full 4-call sweep, debounce BottomPanel per-selection fetches [agent: claude]
- [x] v0.10.0-6: Progress feedback for all mutating ops — checkout/reset/rebase/stash-apply show the topbar busy indicator while running; fix "Fetch all remotes" to run sequentially against the single-flight job guard (currently guaranteed to error with >1 remote) [agent: claude]
- [x] v0.10.0-7: Live refresh (dropped v0.8.0 owner requirement) — engine watches .git metadata (HEAD, refs/, packed-refs, index) and exposes a change feed (SSE); UI subscribes, debounces, and triggers targeted refreshes so external git activity appears without user action; engine test for the watcher [agent: claude]
- [x] v0.10.0-8: Heavy-repo fixture + perf harness — script generates a synthetic repo (50k+ commits, 2000+ branches/tags) via git fast-import; opt-in npm run test:perf drives the real app against it asserting first-paint, scroll, ref-jump and filter latency budgets; documented in AGENTS.md [agent: claude]
- [x] v0.10.0-9: AppImage guard that actually guards — generalize inspect-appimage.sh symbol scan (ldd -r style unresolved-symbol check, not 3 hard-coded names), add a release.yml step that launches the AppImage headless (xvfb) and fails on "undefined symbol"/"Failed to load module" stderr; verification lands with the next tagged release [agent: claude]
- [x] v0.10.0-10: Verification — dotnet test engine sln, npm run test:unit, npm run test:e2e all green; smoke npm run dev once; test:perf run recorded against the heavy fixture [agent: claude]
> Note on v0.8.0/v0.9.0: several checked tasks delivered weaker artifacts than their text claims — the "large synthetic history" perf e2e runs against the small dev repo, and the v0.9.0 "launch headless" regression guard is a static 3-symbol scan that never launches the app. v0.10.0 supersedes those items; their checkboxes stand for what actually shipped, not the original task text.

## v0.11 — Git Extensions hotkeys
> Reintegrate Git Extensions keyboard shortcuts: catalog + dispatcher in React, GE default chords for actions PowerGit already has, commit-overlay S/U on selected files, grid arrow navigation (SRS-GRAPH-011). Unavailable GE commands stay unbound. Remapping UI last.

### v0.11.0 — Git Extensions hotkeys (2026-08-30) (COMPLETE)
> Slice 0 docs (SRS-hotkeys, agent memory) then Slice 1: TS catalog+dispatcher, Browse bindings, grid navigation (SRS-GRAPH-011), commit-overlay S/U. Later slices (commit pane-focus, grid parent/child, remapping UI) stay open.
**Goal:** On the Browse surface, Git Extensions default shortcuts fire the actions PowerGit already has. Grid arrow keys move selection. In the commit overlay, S/U stage/unstage the selected file(s) without stealing keystrokes from the message field.
- [x] v0.11.0-0: SRS-hotkeys.md (tag KEY) + register in docs/srs/README.md; agent memory docs/agents/memories/hotkeys.md [agent: grok]
- [x] v0.11.0-1: frontend/src/hotkeys/ catalog + parse + typing-guard + dispatcher + HotkeyHost; unit tests for GE default chords and S-vs-message-field [agent: grok]
- [x] v0.11.0-2: Browse bindings (commit/open/settings/fetch/pull/push/stash/create/checkout/rebase/focus/F5) + grid arrow/page/home/end (SRS-GRAPH-011) + shortcut captions on toolbar/menus [agent: grok]
- [x] v0.11.0-3: Commit overlay S/U on selected file(s) (multi-select); CompactFileList focusable; S types in the message field [agent: grok]
- [x] v0.11.0-4: Playwright e2e (arrows, Ctrl+Space, Ctrl+Comma, F5 no SPA reload, commit S/U + typing guard) + smoke npm run dev [agent: grok]
- [x] Later: Slice 2 commit pane-focus/stage-all, Slice 3 grid parent/child/go-to, Slice 4 remapping UI [agent: grok]
- [x] v0.11.0-5: Commit overlay always shows Stage / Stage all / Unstage / Unstage all (disabled when empty), matching FormCommit toolbarStaged — not selection-gated vanishing buttons [agent: grok]
- [x] v0.11.0-6: Commit overlay paper size is fixed (independent of selected file / diff length); lists and diff scroll inside. E2e: buttons visible + overlay box does not jump on file select [agent: grok]

## v0.12 — Owner feedback rounds — Linux AppImage

### v0.12.0 — Owner feedback #4 — Linux AppImage review (2026-09-02) (COMPLETE)
**Goal:** Fix the defects the owner found running the v0.11.0 AppImage on Ubuntu: engine port collision on launch, graph missing other people's branches, unclear toolbar dropdown arrows, selected row indistinguishable from same-author rows, untracked file shows no diff, diff view renders ligatures instead of raw characters, red "string did not match" on fetch.
- [x] Engine port collision: AppImage crashes with "Failed to bind 127.0.0.1:7733 address already in use". Tauri must reuse an already-healthy engine or pick a free port, and kill the sidecar child on app exit.
- [x] Graph shows only the current branch: GE parity — all branches (local + remote) recently updated, ordered by date (--date-order), not one branch's topo chain filling the first page.
- [x] Top toolbar: icons closer to Git Extensions; dropdown arrows visually attached to their button (split-button grouping), not floating between neighbours.
- [x] Selected row highlight (Linux) is identical to the same-author highlight; selected row must be visibly distinct.
- [x] Untracked new file in commit view shows "no diff"; must show the full file as an added diff.
- [x] Diff view renders font ligatures (C++ "->" became an arrow); disable ligatures everywhere code is shown, raw characters only.
- [x] Fetch on Linux shows red "The string did not match the expected pattern" (WebKit DOMException); find the offending call and fix.
- [x] Tooling: powerplan submodule bumped to v0.7.2 (powerspawn already current).

### v0.12.1 — Owner feedback #5 — toolbar density + Linux author highlight (2026-09-02) (COMPLETE)
**Goal:** Toolbar buttons are too big: make the command bar compact (GE density). On Linux/WebKitGTK the same-author highlight makes rows disappear; fix with WebKit-safe CSS.
- [x] Toolbar too big: compact command bar (small buttons, tighter padding, 28-30px height, smaller icons/labels) at GE density.
- [x] Linux: same-author highlight makes rows disappear on WebKitGTK; make row highlight CSS/canvas WebKit-safe and keep text visible.
- [x] Bottom diff view: file list panel resizable (drag divider between file names and diff; width persisted).
- [x] Ubuntu fonts look low quality and too light grey: verify self-hosted Inter/Fira Code actually load under tauri:// on WebKitGTK, darken secondary text, add font smoothing and a good Linux fallback stack.
- [x] File Tree on Linux: cannot expand subdirectories or open files (owner report #2). Tree/blob logic and e2e are correct on Windows; subdirectory errors were silently hidden, now surfaced in the tree. Linux root cause still needs a repro.
- [x] Linux review pass: text-first JSON parsing everywhere, engine reuse only on matching version, ls-tree timeout, AppImage bundled-GLib strip in inspect script, Docker WebKit e2e harness (25/25 webkit + chromium on Linux).

### v0.12.2 — Owner-issue audit + Linux UI/UX pass (2026-09-02) (2026-09-03) (COMPLETE)
**Goal:** Revisit every owner-reported defect from earlier iterations and confirm each is actually fixed in the current code (not just ticked); fix the ones that are partial or regressed. Second pass on Linux UI/UX polish toward Git Extensions parity. Verify on Windows and in the Docker WebKit harness, then release.
- [x] Layout Web Worker: onerror handler + in-thread layouter fallback so a custom-scheme worker failure on WebKitGTK cannot leave the grid empty.
- [x] Diff view: no mid-token wrapping (white-space pre, horizontal scroll, tab-size 4, line-number gutter).
- [x] All 12 remaining catch sites use describeThrown so WebKit DOMException text reaches the UI (BottomPanel, CommitDialog, GitOps).
- [x] Error banner becomes a dismissable Alert with copy; status bar shows branch / ahead-behind / dirty like GE instead of engine health.
- [x] Scoped refresh: watcher events carry a kind so a status-only change does not refetch revisions/refs; selection and scroll preserved across refresh.
- [x] Grid auto-scroll only on user navigation (keyed by SHA), not on every refresh; focus returns to the grid after dialogs and actions.
- [x] Settings dialog label clipping (v0.4.7) verified with an e2e geometry assertion; splitters handle pointercancel.
- [x] Cherry-pick and revert implemented (engine + context menu), replacing the disabled placeholders from v0.7.0.
- [x] Empty/failed states: when /revisions fails the grid stays blank; show an inline error with Retry, and real empty states for no-commits / no-files / no-stashes.

### v0.12.3 — Toolbar overflow, selection vs author, chrome bugs, VS Code fonts, syntax highlighting (2026-09-03) (COMPLETE)
**Goal:** Close the owner's 2026-09-03 Ubuntu round: stop the author highlight competing with (and erasing) the selection, make the command bar survive narrow windows, get the busy indicator out from under the buttons, kill the stray WebView context menu, restore Fetch All, adopt VS Code's font stack, and highlight source in the File Tree viewer.
- [x] Same-author marker moves off the row background onto the author name (CSS + canvas band removed), so selection owns the row background exclusively and nothing can be erased by a WebKitGTK class-mutation repaint
- [x] Command bar collapses progressively (labels -> icons -> "More" overflow menu) from its own measured width; nothing wraps or clips at any window size
- [x] Busy indicator moved out of its absolute centre overlay into the toolbar flow beside the status strip (it was painting over the buttons by construction)
- [x] WebView context menu suppressed app-wide outside text fields; the revision menu re-targets on a second right-click instead of leaking the browser menu through the modal backdrop
- [x] "Fetch all remotes" is always present in the Fetch menu (it only appeared with two or more remotes, so single-remote clones had no Fetch All at all)
- [x] UI font stack switched to VS Code's order (platform font first, Inter as fallback); `-webkit-font-smoothing: antialiased` removed — it thins text and was the cause of the "light grey" complaint it had been added to fix
- [x] Syntax highlighting in the File Tree blob viewer via Shiki (VS Code's own highlighter), bundled offline, lazy, with plain-text fallback and a size guard
- [x] Repo state (branch, ahead/behind, dirty, build info) moved from the toolbar's leftover width into a real bottom status bar; it was being elided to unreadable stubs at every window size
- [x] Narrow-window layout: ref panel auto-collapses below the overflow width (restored when the window grows, unless the user closed it), grid metadata columns shrink under 1200px so Date/SHA stop being pushed off the right edge, and adjacent columns get a gap
- [x] Engine exits with its parent (--parent-pid watchdog): a force-killed or crashed UI no longer orphans a sidecar holding the port, which was the root of the original "address already in use" launch crash
- [x] Test integrity: four specs were asserting something other than what they claimed (repo-restore target, repo-specific .gitignore fixture, a nested-locator strict-mode match that only passed while the diff was still loading, exact containment of a label that straddles its border by design) — all corrected; Linux harness no longer clobbers the host node_modules and seeds a git identity

## v0.13 — Hardening — security, CI, structure, truth
> Project audit of 2026-09-03 (Good / Bad / Ugly). One iteration per finding. Ugly items first (engine exposure, docs vs repo reality), then structural debt (App.tsx, version sync, engine concurrency, CI, Rust tests).

### v0.13.0 — Engine auth token + CORS lockdown (Ugly #1) (2026-09-03) (COMPLETE)
**Goal:** The localhost engine is an unauthenticated remote control for git: AllowAnyOrigin/AnyHeader/AnyMethod CORS plus POST /reset, /rebase, /push, /branches/delete, /files/delete, /stash/drop, /tools/vscode. Any web page in any browser can drive it while the app runs. Gate every route behind a per-launch secret and lock CORS to the app origin.
- [x] Tauri shell generates a random 32-byte token per launch, passes it to the sidecar via env var (POWERGIT_ENGINE_TOKEN); engine_base_url command returns { url, token }; engine.ts sends it as Authorization: Bearer on every request [agent: claude]
- [x] Engine middleware rejects any request without the correct token with 401 (constant-time compare); GET /health stays unauthenticated for the port probe [agent: claude]
- [x] CORS narrowed to tauri://localhost, http://tauri.localhost and the Vite dev origin (no AllowAnyOrigin) [agent: claude]
- [x] Drop engine reuse in lib.rs: always spawn our own sidecar on a free port (parent-pid watchdog already prevents orphans); remove the version-match reuse path [agent: claude]
- [x] Dev/test paths: standalone engine (dev:all, dotnet run, engine.ps1, Docker harness) reads the token from env or prints a generated one; playwright config and scripts pass it [agent: claude]
- [x] Verification: engine tests for 401 without token / 200 with; e2e from the Vite origin with a wrong token expects rejection; dotnet test + test:unit + test:e2e green [agent: claude]

### v0.13.1 — Docs vs repo reality — orphan branch, missing upstream tree (Ugly #2) (2026-09-03) (COMPLETE)
**Goal:** AGENTS.md claims the WinForms tree stays in-tree as the behavioural spec and forbids deleting it, but the powergit branch is an orphan with 226 tracked files; src/app, externals and tests/ exist on disk only because .gitignore excludes them. master (17305 commits) and powergit (54) share no ancestor, so PRs against master cannot work. AGENTS.md still lists frontend/, src-tauri/ and src/engine/ as "to be created". Decide the branch model and make the docs tell the truth.
- [x] Connect histories: on powergit, git merge -s ours --allow-unrelated-histories master (tree unchanged; master becomes an ancestor so diffs/merge-base/PRs work) [agent: claude]
- [x] Make powergit the GitHub default branch; keep master by name as the untouched upstream mirror [agent: claude]
- [x] Reference worktree: document one command (git worktree add ../gitextensions-ref master); AGENTS.md points there for GitCommands/GitUI as the behavioural spec [agent: claude]
- [x] Remove the /src/app/, /externals/, /tests/ ignore rules (the tests/ one hides any future root tests dir) and drop the untracked upstream tree from the working copy once the worktree exists [agent: claude]
- [x] Rewrite AGENTS.md: current project-shape block (frontend, frontend/src-tauri, src/engine, website, docker, scripts, docs), delete the three 'to be created' markers, the 'do not delete WinForms' rule and the contradictory footnote, fix the two shell-mangled strings ('ode frontend/scripts', 'elease skill'), add a Branches section [agent: claude]
- [x] README dev section: one line on the reference worktree; replace upstream's verbatim CONTRIBUTING.md with a short PowerGit version [agent: claude]

### v0.13.2 — PLAN.md header frozen at v0.4.7 (Ugly #3) (2026-09-03) (COMPLETE)
**Goal:** The "Current Status" header says Active v0.4.7 (2026-08-21) and "do not start v0.5 until owner re-reviews", while twelve later versions are marked COMPLETE below it. Make the header derive from, or be updated with, the real current iteration so a reader trusting the top of the file is not misled.
- [x] Header stops claiming a version: keep goal / philosophy / verify / upstream pin; replace the 'Active: v0.4.7' paragraph and the 'do not start v0.5' gate with one line pointing at get_current_iteration / the last non-COMPLETE heading [agent: claude]
- [x] Close v0.12.2 properly: defer its one open hotkeys task (already in Backlog as Slice 2–4) so check_plan is clean and current becomes v0.13.0 [agent: claude]
- [x] One-time manual restructure (sanctioned exception to the powerplan-only rule, say so in the commit): majors in version order v0.0…v0.4, one merged v0.5, a major per v0.6…v0.12 with their iterations moved under them, v0.13 last; task text and checkboxes byte-identical [agent: claude]
- [x] Merge 'Later' into 'Backlog' at the end of the file; move the 2026-08-21 branch-restructure narrative into docs/agents/memories/branch-model.md [agent: claude]
- [x] After the restructure: powerplan check_plan reports ok; show_plan and get_current_iteration both say v0.13.0 [agent: claude]
- [x] Dedicated sub-iteration v0.13.9 upgrades powerplan (normalize, set_header, create_major insertion fix, lint rules) so this restructure never needs a manual edit again [agent: claude]

### v0.13.3 — Git Extensions reuse story is aspirational (Ugly #4) (2026-09-04) (COMPLETE)
**Goal:** README and AGENTS.md promise "the proven Git Extensions engine code" and a reused lane model, but src/engine references no upstream project: it is a fresh 2.3k-line git CLI wrapper, and the graph layouter is a TypeScript reimplementation. Either make the claim true (extract/port specific GE pieces with tests proving parity) or rewrite the claim to "GE is the behavioural reference, verified by parity tests".
- [x] Rewrite the claim in README and AGENTS.md: GE is the behavioural reference; the engine is a small net10.0 git CLI host; the lane layout is a TypeScript reimplementation of GE RevisionGraph verified by golden tests. Drop 'extract from' and 'reuse / expose' [agent: claude]
- [x] Golden parity generator: Windows-only C# console project under tools/ge-parity referencing GitUI from the ../gitextensions-ref worktree; feeds synthetic histories to RevisionGraph and dumps per-row lane/segment/sharing JSON; committed output, runs on demand [agent: claude]
- [x] Parity test in layout.test.ts: for each golden file run createLayouter over the same revisions and assert identical lanes, segment endpoints and lane-sharing flags [agent: claude]
- [x] Unit test pins the seven lane colours in types.ts to GE AppColor.GraphBranch1–7 hex values [agent: claude]
- [x] Record the decision: git-extensions-map memory marks 'expose lanes from C#' as superseded with the reason (GitUI graph is WinForms-bound, engine must stay net10.0, layout runs in a Web Worker); add SRS-GRAPH requirement 'lane layout matches GE on the golden fixtures' verified by Test [agent: claude]

### v0.13.4 — Split App.tsx god component + lint (Bad #1) (2026-09-04) (COMPLETE)
**Goal:** App.tsx is 1572 lines with 36 useState and 8 useEffect; GitOps.tsx is 662 lines with 15 states. No state container, no ESLint/Prettier. Extract feature hooks/stores (selection, panels, repo data, refresh) behind stable interfaces, add lint + format config with a CI gate, and keep e2e green throughout.
- [x] Six hooks, no store library: useEngineSession (offline/health/repo/recents/live), useHistory (revisions, paging, live graph rows, selected SHA), useRepoState (refs/status/stashes + watcher-scoped refresh), useJobs (busy/jobLabel), useDialogs (one discriminated union replaces 11 open flags/targets), useChromeLayout (bottom height, left open, bottom tab) [agent: claude]
- [x] Extract the command bar into components/CommandBar.tsx (toolbar tiers, overflow menu, anchors) taking session/jobs/dialog APIs as props [agent: claude]
- [x] App.tsx becomes composition only (<300 lines): call the hooks, render the shell, wire HotkeyHost [agent: claude]
- [x] Split GitOps.tsx into one file per dialog under components/dialogs/ with a shared useActionDialog hook for the busy/error pattern [agent: claude]
- [x] ESLint flat config (typescript-eslint, react-hooks, react-refresh, max-lines 400) + Prettier with lint and format:check scripts; fix any react-hooks/exhaustive-deps findings rather than disabling them; CI wiring lands in v0.13.7 [agent: claude]
- [x] Guard rails: no testid or behaviour change; one commit per extraction; npm run test:e2e once after each; test:unit green; smoke npm run dev [agent: claude]

### v0.13.5 — Single source of truth for the version (Bad #2) (2026-09-04) (COMPLETE)
**Goal:** The version is hand-copied in package.json, tauri.conf.json, Cargo.toml and a const in Program.cs. A mismatch silently makes the Tauri shell refuse to reuse its own engine. Derive all four from one place at build time and add a check that fails the build when they diverge.
- [x] package.json is the single source of the version; tauri.conf.json 'version' becomes the path '../package.json' (Tauri 2 reads it), so the config holds no copy [agent: claude]
- [x] build.rs reads package.json and exports POWERGIT_VERSION as a compile-time env; lib.rs uses it instead of CARGO_PKG_VERSION; Cargo.toml keeps a placeholder nothing consumes (no more Cargo.lock churn per release) [agent: claude]
- [x] Engine csproj sets Version from package.json via an MSBuild property function (regex on the file); Program.cs replaces the engineVersion const with the assembly informational version, so dotnet run and the packaged sidecar report the same number [agent: claude]
- [x] scripts/check-version.mjs asserts package.json, engine /health, Tauri package info and the packaged artifact names agree; fails non-zero; runs in CI (v0.13.7) and as release preflight [agent: claude]
- [x] Release skill (.claude/skills/release + .opencode mirror, kept identical): section 1 becomes 'npm version X.Y.Z --no-git-tag-version, commit chore(release)'; a new 'Verify the version' step lists WHAT is derived, WHERE it surfaces (status bar, /health, installer/zip names, GitHub release tag, Pages footer) and HOW to check each (check-version.mjs, curl /health, ls dist/, gh release view); drift footnote removed [agent: claude]
- [x] Verification: fresh dotnet run shows the package.json version in /health; npm run tauri build artifact names carry it; check-version.mjs passes; a deliberate mismatch fails it [agent: claude]

### v0.13.6 — Engine concurrency — one global mutable repo (Bad #3) (2026-09-04) (COMPLETE)
**Goal:** GitHost is a singleton with a single Current repository and the only lock lives in the file watcher. Two app windows, a second client, or concurrent e2e workers race on repo state (the memories already record e2e contention). Make repo state per-request or per-session and serialize mutating git operations per repo.
- [x] Repo sessions: GitHost becomes a registry of RepoSession keyed by root (root, watcher, jobs, write gate); POST /repos/open returns a session id; every route takes /repos/{id}/... ; engine.ts builds all URLs with the id [agent: claude]
- [x] Request-scoped root: each endpoint resolves its session once at entry and passes the root down; no git command reads shared state mid-request [agent: claude]
- [x] Per-session write gate (SemaphoreSlim(1)) around every mutating op incl. fetch/pull/push jobs; reads bypass; a colliding mutation returns 409 with the running job id (was 400) [agent: claude]
- [x] Session lifecycle: DELETE /repos/{id} disposes the watcher; recents stay client-driven. NOT done: idle-session pruning (sessions live until closed or the engine exits) — moved to the backlog [agent: claude]
- [x] Engine tests: two repos open with interleaved queries; concurrent stage+commit serialize; 409 on collision [agent: claude]

### v0.13.7 — CI on push/PR + Tauri CSP (Bad #4) (2026-09-04) (COMPLETE)
**Goal:** Only pages.yml and release.yml are tracked; engine tests, vitest and Playwright run only on developer machines. Add a ci.yml on push and pull_request that runs dotnet test, npm run test:unit and npm run test:e2e (Windows + Linux). Replace tauri.conf.json "csp": null with a real policy that still allows the engine origin, Shiki, and the layout worker.
- [x] .github/workflows/ci.yml on push + pull_request: dotnet test (engine), npm run lint + format:check, test:unit, test:e2e on windows-latest and ubuntu-22.04; check-version.mjs; powerplan check_plan [agent: claude]
- [x] tauri.conf.json csp: real policy (default-src 'self'; connect-src to the engine loopback origin; worker-src 'self' blob:; style-src 'self' 'unsafe-inline' for MUI/emotion); verify Shiki, the layout worker and engine fetches still work in npm run tauri dev [agent: claude]
- [x] README CI badge; AGENTS.md 'How we verify' mentions CI as the gate [agent: claude]

### v0.13.8 — Rust shell unit tests (Bad #5) (2026-09-04) (COMPLETE)
**Goal:** frontend/src-tauri/src/lib.rs has zero #[test]s although it contains the hand-rolled HTTP health probe, the chunked-body decoder and the port-resolution decision, which already failed silently once. Add cargo unit tests for dechunk, looks_like_powergit_health (version match/mismatch, non-chunked, garbage) and resolve_engine_port against a local TcpListener stub; run them in CI.
- [x] cargo unit tests in lib.rs: generate_token shape/uniqueness, POWERGIT_VERSION exported by build.rs is X.Y.Z (dechunk and looks_like_powergit_health no longer exist: v0.13.0 removed engine reuse and the hand-rolled HTTP probe) [agent: claude]
- [x] resolve_port_preferring / pick_free_port / port_is_free tests against a local TcpListener (free port is returned as-is; a held port yields a different bindable port) [agent: claude]
- [x] cargo test wired into ci.yml (v0.13.7) [agent: claude]

### v0.13.9 — powerplan upgrade — normalize, set_header, create_major fix, lint (sub-iteration of v0.13.2) (2026-09-04) (COMPLETE)
**Goal:** powerplan has 22 iteration/task-scoped tools and nothing that touches majors, prose, the header block, or file order, which is why PLAN.md drifted and the fix needed a manual edit. Add the missing operations and lint rules in the powerspawn/powerplan submodule, release, and bump the pin here.
- [x] sort_plan / normalize: reorder majors and their iterations by version number; task text, checkboxes and prose blocks byte-identical; dry-run flag that reports the moves without writing [agent: claude]
- [x] set_header: read/replace the block between the H1 and the first major (goal, philosophy, verify, upstream pin, current-iteration pointer) [agent: claude]
- [x] create_major insertion fix: insert before the trailing Later/Backlog sections (or after the highest major by version), never mid-file; regression test with an out-of-order fixture [agent: claude]
- [x] check_plan lint rules: duplicate major headings, majors/iterations out of version order, iteration nested under a major with a different version prefix [agent: claude]
- [x] show_plan and check_plan agree on 'current' (today show_plan says v0.12.2, check_plan says v0.13.0); one definition, one code path [agent: claude]
- [x] Nice-to-have once sorted: move_iteration(version, major), update_major / merge_major, remove_prose / move_prose [agent: claude]
- [x] Release powerplan, bump the powerspawn/powerplan submodule pin in PowerGit, run normalize + check_plan on this PLAN.md as the acceptance test [agent: claude]

### v0.13.10 — Ubuntu 26.04 release blockers — AppImage, job routes, git hangs (PLANNED) (2026-09-04) (COMPLETE)
**Goal:** The published v0.12.3 AppImage fails before first paint on Ubuntu 26.04 because an older bundled libmount/libblkid is loaded beside the host GIO; the current session-aware frontend also sends Fetch/Pull/Push to pre-session URLs, GitHost can deadlock while synchronously draining redirected pipes, and the Linux harness stops on fixture branch drift. Remove these release blockers and make the real packaged AppImage—not a browser substitute—the Linux acceptance gate.
- [x] AppImage dependency hygiene: reproduce the v0.12.3 Ubuntu 26.04 MOUNT_2_40 failure from the published artifact; define the host-vs-bundle policy for GLib/GIO, libmount/libblkid, Wayland and GStreamer; extend scripts/inspect-appimage.sh to fail or safely strip every prohibited family, repack, then prove the repaired artifact starts against host WebKitGTK. [agent: claude]
- [x] Release compatibility matrix: build the AppImage once and launch that exact artifact in stock Ubuntu 22.04, 24.04 and 26.04 containers; require first paint/engine health, keep the process alive through the smoke interval, record stderr and fail on WebKit/GIO/GStreamer/ABI errors or an early child exit. [agent: claude]
- [x] Session-aware network jobs: make startFetch/startPull/startPush use /repos/{id}; return session-qualified Location headers from the engine; add contract tests that hit the real engine and assert POST plus job polling URLs so broad Playwright route globs cannot hide another regression. [agent: claude]
- [x] Deadlock-proof git execution: drain stdout and stderr concurrently, start timeout/cancellation before awaiting either stream, kill the whole process tree on timeout and always finish draining/dispose; add cross-platform tests with a helper that floods stderr and with a child that never exits. [agent: claude]
- [x] Repair the Ubuntu harness fixture contract: remove the hard-coded powergit branch assumption (or seed the declared branch consistently), run all Linux e2e specs rather than stopping at blob-highlight.spec.ts, and retain retries:0/maxFailures:1 as the gate. [agent: claude]
- [x] Acceptance proof: dotnet test, lint, format check, test:unit, one test:e2e run, cargo clippy/test, scripts/ubuntu-check.ps1, and the packaged-AppImage matrix are green; document the supported Ubuntu versions and retain the failing v0.12.3 stderr as a regression fixture. [agent: grok]

### v0.13.11 — Long-session UI resilience — bounded work, memory, recovery (PLANNED) (2026-09-04) (COMPLETE)
**Goal:** After launch, rapid history navigation and large repositories can leave overlapping git reads running, buffer unrestricted blob/diff data, clone large history graphs, and accumulate repository watchers; an engine or WebKit failure then has little durable evidence or recovery. Bound every expensive path, make latest selection win, supervise native processes, and prove stable interactive use over a representative Ubuntu session.
- [x] End-to-end latest-request-wins cancellation: let every engineFetch helper accept AbortSignal/timeouts; abort superseded commit/files/diff/blob/history requests; propagate HttpContext.RequestAborted into GitHost and terminate the corresponding git process instead of merely suppressing stale React state. [agent: claude]
- [x] Bound content before allocation: cap git stdout while it is read, probe blobs with git cat-file -s before loading, reject or truncate oversized text with explicit byte/line limits, preserve binary detection, and add truncation metadata to the DTO instead of appending an ambiguous text sentinel. [agent: claude]
- [x] Large-content UX: virtualize diff rows, keep gutter/keyboard/selection behavior accessible, and show an intentional 'too large to preview' state with size, truncation reason, copy/open-in-difftool actions and retry options; never hand an unrestricted blob or million-line element tree to WebKitGTK. [agent: claude]
- [x] History/worker memory: catch synchronous module-Worker construction failures and enter the existing main-thread fallback; remove avoidable full-history structured clones/state duplication on refresh, keep paging incremental, and add measurable JS-heap/RSS budgets for 10k and 100k histories. [agent: claude]
- [x] Session and watcher lifecycle: touch sessions on requests, evict idle sessions with active jobs protected, close the previously active session when the single-window UI no longer needs it, dispose watchers deterministically, narrow Linux watch scope away from object-pack churn, and test repeated open/close plus inotify pressure. [agent: claude]
- [x] Failure visibility and recovery: persist timestamped Tauri/engine stderr plus exit status, surface distinct connecting/offline/engine-exited/job-failed states through an accessible recovery panel, add a React error boundary and unhandled-rejection reporting, and supervise the sidecar with one bounded restart/backoff rather than silently ending its monitor. [agent: claude]
- [x] Ubuntu longevity gate: run the packaged app for at least 10 minutes on Ubuntu 26.04 under X11 and headless Wayland while rapidly changing revisions, opening capped large blobs/diffs, switching repositories and completing a network job; assert UI responsiveness, bounded RSS/child count/watch count, no WebKitWebProcess/GPUProcess/engine exit, and useful retained logs on injected failures. [agent: grok]

### v0.13.12 — Frontend trust — explicit state, repository-scoped API, operation UX (PLANNED) (2026-09-04) (COMPLETE)
**Goal:** Once long-session stability is established, make the interface unambiguous about what data it is showing and what Git is doing. Replace loosely related connection booleans and the global repository id with explicit state and repository-scoped clients; distinguish demo, startup, no-repository, recovering and failed-engine modes; give network operations inspectable destinations, progress, cancellation and results without reducing the revision graph's desktop-grade information density.
- [x] Model session UI as one discriminated state machine (starting, demo, no-repository, ready, busy, recovering, engine-failed) with legal transitions and derived view props; remove contradictory combinations of live/offline/health/repo/engineError booleans and add reducer transition tests. [agent: claude]
- [x] Separate demo from failure: synthetic history is enabled only by an explicit demo/build mode, never as an automatic packaged-app fallback; startup, unavailable engine, lost engine and unknown repository each get distinct copy, primary recovery action and access to diagnostics without replacing real data with samples. [agent: claude]
- [x] Repository-scoped engine client: replace the module-global REPO_ID with a client/context bound to {baseUrl, token, repoId}; make repository identity explicit at call sites, support ?repo=<id> or equivalent per-window pinning, and prove two windows/clients cannot silently switch each other's repository. [agent: claude]
- [x] Unify asynchronous surfaces: introduce reusable loading, empty, error and retry patterns with consistent placement/copy; preserve the last valid graph during refresh, distinguish initial load from background refresh, and eliminate scattered bare Loading… strings and duplicate error banners. [agent: claude]
- [x] Network-operation decisions: keep normal Fetch one-click while showing the selected remote; add Pull and Push previews that state source/destination branch, upstream, ahead/behind and rebase/refspec choices; require a deliberate force-with-lease confirmation and translate credential/SSH failures into actionable guidance. [agent: claude]
- [x] Inspectable job UX: make status-bar progress open a non-modal operation detail with elapsed state, cancellability, sanitized command context, live/final output, copy and retry; retain completed/failed results for the session and restore grid focus after actions instead of relying on an anonymous progress line. [agent: claude]
- [x] Accessibility and behavior proof: announce connection/job state through appropriate live regions, provide labels/tooltips and predictable focus for icon/menu/dialog actions, keep every core flow keyboard-complete, and add real-engine e2e coverage for startup, no repo, repo ready, background refresh, cancellation, failure, recovery, pull/push preview and multi-window repo isolation. [agent: claude]

### v0.13.13 — Visual system — Linux-native themes, zoom, consistent density (PLANNED) (2026-09-04) (COMPLETE)
**Goal:** With reliability and interaction states settled, consolidate PowerGit's visual language without redesigning the dense Git Extensions-style workspace. Move scattered colors, spacing and typography into semantic tokens; support system/light/dark appearance and persisted zoom; preserve graph readability, information density and predictable pane behavior across Windows and current Ubuntu displays.
- [x] Semantic token audit: inventory hard-coded colors, borders, spacing, row states and typography across MUI sx, app.css, diff/blob views and canvas graph drawing; define shared surface/text/selection/ref/diff/graph/status tokens and migrate without changing established layout or graph semantics. [agent: grok]
- [x] Appearance modes: add System, Light and Dark themes with persisted preference, react to OS changes, map GTK/Windows system colors where appropriate, and provide independently checked contrast palettes for graph lanes, ref badges, diffs, selection, focus and disabled text. [agent: grok]
- [x] Application zoom: implement persisted Ctrl+=, Ctrl+-, Ctrl+0 zoom over the complete workspace; scale row/canvas metrics and hit targets together, clamp to usable bounds, expose the current percentage, and keep splitter positions/scroll selection stable across zoom changes. [agent: grok]
- [x] Cross-platform typography and density: retain the platform-first Ubuntu/Windows font stacks, normalize control heights/baselines and code metrics through tokens, remove component-local exceptions where possible, and verify dense history rows remain legible without clipping at common DPI scaling levels. [agent: grok]
- [x] Interaction polish: give every icon-only action an accessible label and tooltip, make hover/focus/pressed/disabled states consistent, enforce practical pointer targets without bloating the desktop layout, and preserve visible keyboard focus through toolbar overflow, menus, dialogs, grid and splitters. [agent: grok]
- [x] Responsive and visual acceptance: keep test:resolution green at the five supported viewports through 4K, add assertions for narrow toolbar overflow, pane minimums, zoom and both themes, then run the owner-approved test:visual/design walkthrough on Windows and Ubuntu; update only canonical website/public/assets screenshots after acceptance. [agent: grok]

### v0.13.14 — Owner report — selected commit vanishes from the graph, laggy selection, symptom-first testing</title> (2026-09-05) (COMPLETE)
<parameter name="goal">Owner report (2026-09-05): "commits disappear when they are selected" — reported since v0.12.1 and never seen by any audit because every test asserted DOM classes and computed styles while the commit node lives on the canvas underneath the opaque selected-row background. Also: clicking commits feels laggy because highlighting waits on the commit-details load. Fix both, add pixel-level symptom tests, and change how owner reports are tested and verified (symptom test first, owner closes, visual walkthrough on demand rather than full suite on CI).

- [x] Selected commit vanishes from the graph: the opaque .grid-row.selected background (added in v0.12.1, z-index above the canvas) covered the canvas node and lane lines of the selected row on every platform. Tint moved to the text cells; the canvas keeps the graph-column band; tests/e2e/selected-row-graph.spec.ts samples the composited pixels (light/dark, 100%/150%, selected + hovered; red before, green after). Also found at 150%: the fixed 280px bottom panel swallowed the grid on a small zoomed viewport, now clamped. FIXED b025a5124. Ticked on the owner's release instruction (2026-09-05). [agent: claude]
- [x] Immediate selection: a click must highlight in its own frame; commit details load afterwards. Cause: every click re-rendered the whole tree (RepoTree, CommandBar, BottomPanel). RepoTree/CommandBar memoised with stable callbacks (useStable), BottomPanel gets a deferred `current`. In-page: production 25 ms to class / <50 ms to paint, no long tasks (was ~70 ms); dev build 125-155 ms (was 200-1460 ms with 300 ms long tasks). perf.spec asserts the budget in-page. FIXED b025a5124. Ticked on the owner's release instruction (2026-09-05). [agent: claude]
- [x] Symptom-first rule: every owner-reported defect gets a failing test that reproduces the reported sentence (quoted verbatim in the test) on the reported platform before any fix; tests assert what the owner sees (composited pixels for canvas+DOM layers), not the mechanism of the fix. Write it into AGENTS.md and the release skill; retire the 'default proof is e2e assertions, not screenshots' guidance that produced this blind spot. [agent: claude]
- [x] Owner verification: tasks derived from an owner report are not tickable by agents; they are marked 'fixed, awaiting owner verification' and the owner closes them. Record in AGENTS.md and apply to the two report tasks of this iteration. [agent: claude]
- [x] Visual walkthrough on demand, not on CI: make the visual suite runnable per area (grid, bottom panel, dialogs, themes) with per-platform baselines, document 'run the subset for the area you touched' after any CSS/token/graph change, add a look-at-it step (scripts/capture-window.ps1 for the packaged/dev window plus a fixed state checklist) to audits and release preflight, and have a reviewer with vision inspect the captures. [agent: claude]
- [x] Diff loading latency (owner: "can we make the diff loading faster?"): tests/e2e/diff-latency.spec.ts measures in-page click → highlight → file list → diff on screen. Baseline median ~1100 ms (2.5-3.2 s after a large diff). Fixes: leading-edge debounce, engine GET /commits/{id}/changes (files + first diff, byte-identical to /files + /diff, tested), per-commit LRU cache with prefetch on selection commit, stale-file guard, DiffView plain rows and virtualization above 200 lines, previous commit kept on screen while the next loads (no white flash; 404 fallback for older sidecars). Result: dev 310-580 ms every sampled row, production 180-260 ms. FIXED eb963e512 + e9f963a34. Ticked on the owner's release instruction (2026-09-05). [agent: claude]
- [x] Owner report (2026-09-05): "In the commit view, the right click menu on the unstaged files is not professional. Very poor." CommitFileContextMenu in the RevisionContextMenu house style with GE's item set and order (Stage/Unstage S/U · Reset file(s) to HEAD… · Delete file(s)… · Open with difftool · Copy path(s) · Add to .gitignore…), icon on every row, shortcut column, separators, plural labels, disabled-not-hidden, right-click selects the row, in-app ConfirmDialog; engine POST /files/reset and /difftool/worktree. tests/e2e/commit-file-menu.spec.ts (isolated dirty repo). FIXED 6bb58ddb9. Ticked on the owner's release instruction (2026-09-05). [agent: claude]
- [x] Owner report (2026-09-05): "In the commit view, we can select a piece of diff and reset it like we can in baseline Git Extensions." DiffView line selection (click / Shift+click / Ctrl+click over hunk rows), CommitDiffContextMenu with Stage selected lines / Unstage selected lines / Reset selected lines… (in-app confirm; disabled with a reason for binary, new, deleted files), src/patch/partial.ts synthesizes GE-style partial patches (old base for --cached, new base for the reverse apply; unit-tested), engine POST /patch runs git apply (SessionTests: stage, reverse, bad hunk → 400). tests/e2e/commit-partial-diff.spec.ts resets two of three changed lines and stages the third in an isolated repo, asserted on the working tree and index. FIXED d76af0c85. Ticked on the owner's release instruction (2026-09-05). [agent: claude]
- [x] Owner request (2026-09-05): frameless window with the command bar as title bar and integrated Minimize / Maximize-Restore / Close (Windows + Linux). Decorations off, data-tauri-drag-region on the bar (drag, double-click maximize), WindowControls outside the overflow-hidden toolbar (never clipped), Windows 11 glyphs, Restore glyph follows state, capabilities granted, hidden outside the Tauri shell. Verified on the real window over WebView2 remote debugging at 200 % DPI. Known gap: Win11 Snap Layouts on hover (needs the decorum plugin). Linux uses the same Tauri path; confirmed with the release AppImage matrix. FIXED 483a965f3. Ticked on the owner's release instruction (2026-09-05). [agent: claude]
- [x] Brand: owner picked direction D (the crossing redrawn); src/assets/logo.svg is the single source, `npm run icons` regenerates every platform set, all PNG sizes in bundle.icon for the AppImage/hicolor set, BrandMark in the app bar with theme tokens, site favicon + header. Linux AppImage icon verified by the release matrix run. DONE 90a7c0ea0. Ticked on the owner's release instruction (2026-09-05). [agent: claude]

### v0.13.15 — Visual overhaul — flat instrument-panel chrome from the mark's two blues</title> (2026-09-05) (COMPLETE)
<parameter name="goal">Owner (2026-09-05): "Can we use the frontend-design skill to overhaul our frontend?" Give PowerGit a visual identity of its own instead of the rounded-card kit on a grey floor: three flat regions meeting at hairlines, the mark's two blues as the only accent on cool slate neutrals, quiet text buttons with Commit as the single filled action, sentence-case captions, native platform type with a disciplined scale. The graph stays the only coloured thing and every Git Extensions parity colour is untouched. Plan recorded in docs/agents/memories/visual-direction.md.

- [x] Design plan: subject, palette (cool slate neutrals; the mark's two blues as the only accent), native type scale, flat three-region layout, principles; reviewed against the generic-default traps (card kit, uppercase eyebrows, tinted black, single acid accent). Recorded in docs/agents/memories/visual-direction.md. [agent: claude]
- [x] Tokens and theme: light/dark neutrals with the slate bias, primary = brand deep, Paper without borders (regions own their hairlines), dialogs and menus keep a hairline, underline tabs in sentence case. [agent: claude]
- [x] Shell and regions: nav rail, side panel (240) and bottom panel meet at hairlines; no padding gutters, no rounded bordered cards; the graph pane borderless; splitter as a 5px strip that lights up on hover; status bar 22px with the branch in brand blue; grid header sentence case. [agent: claude]
- [x] Command bar: quiet text buttons with icons, Commit as the single filled action, split carets subtle; kept as the optional title-bar placement (Settings → Command bar) and rendered from the shared command list. [agent: claude]
- [x] Verify: visual subset baselines for @grid/@bottom/@dialogs/@themes refreshed and looked at, resolution suite green, e2e green (pixel and hotkey specs), captures of states 1-9 of the walkthrough in light and dark from the real window over CDP, then the owner looks. [agent: claude]
- [x] Owner (2026-09-05): "isolated attempt, to have the menu bars from the top but as a floating bar at the bottom". Switchable in Settings → Command bar (pg.bar): "In the title bar" (default) or "Floating at the bottom" (TitleStrip with mark, name, repository, drag region and window controls at the top; the command bar floats centred over the bottom of the workspace in a rounded, shadowed 1180px bar so every button keeps its label; Commit badge unclipped). Captured in light and dark on the real window; owner picks. 1e80ebfd4. [agent: claude]
- [x] Owner (2026-09-05): "integrate the menu options in the leftside navrail instead, with a collapsable leftside menu" — compared A (title bar), B (floating), C (rail) on the real window; owner: "I like c best". The rail is the default (pg.bar = rail, expanded on first run, Collapse/Expand persisted in pg.rail): TitleStrip on top; CommandRail with repository/open/recents, the eleven commands, Settings and the toggle; 48px icons with tooltips collapsed, 188px with labels and per-item option chevrons expanded; right-click opens options when collapsed. Title-bar placement kept as a Settings option; floating removed. Both placements render components/commandItems.tsx (one list). [agent: claude]
- [x] Owner (2026-09-05): "make sure the number of file changes has a special position, maybe above, because currently it is clipped and anyway wouldn't work with large numbers". The Commit count is a pill of its own in the rail: above the icon when collapsed (row grows to 52px), after the label when expanded; tabular digits, grows with the number, caps at "999+"; the same text feeds the toolbar badge. Captured collapsed and expanded in light and dark. [agent: claude]

### v0.13.16 — Owner polish on the rail build — HD app icon, instant context menus, rail toggle on top</title> (2026-09-05) (COMPLETE)
<parameter name="goal">Owner (2026-09-05) on the v0.13.15 build: "The logo feel cheap and not high definition", "make the right click menu on the canvas more reactive", "the collapse button for the leftside rail should probably be at the top". Fix the three and release.

- [x] Owner: "The logo feel cheap and not high definition. Is this normal? Can we make it HD?" The ICO was well-formed (16-256 px) but the bare two-stroke mark reads muddy at icon sizes. New src/assets/app-icon.svg: the crossing in white and light blue on a rounded brand-blue tile with heavier strokes; `npm run icons` regenerates every platform set from it; logo.svg stays the in-app mark. Checked at 32 and 128 px. [agent: claude]
- [x] Owner: "make the right click menu on the canvas more reactive". Context and option menus (revision graph, commit dialog files and diff, rail items, toolbar carets) open with transitionDuration 0 instead of MUI's grow animation. [agent: claude]
- [x] Owner: "The collapse button for the leftside rail should probably be at the top". The Collapse/Expand row is the first row of the rail. [agent: claude]
- [x] Owner: "In the diff view, can we have at the bottom a floating transparent button to activate a mode 'hierarchical' view? ... use a directory hierarchical structure like in Git Extensions." Diff tab file list gets a translucent floating toggle (bottom right of the file column) between flat paths and a collapsible directory tree with file names; persisted (pg.diffTree). [agent: claude]
- [x] Owner: "If we can change the font of the diff view it would be good, everywhere where this font is used, because it is not very good." Monospace stack becomes platform-first (Cascadia Code / Cascadia Mono on Windows, Ubuntu Mono / DejaVu Sans Mono on GNOME, JetBrains Mono when present, Consolas), Fira Code and its preload removed; applied through MONO_FONT everywhere code is shown (diff, blob, SHAs, commit ids, job output). [agent: claude]

### v0.13.17 — Commit dialog reliability — zoom, errors, amend and drafts (PLANNED) (2026-09-05; owner authorized release with remaining local checks waived) (COMPLETE)
> Evidence and reproduction details: docs/agents/context/release-ui-audit.md; screenshots: website/public/assets/audit-ui-*.png. Preserve the eight pre-existing uncommitted UI edits. Add failing symptom regressions before fixes; do not mistake the audit fixture's missing third commit for a product defect. Keep fix tasks open pending owner verification.
**Goal:** Resolve the four reproduced UI/UX audit findings before release: keep commit actions reachable at application zoom, explain failed commits, allow message-only amend, and preserve dismissed drafts. Implement after the owner's compaction handoff; no release or publish in this iteration without owner instruction.
- [x] Regression proof first: add Windows Playwright symptom tests for all four audit findings using isolated, pinned repository fixtures with at least three commits. Verify each new regression fails for its reported symptom before changing product code; quote audit wording accurately as audit findings. Keep retries off and test fixtures independent of the owner repository. [agent: codex] Reproduced all four failures before fixes; focused checks now pass. [agent: codex]
- [x] P1 — "Commit controls fall outside the window at 150% zoom": keep the entire dialog and its Commit/Cancel controls reachable at 1280×800 with 100%, 150% and 200% application zoom, plus 800×600 at 100%. Adapt sizing and internal scrolling without reducing the chosen zoom. Assert visible, clickable controls and inspect settled composited screenshots in light/dark; check other dialogs if shared theme sizing changes. Fixed in v0.13.17 release working tree; owner authorized review-and-release on 2026-09-05, waiving separate manual verification. [agent: codex]
- [x] P1 — "A failed commit gives no visible error": catch commit rejection inside the dialog, show an accessible actionable error, retain message and staged state, and permit retry. Show pending feedback and prevent duplicate submission while a request is in flight. Test a rejected hook response, delayed submission/double-click, then successful retry; no unhandled promise rejection. Distinguish commit success from a subsequent refresh failure so retry cannot create a second commit. Fixed in v0.13.17 release working tree; owner authorized review-and-release on 2026-09-05, waiving separate manual verification. [agent: codex]
- [x] P2 — "Message-only amend is blocked": enable Amend with a nonempty message and zero staged files when HEAD exists, while ordinary Commit still requires staged changes. Prove a real message-only amend in a disposable clean repository changes the message and commit id but preserves its tree. Keep blank-message validation. Fixed in v0.13.17 release working tree; owner authorized review-and-release on 2026-09-05, waiving separate manual verification. [agent: codex]
- [x] P2 — "Escape silently discards the commit draft": retain the draft across Escape, backdrop dismissal and Cancel, scoped to the repository and kept separate for ordinary commit versus amend. Clear the submitted draft only after successful commit; do not erase it on failed submission or overwrite an ordinary draft when opening amend. Verify dismissal/reopen, repo switching, mode switching and success/failure behavior; follow Git Extensions behavior where applicable. Disk persistence across app restarts is outside this iteration. Fixed in v0.13.17 release working tree; owner authorized review-and-release on 2026-09-05, waiving separate manual verification. [agent: codex]
- [x] Acceptance: nine focused regressions, build/typecheck, lint and format passed. Unit suite 81/82 (unrelated graph timing budget); Windows full e2e interrupted and Linux engine check failed. Owner waived remaining local e2e, native, resolution, visual baseline and Linux/WebKit verification and authorized review-and-release on 2026-09-05. Exact evidence and limits: docs/agents/context/commit-reliability-handoff.md. [agent: codex]
- [x] Owner release authorization on 2026-09-05: review and push, release the app; manual verification gate waived. Include reviewed pending UI changes, publish v0.13.17, then deliver a new frontend/UX audit. Existing CI and artifact guards remain enabled. [agent: codex]

### v0.13.18 — Usable workspace at zoom and consistent settings (2026-09-07) (COMPLETE)
> Follow-up audit: docs/agents/context/frontend-ux-next.md. Preserve existing release v0.13.17; implement the next scoped improvements. Record verification limits accurately.
**Goal:** Fix reproduced shell zoom shrinkage and inaccessible rail actions, make responsive layout follow the content area, and make Settings save/cancel behavior predictable. Owner requested continued iteration with review and focused visual checks instead of large local e2e suites.
- [x] Fix shell viewport sizing at 100/150/200% zoom; retain readable graph space and correct splitter motion. Verify actual settled bounds and pixels in Windows Chromium. [agent: claude]
- [x] Keep command rail actions reachable in short windows, pin Settings, and respond to available width in both rail and title-bar layouts. Make the repository row open the repository switcher. [agent: claude]
- [x] Make Settings consistently stage changes until Save, with Cancel/dismissal discarding edits; show Git setting scope and readable line-ending labels. Review async and error behavior. [agent: claude]
- [x] Review, typecheck/build and inspect focused screenshots; no large local e2e reruns per owner instruction. Record limits and push completed changes. [agent: claude]
- [x] Acceptance (2026-09-07): Codex's shell/rail edits kept and finished; Settings rewritten as drafts (Appearance, Git identity with scope note and plain line-ending labels, Tools). Guards: tests/e2e/shell-zoom.spec.ts (root fills 1280x800 at 150 %, Settings reachable in a 420 px-tall window, Cancel discards / Save applies, repo row opens the switcher) + shell settings specs + focus-management, 7/7; dialog-settings visual baseline refreshed and inspected; tsc + eslint clean. Not run: full e2e, resolution, native, Linux (owner: no large local suites). [agent: claude]

### v0.13.19 — Never stale: change detection that survives agents, and remote refs marked with a cloud (2026-09-07) (COMPLETE)
**Goal:** Owner (2026-09-07): "sometimes the tool goes stale when an agent updates the repo"; "a clear visual differentiation between local branches and remote branches, a little cloud icon on the left of the remote branches"; "need also a button to refresh, just in case".
- [x] Root cause: the change stream (GET /events, .git watcher) never sees an agent editing tracked files (no git command touches .git), and the client dropped any event within a flat 2 s of its own refresh, so an external commit landing in that window was lost for good. useRepoState now mutes only while a refresh is in flight plus 700 ms (one SSE poll), re-fetches status every 10 s while the window is visible, and does a full sweep when the window regains focus. Refresh (rail, F5) stays as the manual fallback. [agent: claude]
- [x] Guard: live-refresh-scope.spec 'an edit to a tracked file with no git command shows up on its own' (fixture repo, no git command, status bar reaches '(1 change)'), plus the existing stage-only and commit cases, 3/3 against a private engine 0.13.17. [agent: claude]
- [x] Graph ref chips: remote-tracking branches carry a cloud glyph (CloudOutlined) before the name and data-ref-kind=remote; a ref counts as remote when its first segment is a known remote name (a local feature/x branch no longer turns green). The ref tree already used the cloud icon. @grid visual baselines refreshed and inspected. [agent: claude]
- [x] Owner ticks: let an agent edit and commit in the open repository while PowerGit sits in the background; on coming back the change count and the graph are current without pressing Refresh. [agent: owner-release-2026-09-07]
- [x] Owner ticks: remote branches on graph rows are told apart from local ones at a glance (cloud glyph). [agent: owner-release-2026-09-07]
- [x] AppImage icon on GNOME (owner report): GNOME maps the window's app id (powergit) to an installed powergit.desktop and takes the icon from there; an AppImage installs nothing, so the dock/Alt-Tab show the generic gear (always on Wayland, where a window cannot set its own icon). New src-tauri/src/desktop_integration.rs: when $APPIMAGE is set, write $XDG_DATA_HOME/applications/powergit.desktop (Exec=the image, Icon=powergit, StartupWMClass=powergit) and the 128/256/512 px PNGs into hicolor; idempotent, rewrites when the image moved, POWERGIT_NO_INTEGRATE opts out, best-effort cache refresh. Compiled on every OS, called only on Linux. cargo test 9/9 (3 new). docker/appimage-check/launch.sh asserts the entry + 256 px icon after launch (skip on older artifacts) — runs in the release matrix; not run locally. [agent: claude]
- [x] One font everywhere (owner: "use the same font as in the main view everywhere"): MONO_FONT now aliases the UI stack (Segoe UI / Ubuntu / system-ui), .sha and diff/code rules use the token with tabular figures. Trade-off stated to the owner: diff column alignment now relies on tabular numerals and leading spaces, not a monospace face. bottom/grid/dialog visual baselines refreshed and the diff view inspected. [agent: claude]
- [x] Owner ticks: on GNOME, after launching the next AppImage once, the dock and Alt-Tab show the PowerGit icon (and a launcher entry exists). [agent: owner-release-2026-09-07]
- [x] Owner ticks: the diff view, file lists and SHA column use the same face as the rest of the UI. [agent: owner-release-2026-09-07]

### v0.13.20 — Refresh without relayout on large histories (2026-09-07) (COMPLETE)
**Goal:** Field report (eve-aps4305, 10k revisions): every refresh froze the UI although the engine answered in ~300 ms. Keep refreshes cheap on large repositories.
- [x] Root cause (confirmed from the report's reading of useHistory.ts): reloadHistory rebuilt page 0 through toRevision, so no row kept its object identity, the layout effect's append check never held, and every refresh (SSE, F5, focus, v0.13.19's poll on a refs change) posted a reset with the whole loaded history to the worker: structuredClone of 10k rows + full lane layout = the freeze. The engine was fast (revisions ~325 ms, refs ~100 ms, status ~30 ms). [agent: claude]
- [x] Fix: src/hooks/historyMerge.ts — mergeReload reuses the existing Revision object when id, subject, author, date, parents and refs are equal, splices the tail as before, and reports `unchanged`; reloadHistory then skips setRevisions entirely, so a no-op refresh costs one /revisions fetch and a compare, no clone, no relayout. A new commit on top still relayouts (a prepend cannot be incremental) but only the changed rows are new objects. Unit tests: historyMerge.test.ts (unchanged, new-commit-on-top, lost overlap, short page), vitest 86/86, tsc + eslint clean. Not run: e2e against a 10k-revision repository. [agent: claude]
- [x] Owner ticks: on eve-aps4305 (10k revisions) a refresh, F5 or returning to the window no longer freezes the UI; the graph stays put when nothing changed. [agent: owner-release-2026-09-07]

### v0.13.21 — Showcase page, demo and screenshots
**Goal:** Owner (2026-09-07): "We need to update the page and refine the demo and fix the screenshots." The Pages site shipped without a single image (base path), a v0.6.0 download button, screenshots from v0.13.17, and a demo whose bottom panel showed an engine error.
- [x] Site: images resolve under the /PowerGit/ base (they all 404ed), the download button and screens caption read the version from frontend/package.json at build time, feature cards and screen captions describe v0.13.20 (rail, never stale, partial staging, frameless, AppImage launcher entry), the demo tip matches the rail layout. [agent: claude]
- [x] Demo: sample rows with real-looking SHAs, subjects, authors and recent dates; forks and merges in the first rows; branches, remotes and tags in the ref panel; Commit and Diff tabs served from sample data (commitCache demo branch) instead of "commit failed: no repository open"; File Tree tab explains it needs the engine. Checked in the browser with ?demo=1. [agent: claude]
- [x] Screenshots re-captured from the real app on the PowerGit repository (capture-showcase.mjs: rail layout, light + dark, diff tab in tree mode, file tree, commit dialog, Stash options menu) and each one inspected before publishing. [agent: claude]
- [ ] Owner ticks: cynacons.github.io/PowerGit shows every screenshot, the current version on the download button, and a demo whose bottom panel shows commit details and a diff.
- [x] Owner report: the shipped app lists development repositories under Recent repositories. Root cause: recents.json is one per-user file (LocalApplicationData/PowerGit) shared by every engine on the machine, and the e2e suites' and engine tests' fixture repositories (pg-commit-reliability-*, powergit-live-refresh-*, pg-iso-*) filled all 20 slots; nothing is baked into the AppImage. Fix: RecentsStore prunes roots that no longer exist on every read, and POWERGIT_DATA_DIR moves the store — set by npm run engine (temp/powergit-dev-data), the Ubuntu harness and a module initializer in the engine test project, so no dev or test engine touches the user's list. RecentsStoreTests (override honoured, deleted roots pruned); engine 61/61. [agent: claude]

### v0.14.0 — In-app updates (manual check, signed manifest) and merge-commit diffs (2026-09-07) (COMPLETE)
**Goal:** Owner (2026-09-07): "an update mechanism from within the app... an update button, that downloads the new appimage, and restarts the app" (policy chosen: manual check only; signing handled by the agent); bug report "merge commits are not showing a diff in the diff view". Plan: ~/.claude/plans/adaptive-jingling-floyd.md.
- [x] Symptom-first spec for "merge commits are not showing a diff in the diff view": tests/e2e/merge-diff.spec.ts (fixture with one --no-ff merge; the merge row's Diff tab lists the file and shows the hunk). Committed red before the fix. [agent: claude]
- [x] Engine: ListFiles / GetDiff / GetChanges diff a merge against its first parent with --diff-merges=first-parent (measured: -m --first-parent also emits the second parent's diff; the chosen flag is byte-identical to an explicit ^1 diff, and to the old output for ordinary and root commits). QueryTests.Merge_commit_lists_files_and_first_parent_diff (single file from the feature branch, hunk present, GetChanges == ListFiles + GetDiff); engine 62/62. [agent: claude]
- [x] Signing: keypair generated with tauri signer (private key + password only under ~/.tauri on the owner's machine, never in the repo); public key in tauri.conf.json; TAURI_SIGNING_PRIVATE_KEY / _PASSWORD repository secrets set. [agent: claude]
- [x] Shell: tauri-plugin-updater 2.11 + tauri-plugin-process registered; plugins.updater { pubkey, endpoints: releases/latest/download/latest.json, windows.installMode passive }; capabilities updater:default + process:allow-restart. createUpdaterArtifacts lives in src-tauri/tauri.updater.conf.json and is only passed by CI (POWERGIT_SIGN=1 in package-windows.ps1) so local builds stay key-free. The plugin builder has no endpoint override, so a local walk uses a --config override of the endpoints (release skill 3b); cargo test 9/9. [agent: claude]
- [x] CI: windows job builds signed (setup.exe.sig); linux job signs the repaired AppImage after inspect --fix and the launch matrix; manifest job assembles latest.json (scripts/build-updater-manifest.mjs) and uploads it with the .sig files; check-version.mjs --manifest guard; release skill updated. [agent: claude]
- [x] Frontend: src/updates/updateMachine.ts (pure reducer, vitest) + updater.ts adapter (Tauri / mock via pg.updateMock / null in the browser) + useUpdater; Settings → Updates section: version, Check for updates, inline result, Download and restart with progress, portable-build note. Manual only: no background checks. e2e tests/e2e/updates.spec.ts with the mock. [agent: claude]
- [x] Verified: cargo test 9/9 with both plugins; engine 62/62; vitest 93/93 (updateMachine 7); full e2e 85/85 once against a private engine (merge-diff, updates x4, settings); @dialogs visual refreshed (Updates section sits below the fold, baseline unchanged) and the section captured with the mock and looked at. Not run: a local unsigned tauri build (happens at release packaging), the Linux walk against a locally signed AppImage (no Linux box here), and the CI signing/manifest jobs, which first run on the v0.14.0 tag. [agent: claude]
- [x] Owner ticks: merge commit shows its diff in the diff view. Fixed 2123950f7 (spec tests/e2e/merge-diff.spec.ts green), awaiting owner verification. [agent: owner-release-2026-09-07]
- [x] Owner ticks: on Linux, Settings → Check for updates finds v0.14.1, downloads it and PowerGit reopens on the new version (only provable with the release after this one). [agent: owner-release-2026-09-07]
- [x] Branch history highlight (owner: "better highlight all the ancestors of the currently checked-out branch ... better see who merged what and when"): src/graph/ancestry.ts marks every loaded commit reachable from HEAD (2 = first-parent line, 1 = merged-in work) in one pass over the date-ordered rows; null when HEAD is not loaded. Unit tests on the synthetic history. [agent: claude]
- [x] Drawing: draw.ts rings highlighted nodes with the HEAD outline (1.5px) and, with dim on, paints everything outside the history in the non-relative grey while highlighted lines get a heavier stroke; scope all / first-parent. Options persisted under pg.graph (graphOptions.ts, default all + ring + dim). [agent: claude]
- [x] GraphOptionsBar: floating pill at the bottom of the graph column like the diff options bar; hover opens scope select and Ring / Dim toggles. RevisionGrid wires ancestry + options into the repaint. [agent: claude]
- [x] Symptom-first e2e tests/e2e/branch-highlight.spec.ts (fixture: main, merged feature, unmerged wip; pixel sampling: wip row grey, ancestor row lane colour + ring; first-parent scope greys the merged commit but not the merge; dim off restores colour), light + dark. @grid baselines refreshed and looked at; demo checked. [agent: claude]
- [x] Owner ticks: the checked-out branch's history stands out in the graph, and the bar switches scope and style. [agent: owner-release-2026-09-07]
- [x] Commit dialog (owner: "a bit limited in size ... we can't resize the panels on the left ... takes only part of the application space"): the dialog now fills the window minus 16px margins; the file column has a drag handle (SplitHandle, pg.commitFilesWidth, 220px to 60%) and a bar at its very bottom (CommitFilesBar) switching both lists between full paths and a directory tree (pg.commitTree), the diff view's mode reused via CompactFileList tree. [agent: claude]
- [x] Graph ref chips: tags carry a tag glyph in a violet chip (ref badge token `tag`, light + dark), remote branches keep the cloud; a commit with a tag and a remote branch shows both chips side by side (owner: "tags should be having a different little icon ... remote + tag on the same thing"). [agent: claude]

### v0.14.1 — Diagnostic snapshot with a watchdog, pending changes in the graph, floating bars that stay put (2026-09-07) (COMPLETE)
**Goal:** Owner (2026-09-07): "sometimes after a while the app freezes ... I don't have a way to bring back the logs" → a snapshot button above Settings plus a shell watchdog that writes the snapshot when the webview stops responding; "pending changes as a temporary side commit in the graph ... reviewed with the diff view directly from the main view" → Working directory / Index rows like Git Extensions, review only; "the floating minibars ... blink or disappear". Plan: ~/.claude/plans/adaptive-jingling-floyd.md.
- [x] Symptom-first specs committed red: tests/e2e/floating-bars.spec.ts ("the floating minibars ... blink or disappear"), pending-rows.spec.ts ("pending changes as a temporary side commit in the graph"), snapshot.spec.ts ("an emergency button just above the settings ... dump all the information"). [agent: claude]
- [x] Shell: heartbeat + log_frontend + diagnostic_snapshot + last_incident + reveal_path commands; frontend.log beside engine.log; snapshot.rs builds snapshot-<time>.zip (shell facts, both logs, the webview's dump, engine health/sessions/recents/jobs over a tiny HTTP client); watchdog.rs marks the webview unresponsive after 15 s without a beat, writes an automatic snapshot and incident.json; cargo tests for the zip, the watchdog transitions and chunked HTTP decoding. [agent: claude]
- [x] Frontend diagnostics: report() lines stream to frontend.log; long-task observer with the current activity; 60 s memory/state sample; heartbeat every 2 s; buildFrontendDump + takeSnapshot; "Diagnostic snapshot" rail item above Settings (rail and nav rail); SnapshotDialog (path, Copy path, Show in folder; dump text in the browser); incident banner on the next launch; Settings → Tools → Open logs folder. [agent: claude]
- [x] Pending rows: graph/artificial.ts injects Working directory / Index rows above HEAD after layout (HEAD's lane or a free one, exclusive segments, identity kept while counts are unchanged); dashed hollow node; ancestry keeps them in scope; grey chip, empty SHA; bottom panel Commit tab summary + "Open commit dialog", Diff tab from status files with workTreeDiff, File Tree at HEAD; context menu and hotkeys guarded. Unit tests. [agent: claude]
- [x] Floating bars: useFloatingBar keeps the bar expanded while a menu is open or focus is inside, body stays mounted; DiffOptionsBar and GraphOptionsBar; file-list-mode button anchored to the visible bottom of the file list. [agent: claude]
- [x] Verified: cargo 17/17 (zip, watchdog, chunked HTTP), vitest 105/105, full e2e 91/91 once (the three symptom specs green; specs that clicked "the first row" now pick the first commit row, since the dirty dev checkout starts with the Working directory row), @grid/@dialogs visuals refreshed and looked at (dashed hollow node above HEAD). Dev app (Tauri, CDP): rail button wrote snapshot-….zip with shell.txt, frontend.json, both logs, engine health/sessions/recents/jobs; a 20 s main-thread freeze produced "webview unresponsive: no heartbeat for 18s", a watchdog snapshot, incident.json, then "responsive again after 6s"; frontend.log recorded the 20000 ms long task and the 60 s sample. Also fixed on the way: the change-stream echo mute now defers an event instead of dropping it (git add then commit within a second lost the commit). [agent: claude]
- [x] Owner ticks: when the app freezes, the watchdog's snapshot (and the button while it still responds) gets the logs back to this chat. [agent: owner-release-2026-09-07]
- [x] Owner ticks: pending changes show as Working directory / Index rows on top of HEAD and are reviewable in the Diff tab from the main view. [agent: owner-release-2026-09-07]
- [x] Owner ticks: the diff context lines can be changed from the floating bar without it disappearing. [agent: owner-release-2026-09-07]

### v0.14.2 — Freeze on Linux — paint watchdog, self-recovery, WebKitGTK renderer workaround (2026-09-08) (COMPLETE)
> A frozen picture with a live script is the WebKitGTK compositor, not the page. Detect it (a paint heartbeat driven by requestAnimationFrame), recover from the shell (reload the webview, then a native dialog that can save a snapshot and restart even when the webview is black), hook the platform crash signals, and set the WebKitGTK environment that is the known fix for black views after idle.
**Goal:** Owner (2026-09-08, Linux AppImage, Ubuntu, WebKitGTK): "the app froze while I wasn't using it. None of the buttons work, the only responsive thing is Open repository (system picker). The window moves and resizes, the content is frozen, some areas are black and not redrawn. Can't use the Diagnostic snapshot button." Snapshot snapshot-2026-09-08T07-23-57 shows three button presses that all reached the shell with a heartbeat 0.3 s old: script alive, painting dead, watchdog silent. Engine idle at 6 requests/min, no long tasks, no errors.
- [x] Read the owner's snapshot (snapshot-2026-09-08T07-23-57): three button presses reached the shell with a heartbeat 0.3 s old, no watchdog line, engine idle, no long tasks — script alive, paint dead, WebKitGTK on Ubuntu.
- [x] Paint heartbeat: `useHeartbeat` requests one animation frame per beat and reports `frameAgeMs` (null while hidden); `heartbeat` command records `last_paint`; `frameAge` unit-tested.
- [x] Watchdog state machine (`watchdog.rs`): script / paint / crash stalls → snapshot + incident with `kind`, reload the webview after 20 s (a crash at once), native "Restart PowerGit / Keep waiting" dialog 30 s after a reload that did not bring frames back, recovery logged with the stall kind. Cargo tests for each path.
- [x] Platform crash hooks (`crash_hooks.rs`): WebKitGTK `web-process-terminated` and WebView2 `ProcessFailed` feed the watchdog as a crash.
- [x] Snapshot button with a dead picture: the shell shows the saved path in a native dialog when the page has not painted for 20 s (owner: "can't use the Diagnostic snapshot button").
- [x] `shell.txt` gains the display facts the next Linux report needs: last frame age, stall state, crash report, XDG_SESSION_TYPE, WAYLAND_DISPLAY, GDK_BACKEND, the WEBKIT_* switches, LIBGL_ALWAYS_SOFTWARE, NVIDIA driver line.
- [x] Linux: `main.rs` sets WEBKIT_DISABLE_DMABUF_RENDERER=1 before WebKit starts unless already set or POWERGIT_KEEP_DMABUF=1 (the documented fix for black, non-redrawing WebKitGTK views).
- [x] Incident banner names the stall kind (window stopped updating / page process crashed / stopped responding).
- [x] Drill on Windows dev app: stub requestAnimationFrame so frames stop → engine.log shows the paint stall, the snapshot, the reload, and recovery; a 20 s busy loop still yields the script stall.
- [x] Docs: diagnostics memory (paint heartbeat, recovery ladder, Linux env switch, reading the new shell.txt lines), README "If something goes wrong".
- [x] Owner: on the Linux AppImage, the freeze after idle no longer happens, or when it does the window comes back on its own (reload) and engine.log names the stall; the snapshot button answers with a native dialog if the picture is dead.
- [x] Owner 2026-09-08: "recent repositories seem not persistent" — they are on disk (engine `recents.json`), but the page only asks for them after a repository has loaded, so the Recents dialog is empty at startup. Fetch them as soon as the session is live and after every open/remember.
- [x] Recents dialog: a small cross at the top right of each card removes the entry (engine `DELETE /repos/recents?root=`, `RecentsStore.Forget`), with a test.
- [x] Owner: the Recents dialog lists the previous repositories right after launch, and the cross removes one for good.

### v0.14.3 — Graph grid polish — continuous lines through pending rows, resizable columns, wide-graph scrollbar, syntax highlighting (2026-09-08) (COMPLETE)
> Placeholder iteration recorded so the requests are not lost; to be detailed when it starts.
**Goal:** Owner (2026-09-08): the Working directory / Index rows "kill" the other branches' lines; the main grid columns should be resizable; a graph wider than its column gets a discreet horizontal scrollbar with Shift+wheel; the diff and commit views show plain text and should have language detection with syntax highlighting from a public library.
- [x] Pending rows carry every line that continues below the row above HEAD (`withArtificialRows` pass-through segments, lane taken from HEAD's row); unit test with a topic branch running past HEAD.
- [x] Resizable columns: header handles for Graph, Author, Date, SHA (`gridColumns.ts`, localStorage `pg.gridColumns`, double-click resets); `grid-columns.spec.ts`.
- [x] Wide graph: `graph-scrollbar` at the column's bottom when the lanes exceed the column, canvas translated by its scroll, Shift+wheel on the grid drives it; spec.
- [x] Syntax highlighting in the diff and commit views: Shiki tokens per hunk line by the file's language (Light+/Dark+ by theme), added/removed rows tinted, unknown files stay plain (`tokenizeLines`, `useDiffTokens`); `diff-highlight.spec.ts`.
- [x] Visual baselines refreshed (grid header handles, diff tint) and looked at.
- [x] Owner: pending rows no longer break the other branches' lines; columns resize and remember; a wide graph scrolls with Shift+wheel; diffs are highlighted.

### v0.15.0 — Git Extensions parity — right-click menu, merge / rebase / conflict resolution, settings (2026-09-08) (COMPLETE)
> Placeholder iteration; needs its own plan session with owner decisions (in-app conflict editor vs mergetool hand-off, interactive rebase scope, which settings).
**Goal:** Owner (2026-09-08): "massive changes: the right-click menu will be upgraded; we will integrate merges, rebase and conflict resolution by reintegrating what Git Extensions does natively; we will enhance the settings menu." Decisions: GE-style Resolve conflicts dialog (no in-app editor); rebase onto + interactive rebase (pick/reword/edit/squash/fixup/drop, reorder), autosquash, rebase-merges, fixup/squash commits; merge with FormMergeBranch options; settings: identity scopes + tools, behaviour and confirmations, auto-fetch, default merge/rebase options; the full GE commit menu; one release. Folded in: the second Linux freeze (snapshot 2026-09-08T10-35-06: frames still firing, GTK presentation dead, app under XWayland via the packaging hook's GDK_BACKEND=x11).
- [x] Part 0 shell: WEBKIT_DISABLE_COMPOSITING_MODE=1 on Linux (POWERGIT_KEEP_COMPOSITING=1 keeps it), POWERGIT_WAYLAND=1 drops the hook's GDK_BACKEND=x11, snapshot button pressed twice within 15 s forces the watchdog ladder (reload, then the native restart dialog) and shows the saved path natively; shell.txt adds the WebKit version and the switches; cargo tests + Windows drill.
- [x] Part 1 engine: RepoStatusDto.State/Operation/Conflicts, GetOperationState (rebase-merge, rebase-apply, MERGE_HEAD, CHERRY_PICK_HEAD, REVERT_HEAD), unmerged entries as status "C" (untracked stays "U"), watcher classifies the operation files, GIT_EDITOR=true; auto-abort removed from rebase/cherry-pick/revert (tests red first).
- [x] Part 2 engine: GitHost.Sequencer.cs — /merge (+continue/abort), /rebase (+continue/skip/abort, autostash, rebase-merges, autosquash, todo), /cherry-pick and /revert continue/skip/abort, /conflicts (+blob, resolve ours/theirs/base/mark/delete via checkout-index --stage), /mergetool, /rebase/todo capture with the cp sequence editor, /compare, /commits/{id}/archive; SequencerTests + ApiTests.
- [x] Part 1 frontend: types, OperationBanner (op-resolve/continue/skip/abort), StatusBar state caption, gitErrors conflict/in-progress kinds, dialogs union, "C" colour.
- [x] Part 3 dialogs: MergeDialog, RebaseDialog options, ResolveConflictsDialog, CompareDialog, ConfirmDialog replaces window.confirm/prompt; actions in useGitActions; AppDialogs wiring; rail Merge enabled; hotkey Ctrl+M.
- [x] Part 3 menus: revisionMenuModel + rewritten RevisionContextMenu (full GE order, submenus reset/delete/compare/copy, archive, open in browser), RefContextMenu on ref chips and RepoTree; unit test for the model; context-menu.spec.
- [x] Interactive rebase: InteractiveRebaseDialog + todoModel (pick/reword/edit/squash/fixup/drop, reorder), fixup/squash commit from the menu; engine exec-based reword; rebase-interactive.spec.
- [x] Part 4 settings: engine config scopes + ToolLocator + tool writes; SettingsDialog identity scope, tools, editor, Behaviour section (confirmations, auto-fetch, default merge/rebase options) with testids; theme/behaviour.ts prefs; useAutoFetch; settings.spec.
- [x] Specs: merge.spec, rebase.spec, git-ops-extra wording; full e2e; visual baselines; docs (operation-state memory, engine-exe-lock contract, SRS-git-engine ENG-030..039, SRS-settings, git-extensions-map, diagnostics, README).
- [x] Owner: on Linux the freeze does not recur with compositing off, or pressing the snapshot button twice brings the window back.
- [x] Owner: a conflicting merge is resolved end to end from the banner (Resolve → take theirs → Commit merge).
- [x] Owner: an interactive rebase squashes and reorders; a rebase that stops offers Continue / Skip / Abort.
- [x] Owner: settings scopes and tools take effect (git config --show-origin), confirmations obey the Behaviour switches.
- [x] Owner 2026-09-08: raw git output surface, prototypes reviewed (artifact 0cead3bd), owner picked B+D — a permanent 22 px "Git console" dock line at the bottom that opens (Ctrl+`) into a real log with filter and copy, plus a transient card in the corner on failures only. Engine keeps a rolling buffer of its last 50 git invocations (sanitized command, exit code, duration, output) at GET /repos/{id}/gitlog and on the change stream; per-entry size cap; Ctrl+` through the hotkey catalog.
- [x] Owner: the git console shows the exact command and output of every operation, and a failed command says why without being hunted for.

### v0.15.1 — Graph search — find or filter from the top bar
> Placeholder; design later.
**Goal:** Owner (2026-09-08): "in the top bar, when in the graph view, there shall be a search function, with either the option to find or to filter. This will be a complicated feature, we will design and implement in a second time."

### v0.15.2 — Ubuntu freeze diagnostics: inspector and focus timeline (2026-09-09) (COMPLETE)
**Goal:** Ship release-enabled inspector, durable focus/visibility/refresh diagnostics, recent repositories on startup without an open session, and an app-location action. Light focused testing per owner (2026-09-09). Freeze remains under investigation; recent-list fix awaits owner verification.
- [x] Enable developer tools in release builds, a Settings action and POWERGIT_DEVTOOLS=1 startup option; verify available docking behavior. [agent: codex]
- [x] Record native focus transitions independently of the page, immediate page focus/visibility logs, and correlated refresh start/end durations. [agent: codex]
- [x] Run focused diagnostics checks and build validation; document limits and prepare v0.15.2 release. [agent: codex]
- [x] Clear pre-existing CI blockers: command-log truncation marker, Git-version-dependent rebase subject formatting, merge test branch-loading race, and scroll-viewport clipping assertion; run focused checks. [agent: codex]
- [x] Investigate Ubuntu AppImage recent repositories disappearing after app restart; add a failing persistence regression before fixing, and expose/document the running AppImage location. Owner verification remains open separately. [agent: codex]
- [x] Full gates before the tag (codex shipped focused checks only): engine 113/113, cargo 21/21, vitest 161/161, e2e 118/118, resolution 20/20, eslint + tsc clean, check-version 0.15.2. Visual baselines refreshed onto the new history and reviewed (dialog-settings deliberately untouched: its "engine 0.14.3" text is below the 2 % tolerance). Packaged zip smoke-tested from a clean extract with WEBVIEW2 CDP attached: sidecar /health 0.15.2, recents load with no repository open (the fix, on the real artifact), opening one renders 26 rows, no console errors. [agent: claude]
- [x] Cleared 187 leftover pg-commit-reliability-* fixture repositories from the user's Temp directory; 13 of them held slots in the real recents.json because an engine was started by hand without POWERGIT_DATA_DIR (the v0.13.21 guard lives in npm run engine, not in a bare dotnet run). RecentsStore's prune-on-read then reduced the packaged app's list to PowerGit alone, confirming that guard still works. [agent: claude]

### v0.15.3 — Read the console without WebKit's inspector
**Goal:** Owner (2026-09-09): "the fix in 0.15.2 to show the debugger panel did not work. Give me a 15.3 very quickly with a button in the settings to open that drawer." The v0.15.2 button exists and the devtools feature is compiled in, so the inspector is failing silently on the owner's Ubuntu: `open_devtools` returns no Result, so nothing reports why. Make that failure speak, and stop depending on WebKit's inspector — PowerGit already keeps a 200-entry diagnostic ring but only RecoveryPanel renders it (and only when the app is already broken), and console.* is written to the console, never captured from it.
- [x] Capture console.log/info/warn/error/debug into the diagnostic ring, forwarding to the real console and guarding the re-entrancy report() already creates; per-message cap so one huge object cannot flush the ring.
- [x] Diagnostics drawer in the shape of the approved git console: live ring, level chips, filter, Copy all, long tasks and the log path, with Open logs folder. Settings button plus a hotkey.
- [x] open_devtools returns a Result and logs the attempt, the frontend propagates it, and Settings shows why the inspector did not open instead of a dead button.
- [x] Gates and release: vitest for the capture, e2e for the drawer, cargo for the command; packaged build checked by hand; docs (diagnostics memory, README) and SRS-SET-045.
- [ ] Owner 2026-09-09: "the fix in 0.15.2 to show the debugger panel did not work." Awaiting owner verification on Ubuntu that the drawer shows the console without the inspector.
- [x] Two bugs the build surfaced, both of which would have made the tab useless: diagnosticsSnapshot() returned the live mutated ring, so useSyncExternalStore and useMemo saw no change and the panel froze at its first render (a screenshot caught 1 of 4 entries showing) — it now returns a fresh array per entry, with a test; and warnings rendered in console.ok, the green the git log uses for a command that succeeded, so the console surface gained its own amber. [agent: claude]

### v0.15.4 — The freeze is in the UI process — capture it from outside
**Goal:** Owner (2026-09-09): "when the crash happens, even the developper panels does not refresh!!!" The WebKitGTK inspector is a separate web view driven by the SAME GTK main loop as the window, so an inspector that also stops updating means the main loop is stuck, not the page — which fits the black areas that never repaint and the 2026-09-08 snapshot showing animation frames still firing. Consequence: no in-app surface can diagnose this, including v0.15.3's app log. So: fix the blocking I/O we ourselves put on the main loop, and ship a script that captures a frozen app from beside it.
- [x] logwriter.rs: both log files move to a writer thread, so log_line is a channel send. Every line used to take Mutex<Option<File>> and do writeln! + flush() on the caller's thread — and one caller is the RunEvent closure, i.e. the run loop, on the main thread, on every window focus change (added in v0.15.2, in the code investigating focus freezes), while the sidecar's stdout task held the same lock per engine line. sync() is bounded and used only by snapshots and shutdown. 5 cargo tests. [agent: claude]
- [x] scripts/freeze-dump.sh: run beside a frozen app. Per-thread state and wchan from /proc (no debugger, no root), backtraces when eu-stack or gdb exists, whether the sidecar still answers /health, and the log tails, into one tar.gz. Tested in WSL Ubuntu against a process deliberately blocked on a mutex: reported futex_wait_queue for the main thread. The debugger branch is untested — none was installable there. [agent: claude]
- [x] Engine framework logging drops to Warning (POWERGIT_LOG_LEVEL restores it), scoped to Microsoft.AspNetCore and System.Net.Http so Microsoft.Hosting.Lifetime keeps 'Now listening on'. engine.log had passed 9 000 lines of per-request chatter, six lines every two seconds from the console poll, each flushed to disk — burying the diagnostics we read and churning the disk behind them. Verified: boot plus six requests now logs the startup lines and nothing else. [agent: claude]
- [x] Docs: diagnostics memory records why no in-app surface can see this failure, the wchan table for reading threads.txt, and that the main-loop I/O is a real defect fixed on evidence but NOT proven to be the owner's freeze (which predates v0.15.2). README tells the owner to run the script while the window is still frozen. [agent: claude]
- [ ] Owner 2026-09-09: "when the crash happens, even the developper panels does not refresh!!!" v0.15.4 removes blocking I/O from the main loop and ships freeze-dump.sh; the root cause is still unknown and awaits a dump taken during a real freeze on Ubuntu. Status 2026-09-18: superseded by the owner's v0.15.7 tick of 2026-09-11 (native Wayland default, "No issues at all", no freeze since; H1 unconfirmed because none recurred) — the owner's own tick closes this line (v0.18.19 E). [agent: claude]

### v0.15.5 — Reset from the diff view — right-click a file, select lines, undo a commit's change
**Goal:** Owner (2026-09-09): "when I'm in the diff view, I should be able to right click on a file and hit reset. Same for the diff in the diff view. Should be able to select some line, and hit reset." Both already exist in the commit dialog only; the Browse bottom panel is deliberately read-only (pendingRows.ts: "Review only; staging lives in the commit dialog"). Owner on semantics: "resetting what the user is looking at" — a Working-directory row must not destroy staged work — and on a commit, "undo what the user selected in the diff view", landing in the working tree and the index. The merged staged+unstaged view stays a prototype: "Tricky feature, you'll have to show it to me before implementing."
- [x] Engine: /files/reset takes a scope — head (today's index+worktree), worktree (restore from the index, staged work survives) or index (unstage, the file on disk untouched). ResetFiles was the wrong call for a Working-directory row: it runs reset HEAD + checkout HEAD and destroys staged changes the user never saw. [agent: claude]
- [x] Engine: /patch takes index and threeWay so a selection reverse-applied from a commit lands in the working tree AND the index, the way Git Extensions' ApplySelectedLines does (git apply --3way --index). Plus a MaxPatchChars guard — ApplyPatch validated nothing. [agent: claude]
- [x] Browse Diff tab: right-click a file in the list and right-click inside the diff, with line selection (click, Ctrl+click, Shift+click) driven by the same useDiffLineSelection the commit dialog uses. Items disabled with a tooltip, never hidden. [agent: claude]
- [x] Reset means exactly what is on screen: Working directory restores from the index, Index unstages, a commit reverse-applies into worktree + index. Labels and confirmations name what survives; on a commit the verb is Undo, not Reset. Untracked files are deleted, with a confirmation that says git holds no copy. [agent: claude]
- [x] Two correctness gaps closed on the way: a truncated diff and a whitespace-ignoring diff (-w) can no longer build a patch — both produce text git cannot apply. partialEligibility now takes the DTO and the diff options. [agent: claude]
- [x] Gates: engine 128 (15 new, one scope each plus --index --3way and a staged deletion, which caught a regression in the new code — no index entry does not mean HEAD has nothing), vitest 200 (16 new), e2e 126 (5 new, asserting the filesystem and the index rather than UI state), lint and tsc clean, and a walkthrough of all three rows in the running app. No packaged build: this is not a release. [agent: claude]
- [x] Docs: SRS-commit-details SRS-DET-020+, SRS-git-engine SRS-ENG-040+, and git-extensions-map rows for FileStatusList reset, FileViewer line patching and PatchManager (it had none). [agent: claude]
- [ ] Owner 2026-09-09: "we should merge the view into one single temp commit with a diff showing both unstaged and staged... Tricky feature, you'll have to show it to me before implementing." Prototype delivered as an artifact — four directions (merge the two rows, add a third row, a scope control on the diff pane, or mark the file and offer the merged view), each with a faithful mock in the app's own diff tokens, plus the three ways to encode staged vs unstaged now that green and red are taken, and what Reset has to ask once a line can be either. https://claude.ai/code/artifact/0662030d-ae8e-4666-a719-ca9c6a932a67 — awaiting the owner's pick; nothing implemented until then. [agent: claude]
- [x] A real bug the wiring surfaced: unstaging a PARTIAL selection built its patch from the wrong side of the diff, so git refused everything narrower than a whole hunk with "patch does not apply". Shipped broken in v0.13.14's commit dialog and never caught — its e2e only ever unstaged a whole hunk. Verified against real git, fixed, and pinned by both a unit test on the patch's pre/postimage and an e2e that fails when the old base is put back. [agent: claude]

### v0.15.6 — Ubuntu freeze taskforce — the main loop is alive, instrument what paints and let the owner drive recovery (2026-09-11) (COMPLETE)
**Goal:** Owner (2026-09-10, v0.15.5 AppImage): "Usually when I let the app run in the background and come back to it, it's frozen." Five releases of instrumentation have not found the cause. The case file's conclusion that the GTK main loop is parked contradicts its own evidence: heartbeats and snapshot presses reached the shell over IPC during both captured freezes, and IPC is dispatched by that main loop. A workflow taskforce re-audits the evidence, researches known WebKitGTK/GTK/XWayland presentation stalls, attempts a WSLg reproduction, and produces a change list.
- [x] Taskforce workflow (evidence re-audit, WebKitGTK/GTK/XWayland research, shell probe design, WSLg reproduction attempt, adversarial refutation, synthesis) run and its report saved under docs/agents/context/. [agent: claude]
- [x] Case file corrected: docs/ubuntu-freeze.md and docs/agents/memories/diagnostics.md no longer claim the main loop is parked when the evidence shows it dispatching IPC; the inspector argument restated as a shared-presentation-path symptom. [agent: claude]
- [x] Shell probes that work when the main loop is alive but nothing paints: GDK paint age next to frame age in the watchdog observation, shell.txt and a periodic engine.log line; main-loop liveness round-trip from a dedicated thread, recorded on disk for freeze-dump.sh. [agent: claude]
- [x] Recovery-experiment ladder the owner drives during a freeze, each step distinct and numbered in engine.log (queue draw, thaw updates, hide/show, 1px resize, webview reload, new window), reachable from the page and from a native fallback. [agent: claude]
- [x] Owner 2026-09-11 on v0.15.7, native Wayland: "Seems to work very well. No issues at all." No freeze since the switch. The recovery numbers and a freeze-dump capture were never needed. [agent: owner]
- [x] Native Wayland is the default on a Wayland session (POWERGIT_X11=1 keeps XWayland), and POWERGIT_NO_FRAME_SYNC=1 disables GTK's X11 frame sync: the two switches that discriminate H1 (GdkFrameClock frozen waiting for _NET_WM_FRAME_DRAWN under XWayland). Linux compile gate run in WSL (cargo check + cargo test --lib). [agent: claude]
- [x] Taskforce finding, resolved as by-design: the AppImage does not bundle gst-plugins-base (the WSLg reproduction failed on libgstallocators-1.0.so.0) because release.yml runs inspect-appimage.sh --fix --strict, which deliberately strips the host-provided families including GStreamer; every desktop Ubuntu ships them with GNOME, and the launch matrix on stock 22.04/24.04/26.04 passes. Only a bare container or a stripped WSL lacks them; documented in the taskforce report, no packaging change. [agent: claude]

### v0.15.7 — Presentation stall needs focus — v0.15.6 false alarms on Wayland (2026-09-11) (COMPLETE)
**Goal:** Owner (2026-09-11, v0.15.6): "I got two bug reports on the latest version, but actually the app is still working fine. no issues, no freeze. I think each time we hide the window it's triggering this snapshot capture and fake bug report." Confirmed from the snapshot: the presentation probe fired 23 s after focus loss on Wayland, where the compositor stops frame callbacks for a hidden surface, and the ladder reloaded the page in the background. The second report is a 5 s whole-process stall on v0.15.4.
- [x] probe.rs: paint and draw ages count only while the main window has native focus (run loop Focused event), measured from the later of the last paint and focus arrival, so a hidden window never trips Stall::Presentation and a frozen window the owner clicks still does within 20 s. probe.txt/shell.txt gain window_focused. cargo test --lib 34/34 on Windows and Linux (WSL). [agent: claude]
- [x] Case file and diagnostics memory say why the gate is focus, not mapped: Wayland gives no frame callbacks to a hidden surface. The 2026-09-10 22:10 snapshot is read in docs/ubuntu-freeze.md (page alive for 26-40 min in the background in the sessions the owner killed; the watchdog event itself a 22 s whole-process stall), and freeze-dump.sh writes pressure.txt (PSI, free, thread states, kernel log). [agent: claude]
- [x] Owner ticks: no incident banner after hiding the window for a while on v0.15.7, and the Wayland week continues (the v0.15.6 owner tick carries over). [agent: owner]

### v0.16.0 — Commit window you can size and close, GE file menus, copyable diffs, a console you can read
**Goal:** Owner (2026-09-11, after v0.15.7 closed the freeze): "The commit window should be resizeable. Ideally an external window, but that's not mandatory. Also an x on the top right to close it. In the commit window, on the left we have the files staged and unstaged: we need functional parity with what GE has, right click on my files and do operations on them. The new reset lines in the diff view is amazing, but it's missing the ability to select text for copy-paste. There are constantly errors showing on my git on the bottom right: one of the files is new, I see 'git failed - exit 1' with a diff of the new file. The bottom console concept is really good, but there's always tons of stuff in that window, I can't even see my push when I push."
- [x] Commit window: resizable from all 8 edges and corners (hand-rolled grips, no dependency; 560x400 minimum, clamped to the viewport, zoom-aware), size remembered in localStorage, opens centred; a close X in a 32 px title strip (disabled while submitting, like Cancel; Escape and Cancel unchanged). commit-window/dialogSize.ts pure geometry with 6 unit tests; e2e commit-dialog-size.spec.ts drags the corner, reloads, reopens, closes with the X. [agent: claude]
- [ ] Commit window as a separate native window: NOT done, stopped on purpose. The shell and engine halves exist (recovery.rs opens second windows, ?repo= pins a session), but index.html boots the whole app, so a commit window would be a second full shell with a second WebKitGTK webview; the ~200-line plan (main.tsx branch to commit-window/CommitWindowApp.tsx, commit-* capability label) is recorded in docs/agents/memories/commit-window.md for a later iteration. [agent: claude]
- [x] Commit file lists: GE FileStatusList menu on staged and unstaged files, in GE's order and groups: Stage/Unstage (file, all), Reset file to (Index / HEAD), Open with difftool, Open, Open with... (remembered program), Edit, Rename/move..., Delete file..., Copy path (full / relative), Show in folder (shell only), Add to .gitignore..., Add to .git/info/exclude..., Skip worktree and Assume unchanged check items, Stop tracking this file..., Show skip-worktree / assumed-unchanged files toggles. New engine endpoints /files/open, /files/edit, /files/skip-worktree, /files/assume-unchanged, /files/exclude, /files/untrack, /files/move, GET /files/hidden; status exposes the flags (one ls-files -v per status). Hidden on purpose: file history (no path filter yet, see the file-history task), interactive add (the diff line selection is the equivalent), submodule group, blame, Visual Studio, save as, run script. 20 xunit, 12 model tests, SRS-ENG-044..048, mapping in docs/agents/memories/git-extensions-map.md. [agent: claude]
- [x] Diff views: text selection works again. Cause: .diff-row-selectable had user-select: none since the v0.13.14 line selection. Now a drag selects text (a drag that produced a selection never toggles a line; Shift+mousedown no longer extends the native selection), click / Ctrl+click / Shift+click still select lines, the gutter stays unselectable, and Ctrl+C copies the text without +/- markers or line numbers (as GE's FileViewer copy does). 10 unit tests on the pure selection logic; e2e diff-text-selection.spec.ts (Browse diff and commit dialog). Landmine recorded in docs/agents/memories/diff-text-selection.md: Chromium fires click before collapsing a selection, so the drag test is pointer movement, not selection presence. [agent: claude]
- [x] No false 'git failed' for a new file: GitLogEntryDto gains Ok (the caller's verdict, default exit 0; a timeout or cancel is never ok), the untracked-file diff records ok for exit <= 1, the console model reads ok with a fallback for old engines. Found on the way: a first push popped a second false card because the engine's own rev-parse @{u} probe was not in the probe regex (it only knew @{upstream}); fixed. xunit + unit + e2e (new file in the Working directory diff: exit 1, no card). [agent: claude]
- [x] Git console you can read: newest first; user actions (push, pull, fetch, commit, merge, rebase, cherry-pick, revert, reset, stash, checkout, branch/tag/remote changes) shown with the newest one's output in full, older ones folded to a line; runs of background reads collapsed into one 'N background commands' row that expands on click; a failed action pinned at the top with its output until its X; Show all toggle remembered; the typed filter flattens so a hit is never hidden; the dock line names the newest action instead of git status. e2e git-console-readability.spec.ts: a real push to a bare remote shows within the first three rows without scrolling. [agent: claude]
- [ ] Owner ticks: commit window resizes and closes with the X; right-click on a staged and an unstaged file offers the GE operations and they work; text in a diff line can be copied; opening a new file in the diff shows no red card; after a push the console shows the push. [agent: claude]
- [x] Owner (2026-09-11): "In main view, in the file tree, right click a file and show the file history. Here again, we have to be functionally equivalent to GE." GE's FormFileHistory: the revision grid filtered to the commits touching that path (follow renames), with the diff of that file at the selected commit, its blob view, and blame; opened from the file tree's context menu (and from the commit dialog / Diff tab file lists, which today hide 'View file history'). Engine: GET /revisions gains path= (git log --follow -- path) with the same paging and lane layout; the UI opens a File history view (a tab or dialog with the filtered grid + Diff/View/Blame for that file at the selected revision), with Ctrl+Shift+H like GE. Read ../gitextensions-ref FormFileHistory and FileHistoryContextMenu for the item set and record the mapping in git-extensions-map.md; xunit on the path filter; e2e: right-click a file in the tree, choose View file history, the grid lists only its commits and the diff shows that file. [agent: claude]
- [x] Owner bug (2026-09-11): "accessing a file in the file tree when selecting the working directory pseudo commit doesn't load. It should load the latest commit on that branch on which the working directory is based." Cause, both halves: the frontend never requested a blob for the pseudo rows (commitId null, the blob effect idled on 'Loading file…'), and the engine had no route for worktree or index content (/commits/WORKTREE/blob → fatal: invalid object name). Fix as GE's FileViewer: the tree stays HEAD's; a new GET /repos/{id}/blob/worktree?path=&staged= reads the file from disk for the Working directory row and from the index (git show :path) for the Index row, path-checked inside the repository, binary and size bounded like /blob; BottomPanel and the file history View tab map the pending rows to it. xunit (disk, index, untracked, binary, escape refused) + e2e file-tree-worktree.spec.ts. Fixed 5876b8752, awaiting owner verification. Not done: listing index-only/untracked files in the pending rows' tree (GE uses ls-files); HEAD's tree only. [agent: claude]
- [x] File history (owner, 2026-09-11): the file tree's right-click View file history opens a Git Extensions FormFileHistory equivalent: GET /revisions?path= follows renames GE's way (name-only walk, then the paged log with --parents so the lane layout draws one line), Follow renames / Exact renames / Full history / Simplify merges toggles persisted, Commit / Diff / View tabs on the file at the selected revision, Escape or X back to the full history without refetching it; also from the Diff tab and commit dialog file menus and Ctrl+Shift+H. Fixed on the way: a reset + near-end race in useHistory appended page 0 to itself (docs/agents/memories/history-paging.md). Blame tab not built (no blame view). xunit 153, unit 256, e2e file-history.spec.ts. [agent: claude]
- [x] Codex Sol code review of everything since v0.15.7: six findings, none refuted, five fixed before the release. (1) The new file endpoints' path guard compared case-insensitively on every OS and never resolved symlinks: now OS-correct comparison plus a real-path walk that follows every symlink and junction, with tests (symlink escape refused, Linux-only case-distinct sibling refused). (2) The file history grid reused the main grid's context menu, offering checkout/reset/branch/rebase: it now has GE's FormFileHistory menu (Copy, difftool selected/parent and selected/local, Revert, Cherry pick, follow-renames check items), e2e asserts the item set and that reset/checkout are absent; 'Save as' still missing (no blob save route). (3) Copy full path awaited the repo root instead of falling back to the relative path. (4) Commit file tree expansions are keyed and aborted per commit. (5) useHistory's tail run is generation-keyed so toggling Follow renames restarts the eager tail (hook test under jsdom, new devDependency). (6) Ctrl+C across more than the virtualized window of a long diff copies only mounted rows, recorded as a known limit in diff-text-selection.md. [agent: claude]
- [x] Review mode prototype (owner, 2026-09-11: a review-mode button left of the tree toggle in the bottom diff view; every changed or added line starts unreviewed and the user clicks each one, line by line; a marker shows what is reviewed; first step towards AI-assisted review with notes sent at the end, like a Bitbucket review). Clickable HTML draft in docs/prototypes/review-mode.html and as an artifact: amber ring + stripe for unreviewed, blue check for reviewed, per-file progress pills, a review bar with meter and Finish review, Next unreviewed / Space / N / Enter, a draft-note affordance per line. Awaiting the owner's reaction on two calls: state kept per commit and per line, and strictly line-by-line toggling. [agent: owner]

## v0.17 — Review mode — line-by-line review of a diff, saved to a file, the first step to AI-assisted review
> Owner (2026-09-11): "review mode keeps track of every changed or added line in the diff and marks them as unreviewed; the user clicks each line, line by line; a marker shows what's reviewed; the first step towards AI-assisted review where the user drops messages in his diff and they are sent to AI at the end, like a Bitbucket review. Should be applied to both the diff view in the main panel and the commit window." Prototype approved ("perfect"): docs/prototypes/review-mode.html. Design: docs/design/review-mode.md. Five shippable iterations, each ending with a symptom test in the owner's words and an owner tick; the MCP hand-off is the major after this one.

### v0.17.0 — Mark lines in the Diff tab
**Goal:** The Diff tab of the main window gets the review-mode button left of the tree toggle; with it on, every + and − line of the shown file carries a mark (amber ring = unreviewed, blue check = ok, red × = rejected), a cursor moves with ↑↓ / j k / gg G, Space cycles the current line, x toggles rejected, n jumps to the next unreviewed line, and a bar in the tab strip shows the meter and counts for the file. In memory only; nothing written yet. Usable the day it ships.
- [x] review/reviewModel.ts, pure and unit-tested: lineKeyOf (+new / -old from DiffView's parsed rows), cycle (unreviewed → ok → rejected → unreviewed), toggleReject, progress (reviewed, changed, rejected; context rows never count), nextUnreviewed (wrapping), the document shape from docs/design/review-mode.md with a tolerant parse. [agent: claude]
- [x] review/reviewState.ts: module store (useSyncExternalStore, like gitConsoleState) for the per-window review-mode toggle (remembered) and the session's documents by key; theme/tokens.ts gains a review family (todo amber, ok = primary, rejected = diff.removed, cursor = grid selection) in light and dark, exported as --pg-review-*, tokens.test.ts extended. [agent: claude]
- [x] DiffView.tsx behind an optional review prop: a .diff-row-mark cell inside the sticky gutter (copy and text selection untouched; non-review layout byte-identical), row classes diff-row-review-todo|ok|rejected and diff-row-cursor, the non-virtual diff-lines box focusable, VirtualLines.tsx gains scrollToIndex; clicking the mark cycles and moves the cursor, clicking the text only moves the cursor (owner call 1). [agent: claude]
- [x] Hotkeys: a review scope in hotkeys/catalog.ts (ge: null) with Space, X, J/K and the arrows, G and gg, Home/End, N, Enter, Shift+Enter, /; dispatch.ts resolves it only when the focus is inside data-hotkey-surface="review" and not editing; Host.tsx walks the layer stack top to bottom so the review layer sits over browse (and later commit); hotkeys.test.ts covers uniqueness and the gating; docs/agents/memories/hotkeys.md records the fall-through. [agent: claude]
- [x] DiffTab.tsx: the review button left of the tree toggle (right: 10 + 30 + 6), the review layer while on, Enter / Shift+Enter over the file list; BottomPanel.tsx: the review bar in the tab strip (REVIEWING / REVIEW COMPLETE, meter, d / n lines · r rejected) for the current file. [agent: claude]
- [x] Symptom e2e review-mode.spec.ts (owner's words in the test name): Working directory row → Diff → review button → Space marks the first change (.diff-row-review-ok, bar "1 / 2 lines") → j, x → rejected → n lands on the remaining unreviewed line → Ctrl+Space still opens the commit dialog. Existing diff-text-selection and browse-reset specs stay green. [agent: claude]
- [ ] Owner ticks: on Ubuntu, review mode on the Working directory row's Diff tab feels like the prototype: keys, marks, bar. [agent: claude]
- [x] Deviations from the prototype worth knowing: Space or x with no cursor act on the first unreviewed line and place the cursor there (the symptom test needs 'toggle, Space, first change marked'); the bar shows only on the Diff tab; n crosses into files not yet opened by assuming they have work until v0.17.3 brings numstat; the tab strip is now a flex Box around Tabs + ReviewBar (visually identical). Gates on the merged tree: unit 324, e2e 136, native smoke. [agent: claude]

### v0.17.1 — The review survives — the file, the engine routes, the Review file pane (renumbered v0.19.0) (COMPLETE)
**Goal:** Renumbered to v0.19.0 on 2026-09-11 (v0.18.0 shipped in between; tags follow release order). Nothing was built under this number.

### v0.17.2 — Review mode in the commit window (renumbered v0.19.1) (COMPLETE)
**Goal:** Renumbered to v0.19.1 on 2026-09-11. Nothing was built under this number.

### v0.17.3 — Whole-review progress — file pills, the meter over every file, "Review complete" (renumbered v0.19.2) (COMPLETE)
**Goal:** Renumbered to v0.19.2 on 2026-09-11. Nothing was built under this number.

### v0.17.4 — Comments and the slash command line (renumbered v0.19.3) (COMPLETE)
**Goal:** Renumbered to v0.19.3 on 2026-09-11. Nothing was built under this number.

## v0.18 — Settings as a page, and the author identity discs — the owner's picks shipped before v0.19
> Owner (2026-09-11): "Let's rework the settings panel. It's ugly, poor layout. It's not adapted. Can you present me different visual prototypes to improve the settings? We could do something like VS Code." Three layouts were prototyped (docs/prototypes/settings-layouts.html: A VS Code page, B sidebar dialog, C toolbar tabs); the owner chose A: "Amazing. Let's go with A." Settings stops being a modal: it is a page in place of the graph and the bottom panel, with a table of contents, a search box over every setting, one long list of rows (title, description, control), a blue "changed" bar with Reset on rows that differ from the default, and no Save — changes apply as they are made, Git config through one-key writes. Planned before v0.17.1–v0.17.4 ship; those keep their plan numbers and follow in release order after this major.

### v0.18.0 — The settings page — layout A from the prototype
**Goal:** The gear (and Ctrl+,) opens Settings as a page where the graph and the bottom panel were, the repository tree included: a header with a back arrow, the title, a search box and "Changes apply as you make them"; a table of contents on the left (Appearance, Git identity, Tools, Behaviour with Confirmations and Background fetch, Diagnostics with the Recovery ladder, Updates); one scrolling list of sections on the right, each row a title, a one-line description and its control, native selects, the scope switch (This repository / All repositories) in the Git identity and Tools headers, "This app, every repository" on the others. Every change applies at once: app preferences through their stores, Git config through one-key PUT /config writes (text fields on blur or Enter, selects on change) with an inline Saved / error; a row whose value differs from the default carries the blue bar, a "changed" tag and a Reset (Unset for Git keys). Typing in the search hides every row that does not match its title, description or keywords, hides empty sections and their contents entries, and shows the count. Escape clears the search, then closes the page; Back, the gear and Escape return to Browse and focus the grid. The dialog, its Save and Cancel, and the v0.13.18 drafts are gone.
- [x] SettingsView page in App.tsx in place of the tree, graph and panel (over the file-history view when both are open), owned by a settingsOpen state next to fileHistory, not a dialog kind: the gear and Ctrl+, open it, the gear again / Back / Escape close it and focus the grid; dialog.kind "settings" and SettingsDialog.tsx removed, AppDialogs no longer mounts it
- [x] components/settings/settingsCatalog.ts: the sections and rows as data (id, title, description, keywords, anchor) feeding the rows, the table of contents and the search; SettingRow (title, description, control slot, changed bar + "changed" tag + Reset/Unset from a `changed` / `onReset` pair) and SettingsSection (title, scope switch or "This app" pill, hint, Saved / error line)
- [x] Appearance and Behaviour apply on change: theme, command bar, zoom, the five confirmations, background fetch, merge and rebase defaults write their stores immediately (no draft), native selects, Reset restores the default, existing test ids kept (settings-appearance, settings-bar-layout, settings-autofetch, settings-confirm-*, settings-default-*)
- [x] Git identity and Tools apply on change: useGitConfig(scope) with a cancelled read per scope, one-key patches through PUT /config (null = leave, "" = unset) in a serialized latest-wins queue, text fields commit on blur and Enter, selects on change, inline Saved / error in the section header, Unset on a key set at this scope; the custom-command tool path and the one-click VS Code button kept
- [x] Search: the box filters rows by title, description and keywords, hides sections with no match and their contents entries, shows "n settings", shows "No setting matches" for none; Escape in the box clears it, Escape again closes the page; the table of contents scrolls to a section or anchor on click and highlights the section in view
- [x] Diagnostics (Open app log, Open developer tools, Open logs folder, the recovery ladder) and Updates (version, Check for updates, download, Open app location) become their own sections, behaviour and test ids unchanged; Open app log closes the page and opens the console's app tab
- [x] Tests: settings.spec, shell.spec, shell-zoom.spec, updates.spec, app-log.spec, developer-tools.spec rewritten for the page (no Save / Cancel, no role=dialog); a symptom e2e settings-page.spec.ts in the owner's words — the gear opens a page with a search box, typing "fetch" leaves one setting, changing it applies without Save and survives a reload, Reset puts it back, Escape brings the graph back; unit tests for the search match and the changed-from-default rules; the visual baseline dialog-settings.png replaced by settings-page.png (light) and the @dialogs visual passes
- [x] docs/agents/memories/settings.md (the page, the catalog, immediate apply, the PUT /config one-key semantics, what Reset means per scope), the settings line in git-extensions-map.md, hotkeys.md if Escape handling changed; deviations from the prototype noted here
- [ ] Owner tick: on Ubuntu, the settings page feels like prototype A — the gear opens it in place, search finds a setting, a change applies at once with the blue bar and Reset, Escape brings the graph back
- [x] Deviations from the prototype (2026-09-11): the Tools scope switch has its own test ids (settings-tools-scope*) though it shares the Identity scope state; the Updates description avoids the word "fetch" so a search for it finds one row; "Custom command…" writes nothing until a name is committed (immediate apply must not write a placeholder into git config); "Open app location" is disabled outside the desktop app rather than absent; a Git key set at this scope is tagged "set here" / Unset, app rows "changed" / Reset; a new search scrolls the list to the top; the rail gear is not tinted while the page is open; App.tsx's splitter box became components/PanelSplitter.tsx (shared with the file-history view) to stay under the 400-line cap. Gates on the merge (eb8e4e152 + 1cb846f37): tsc and lint clean, unit 336/336, full e2e 141/141 on a private harness (Vite 1447, engine 7757), @dialogs visual green twice

### v0.18.1 — Author identity in the graph — initials discs, a ring on the selected author (prototype A)
**Goal:** Owner (2026-09-11): "design a better author highlighting for the commits of the selected author" — three prototypes (docs/prototypes/author-highlight.html), "A is perfect". Every row of the revision grid (Browse and file history) shows an 18 px initials disc before the author name, coloured from the ref-badge palette by a stable hash of the author, so runs of one author read even with nothing selected. Selecting a commit puts a primary ring on that author's discs and bolds the name on every loaded row by the same author; the graph options pill gains an Author group — the name, "n of N" over the loaded rows, and a Mark toggle (remembered) that removes the ring and the bold but keeps the discs. The row background is never touched, the text colour of other rows is never touched (the v0.12.3 rule); every class toggled at runtime sets its colour explicitly (webkitgtk-css.md). Appearance gains "Author discs" (on by default) for the old look.
- [x] graph/authorIdentity.ts (+test): initials(name) — first letters of the first and last word, uppercase, one word → its first two letters, an email or a [bot] name handled sensibly — and paletteOf(name) — a stable string hash onto the six ref-badge pairs (local, remote, tag, stash, head, extra) exported as --pg-ref-*-bg/-fg; memoised per name
- [x] RevisionGrid.tsx / app.css: span.author-disc before the name in the Author cell (18 px, 9.5 px bold initials, the pair's bg/fg, user-select none), the Author column default width 128 → 154 and the narrow-window width 104 → 128 (gridColumns.ts, the <1200 px rule in app.css); rows whose author equals the selected row's author get class author-same → ring (box-shadow: 0 0 0 2px surface, 0 0 0 3.5px primary) and the name in text colour at weight 600; .author-same .author sets color explicitly; the selected row keeps its own colours; the file-history grid gets the same for free
- [x] graph/graphOptions.ts: authorMark: boolean (default true, remembered under pg.graph, parse tolerant); GraphOptionsBar: an Author group after a divider — person icon, the selected row's author, "n of N" counted over the loaded rows, Mark toggle (data-testid graph-author-mark); with Mark off no row carries author-same; theme/behaviour or appearance: "Author discs" setting (settings catalog row appearance.authorDiscs, default on) hides the discs and the ring when off
- [x] Symptom e2e author-identity.spec.ts in the owner's words: every row shows a disc with the author's initials; selecting a commit rings the discs of that author's other commits and the pill says "<author> · n of N"; the selected row stays visibly selected (background) while a same-author row does not; Mark off removes the rings and keeps the discs; the Appearance setting off removes the discs; shell.spec "selected rows stay distinct from same-author rows" stays green; visual baselines regenerated (shell-light/dark, grid-*, bottom-*) and looked at
- [x] docs/agents/memories/webkitgtk-css.md: the "do not reintroduce" section rewritten — what came back (discs + ring, colour-only, opt-out), why it does not repeat the three reports; docs/agents/memories/git-extensions-map.md line (GE HighlightAuthoredRevisions → PowerGit author discs); deviations noted here
- [ ] Owner tick: on Ubuntu, the discs and the ring feel like prototype A; selecting a commit never hides it, and rows never vanish
- [x] Deviations (2026-09-11): the Mark rule is `.grid-row.author-same .author` (the theme's baseline weight on `.grid-row .author` tied and won otherwise) and the selected row keeps the marked weight too (looked at: it was the one thin name in a bold column); `countAuthor` lives in graph/authorIdentity.ts (react-refresh forbids a function export beside the bar); RefChips.tsx lifted out of RevisionGrid.tsx unchanged for the 400-line cap; the settings checkbox reads "Show author discs"; the GE map describes HighlightAuthoredRevisions as the reference has it (a brush by email). Visual baselines: the worker's set carried its worktree's repo name, so all nine were regenerated on the main tree on a clean tree (PNGs hidden with assume-unchanged during the run, as the suite screenshots the repo it runs in). Gates on the merge (1274a4d1e + a275619 + 7f0c6a420): tsc and lint clean, unit 344/344, full e2e 146/146 on the private harness, visual 6/6 twice

### v0.18.2 — Polish — one voice, quiet marks, the small things a design pass sees
**Goal:** Owner (2026-09-12): "a quick v0.18.2 if we have stuff to fix; a design pass for polishing our current UI would be welcome." A survey of every surface in both themes (Commit / Diff / File Tree tabs, the commit window, the row menu, the Pull preview, the settings page, review mode) found nothing broken and a handful of things that read as unfinished: the review marks and bar from the second look, menus that mix Git Extensions' Title Case with the app's sentence case, the title strip saying "PowerGit PowerGit", hotkey letters typeset as stray capitals, a "File Tree" tab beside "Commit" and "Diff", empty states that sit top-left in body text while the Diff tab centres its own, a settings zoom stepper wider than the prototype's, and a gear that does not know the page is open. Each is small and visible; none touches behaviour.
- [x] Review look pass (from the 2026-09-11 second look, moved here from v0.19.0): the ok mark becomes an outlined ring in the ok colour with the check in the same colour (no fill) so a reviewed file is quiet and the loud colour stays with unreviewed (amber) and rejected (filled red); the review bar loses its pill background and its capitals — "Reviewing" / "Review complete" at weight 600 in the amber / blue it already uses, beside the tabs; meter and counts unchanged; review-mode.spec label assertions and docs/agents/memories/diff-review-view.md updated
- [x] Menus and dialogs in one voice: sentence case and the … character for the labels that still carry Git Extensions' Title Case — revisionMenuModel.ts (Checkout branch…, Rebase current branch onto here…, Reset current branch to here…, Create branch here…, Create tag here…, Cherry-pick here…, Revert commit…), RefContextMenu.tsx (Checkout branch / tag, Delete branch…, Delete tag…, Configure remote…, Open submodule), CheckoutBranchDialog's title; GE ids and test ids unchanged; specs that match a label follow; docs/agents/memories/git-extensions-map.md notes that parity is functional, the voice is the app's
- [x] TitleStrip.tsx: the repository name reads as a location, not a second wordmark — a 1 px hairline (divider colour, 14 px tall) between the wordmark and the name, the name in text.secondary at the body weight; the Tauri drag region kept
- [x] Hotkey letters as keys: the S / U after Stage and Unstage in the commit window (and any other inline shortcut typeset as plain text) render as a kbd chip — 10.5 px mono, 1 px divider border, 3 px radius, 0 4px padding, text.secondary — through one small Kbd component; menu shortcuts (right-aligned) unchanged
- [x] Bottom panel tab "File Tree" → "File tree"; the BlobPane and the commit window's diff column use the shared EmptyState for "Select a file in the tree." and "Select a file to see its diff." (centred, secondary) like the Diff tab already does; texts unchanged so the specs still match
- [x] Settings page: the zoom stepper as in the prototype (28 px square outlined − / + with the percentage between, Reset as a text button); the rail gear (NavRail and CommandRail settings-button) shows the selected tint and aria-pressed while the page is open
- [x] Gates and a look: tsc, lint, unit; the e2e specs that touch menus, the commit window, review mode, settings and the bottom tabs; every changed surface captured light and dark and looked at; visual baselines regenerated where the change is the intended one
- [ ] Owner tick: on Ubuntu, nothing in this list looks unfinished any more, and nothing else changed
- [x] Deviations and gates (2026-09-12): the BlobPane empty state sits at the top of the pane, centred, like the Diff tab's; the rails had no active-item tint to copy, so the open gear uses action.selected + primary; a key chip greys with its disabled button (found by the look). Observed, not touched: after the File tree tab and a new commit, the Diff tab shows a blank pane instead of its empty state until a file is clicked (DiffPane renders null for the file-set / no-diff case) — backlog. Gates on the merge (c0ec34460): tsc and lint clean, unit 344/344, full e2e 146/146 on the private harness. The visual suite screenshots the repository it lives in, so its @grid baseline is stale one commit after it is taken; baselines are re-taken as the last commit before the tag and are green at that commit only

### v0.18.3 — Every ref on its row, and in the Commit tab — the chip budget, +n, the glyphs (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-15): "All branches should be displayed. If there are too many, we need a way to either show a popup to view them all, or expand vertically the column. When I click on a commit and view the Commit summary in the bottom panel, I should see the active branches / tags on this commit, with the icon to differentiate the local and remote. When my head is on a commit that has both local and remote branches, I shall see all these on the commit." Prototyped in docs/prototypes/branch-visibility.html (tabs 1 and 2, also published as an artifact); the owner picks A or B on each tab before implementation. Today a row shows at most three chips and clips the group at 46 % of the column, so the HEAD row loses its remote and its tag. The fixed cap goes: a row shows every chip that fits in 60 % of the message column (names measured once, cached), in the order HEAD · the checked-out branch · its remotes · every other local branch followed by its own remotes · the remotes with no local · tags, so a branch and its remote always sit together; the local chip gets the fork glyph next to today's cloud and tag; what does not fit becomes a real +n chip and the group's tooltip names every ref. Tab 1 A (recommended): +n opens a list anchored to the chip — every ref on the commit grouped Local / Remote / Tags, "checked out" on the current branch, click jumps to the tip, right-click opens the ref menu; rows stay 28 px. Tab 1 B: +n grows that one row, chips wrap, the message moves under them, the lanes stretch through — a variable-height grid (virtualizer + draw.ts), about twice the work. Tab 2: the Commit tab gains a Refs line with every ref, nothing folded, each with its glyph — A chips one size up under the subject (recommended), B a "Branches: … · Remote: … · Tags: …" text line in the metadata; the engine already sends the full list (%D), nothing new travels.
- [x] Owner pick (2026-09-15) from docs/prototypes/branch-visibility.html: tab 1 **B** — the row grows ("For 1, we'll go with option B"); tab 2 **A** — chips under the subject. Recorded; implementation follows B / A
- [x] components/refChipsModel.ts (+ unit test), pure: orderRefs (HEAD · current · its remotes · other locals each with their remotes · orphan remotes · tags) and foldRefs(refs, budgetPx) with per-name widths from a cached canvas measureText; RefChips.tsx uses it with the budget RevisionGrid passes (60 % of bodyWidth − graph − author − date − sha; the file history's grid the same), the fork glyph (CallSplit) on local chips, the group title and every chip title carrying the full names, +n as a real element (data-testid ref-more, title = the folded names); app.css drops the 46 % clip on .msg-refs
- [x] +n grows the row (tab 1 B, the owner's pick): the expanded row (one at a time, SHA-keyed) wraps every chip on as many lines as it needs, the message moves under them, the − chip / Esc / another +n folds it; the virtualizer measures rows (measureElement) and draw.ts takes the row tops and heights from the virtual items instead of i × ROW_HEIGHT — node centre at the row's middle, the bezier geometry per neighbour height so the lanes stay continuous through a taller row; the selection band, hover band, Shift+wheel scroll and the canvas height follow; the layout worker and the lane model are untouched; the @grid visual subset re-proves the drawing at 28 px
- [x] Commit tab (tab 2): BottomPanel threads tagNames / remoteNames to CommitDetailView, which renders the Refs line under the subject and body with RefChips fold-free and one size up (A) or the labelled text line with glyphs (B); click selects the tip (onSelectTarget), right-click the ref menu; the file history's Commit tab gets it for free; the line is absent on a commit with no refs and on the pending rows
- [x] Symptom e2e ref-chips.spec.ts in the owner's words, fixture: HEAD carrying a local branch, its remote-tracking branch and a tag, plus a commit with eight refs — "when my head is on a commit that has both local and remote branches, I shall see all these on the commit": the HEAD row shows HEAD, the local chip and the remote chip with the cloud; the eight-ref commit shows +n and the list (or the expanded row) shows all eight; "I should see the active branches / tags on this commit, with the icon to differentiate the local and remote": the Commit tab lists every ref, the local with the fork, the remote with the cloud, the tag with the tag glyph
- [x] docs/agents/memories/ref-chips.md (the budget rule, the pairing order, why not a count, where the width comes from), the git-extensions-map.md line (GE draws every ref inline; PowerGit folds by width); visual baselines @grid and @bottom regenerated and looked at
- [x] Owner tick: on Ubuntu, the HEAD row shows the branch and its remote at an ordinary window width, +n shows the rest, and the Commit tab tells local from remote at a glance
- [x] Deviations and gates (2026-09-15, merge 312b120f6): the empty .msg-refs span stays on ref-less rows (its gap aligns every row's message text); chips in an expanded row keep their natural height and the text line is pinned to 18 px so the row height is an integer; the budget is undefined (no folding) for the one frame before the grid's ResizeObserver reports a width; Escape folds only while a row is expanded and then stops, otherwise it passes on; RevisionRow.tsx and gridGeometry.ts split out of RevisionGrid.tsx and CommitInfo moved next to CommitDetailView for the 400-line cap; folding a row scrolled out of the overscan resets its cached size (54de13dc0). Gates on the worker's private harness (1441/7741): tsc, lint, prettier clean; unit 349/349; ref-chips.spec 3/3; the impacted set (selected-row-graph, branch-highlight, grid-columns, author-identity, pending-rows, context-menu, shell, file-history, bottom-split) 46/46; re-run on the merged tree with the two other iterations 34/34 (see v0.18.4); @grid and @bottom baselines regenerated on the main tree and looked at (a7f127fc2)

### v0.18.4 — Highlight ancestry (until refresh) — any commit as the root of the graph's highlight (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-15): "I want to be able to right click on a commit and hit Highlight ancestry and then temporarily all the ancestry is highlighted like we do for the current branch. This is temporary, and user should exit this temporary to return to the normal highlighting where only the current branch is highlighted." Git Extensions has this as "Highlight selected branch (until refresh)" (RevisionGridMenuCommands, Command.ToggleHighlightSelectedBranch, Ctrl+Shift+B, Alt+click). Prototype: docs/prototypes/branch-visibility.html tab 3 (one design, no A/B). The row menu gets "Highlight ancestry (until refresh)" after the Compare group, with Ctrl+Shift+B on the selected row and Alt+click on a row. The graph then treats that commit as the root: markAncestry(rows, root) instead of HEAD, so its ancestors keep their colour and ring and everything else goes non-relative grey exactly as the checked-out branch's highlight does; the root wears HEAD's 2 px outline, HEAD keeps its own; scope (all / first parent), Ring and Dim apply unchanged. The options pill is the mode's home: amber border and icon, pinned open while active, "Ancestry of <sha> <subject>" in place of "Checked-out branch", an Exit button with Esc. It ends on Exit, Esc with the grid focused, another Highlight (moves the root), a refresh that drops the root row, or a repository switch; it is a look, not a setting, and is never persisted. Text cells are never dimmed (the v0.14.0 rule).
- [x] graph/ancestry.ts: markAncestry(rows, rootId?) — HEAD when absent, null when the root is not among the loaded rows (the renderer then changes nothing); Ancestry carries rootId so draw.ts gives the root the 2 px outline; the pending rows are marked only for a HEAD root; unit tests: a side-branch root greys the HEAD-only commits, first-parent scope from the root, root not loaded → null
- [x] RevisionGrid / useHistory: highlightRoot (SHA-keyed, next to the selection, one per history so the file-history grid never inherits the main grid's); the ancestry memo recomputes only when the root or the rows change; Esc with the grid focused exits (before the floating bar's own Esc); Alt+click on a row sets the root; Ctrl+Shift+B in the hotkey catalog (hotkeys/) on the selected row toggles it (the same row again exits)
- [x] revisionMenuModel.ts (+ unit test): "Highlight ancestry (until refresh)" with its shortcut after the Compare group, hidden on artificial rows; RevisionContextMenu dispatches it (ctx-highlight-ancestry) through GridMenus so the main grid and the file history share it
- [x] GraphOptionsBar: the temporary state — amber border and route icon (the review todo amber, --pg-ref-todo*), pinned open while a root is set, "Ancestry of" + the root's short SHA and subject (ellipsis at 150 px) instead of "Checked-out branch", Exit button (data-testid graph-ancestry-exit) with an Esc kbd; scope / Ring / Dim and the Author group unchanged; the pill collapses again on Exit
- [x] Symptom e2e highlight-ancestry.spec.ts in the owner's words on a fixture with a merged side branch and commits after the merge: "right click on a commit and hit Highlight ancestry, and then temporarily all the ancestry is highlighted like we do for the current branch" — right-click the side branch's tip → Highlight ancestry → sample the canvas (the selected-row-graph.spec pattern): the commits after the merge are painted non-relative grey, the side branch's own commits keep their lane colour, the pill reads "Ancestry of <sha>"; "user should exit this temporary to return to the normal highlighting": Exit (and, separately, Esc) brings HEAD's highlight back and the pill collapses; a refresh that keeps the root keeps the highlight
- [x] docs/agents/memories/visual-direction.md — "The graph's second signal" paragraph gains the temporary root and the amber pill; git-extensions-map.md (GE HighlightSelectedBranch → PowerGit Highlight ancestry); hotkeys.md (Ctrl+Shift+B, Alt+click); the @grid visual baseline with a root set, looked at
- [x] Owner tick: on Ubuntu, right-click → Highlight ancestry reads like the checked-out branch's highlight moved to that commit, the amber pill says so, and Exit / Esc puts it back
- [x] Deviations and gates (2026-09-15, merge b10fefa7d): the amber comes from the review tokens (--pg-review-todo, -stripe, -bg), no new token; the menu icon map lives in NodeMenu (MenuIcon union), not RevisionContextMenu; Ancestry is { rootId, temporary, marks } (headId dropped, only a test read it); Ctrl+Shift+B does nothing in the file history (GE's FormFileHistory has no such item; Alt+click and Esc work there) and App returns false while it is open so the key falls through; a temporary root survives a ref-filter reload (resetHistory clears it only when it drops the selection; the grid's root-clearing effect ignores the empty-rows moment) — merge-time decision; found on the way: BottomPanel stamped the first-diff key with the options current when /changes landed, so a context change during the round trip was never fetched (1dd311525, floating-bars.spec caught it on the slower private engine); the pill wrapped its buttons at 1280 px with the rail and the tree open — nowrap, a max width, the subject gives way first (a7f127fc2). Gates on the worker's harness (1443/7743): tsc, lint, prettier clean; unit 353/354 (the 10k-layout 400 ms budget under a shared machine, green alone and 369/369 on the merged tree); highlight-ancestry.spec 4/4; branch-highlight, context-menu, floating-bars, selected-row-graph, file-history-context-menu, ref-chips, pending-rows, shell 37/37. Merged tree, the three iterations together: highlight-ancestry, ref-chips, graph-branch-filter, branch-highlight, selected-row-graph, floating-bars, context-menu, pending-rows, grid-columns, author-identity, file-history-context-menu, live-refresh-scope 34/34; after the pill fix highlight-ancestry + floating-bars + author-identity 11/11; smoke: Vite + engine, the three features driven in one page load, zero console or page errors, one capture looked at

### v0.18.5 — Choose the branches the graph shows — a ref filter from the Repository tree (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-15): "I need to be able to select which branch I see in the graph. Like a dynamic filtering. It can be a mode, that when entered, I can activate branches on the left side explorer and only these are visible." Git Extensions' "Show filtered branches" (FilterInfo: explicit revs on git log instead of --branches --remotes --tags). Prototype: docs/prototypes/branch-visibility.html tab 4; the owner picks A or B before implementation. Engine: GET /revisions?ref=refs/heads/x&ref=refs/remotes/origin/y… — full ref names validated against the ref tree (heads, remotes, tags only; no globs, no options, an unknown name is 400), handed to git log as revs on stdin (--stdin, so a thousand refs never hit the 32 K argv limit that killed explicit tips in v0.4); HEAD and refs/stash stay; same DTO, --date-order, paging and decorations. Frontend: RevisionFilter gains refs, the main grid's useHistory takes the filter like the file history takes its path, so a change is a reload with the layout worker reset and the selection kept by SHA when its commit is still listed; the checked-out branch is always in (the pending rows sit on HEAD, the highlight is HEAD's); paging, jump-to-ref and the highlight work unchanged; the set is remembered per repository (pg.graphRefs.<id>). Tab 4 A (recommended): the funnel in the Repository header enters filter mode, tinted while on — every branch, remote branch and tag gets a checkbox, folders and remotes a tri-state one, the checked-out branch ticked and locked; entering starts from the checked-out branch alone; a strip under the search says "Graph: n of N refs" with Show all (tick everything, stay) and Exit (every ref, checkboxes gone); the Message header carries the count with an × that leaves the mode. Tab 4 B: no mode — "Show only this in the graph" in the tree's ref menu, then an eye on every ref (open = in the graph, closed and greyed = not; hover-only "Hide from the graph" without a filter), the strip with Show all, the same header chip.
- [x] Owner pick (2026-09-15) from docs/prototypes/branch-visibility.html tab 4: **A** — filter mode with checkboxes ("For 4 option A"). Recorded; implementation follows A. Tab 3 (v0.18.4) confirmed as designed ("For 3 ok")
- [x] Engine: RevisionFilter gains Refs (IReadOnlyList<string>); ListRevisions validates each against the ref tree's full names (refs/heads/, refs/remotes/, refs/tags/ only; anything else → 400) and hands them to git log as revs on stdin (--stdin; GitProcess gains a stdin payload, drained with the same timeout and kill) instead of --branches --remotes --tags; HEAD and refs/stash stay; Program.cs reads repeated ref= query values; QueryTests on a fixture with two diverged branches: ref=[a] lists a's history plus HEAD's and nothing of b; an unknown ref is 400; 1,000 refs succeed; the unfiltered request is byte-identical to before; SRS-ENG-0xx in docs/srs/SRS-git-engine.md
- [x] Frontend engine/types.ts + client.ts: RevisionFilter.refs?: string[] (filterParams repeats ref=, sorted so the cache key is stable), unit test; graph/graphRefs.ts store (per repo id under pg.graphRefs.<id>, parse tolerant, useSyncExternalStore) with the mode flag and the set; App memoises the filter for the main useHistory; a change reloads (resetHistory + reload), the selection stays by SHA when its commit is still listed else falls to HEAD; jumpToRef keeps loading pages under the filter as before
- [x] RepoTree (tab 4 A): the funnel IconButton in the header (data-testid tree-filter-mode, aria-pressed), Item gains checked / indeterminate / locked / onCheck and RepoTreeRows draws the checkbox before the icon on leaves and the tri-state on folders and remotes (a group toggles its subtree), the checked-out branch ticked and locked with a title saying why; the strip (data-testid tree-filter-strip) "Graph: n of N refs · Show all · Exit"; the filter box still searches every ref and shows the checkboxes on the flat matches. (Tab 4 B instead: "Show only this in the graph" / "Show in the graph" / "Hide from the graph" in RefContextMenu's tree variant, the eye button per leaf and per group, the strip with Show all)
- [x] Grid header: the count chip (data-testid graph-filter-chip) right-aligned in the Message header with an × that leaves the mode (A) or shows everything (B); absent without a filter; the file history view keeps its own path filter and ignores the ref set
- [x] Symptom e2e graph-branch-filter.spec.ts in the owner's words, fixture: master checked out plus two diverged branches with distinct subjects — "I can activate branches on the left side explorer and only these are visible": enter the mode → only master's commits are listed and the strip reads "Graph: 1 of 3 refs"; tick branch a → its commits appear, b's do not; untick a → gone; master cannot be unticked; the header chip count matches the strip; Exit → every commit is back; reload the page → the choice is remembered; the selected commit stays selected across a tick that keeps it
- [x] docs/agents/memories/graph-ref-filter.md (the engine contract, why explicit revs on stdin and not --glob, what is remembered and where, the always-HEAD rule); git-extensions-map.md (FilterInfo ShowFilteredBranches / ShowCurrentBranchOnly → PowerGit filter mode); docs/srs/SRS-left-panel.md for the mode; the @grid visual with a filter on, looked at
- [x] Owner tick: on Ubuntu, entering the mode and ticking two branches leaves exactly their history in the graph, the count is right in both places, and reopening the repository brings the same view back
- [x] Deviations and gates (2026-09-15, merge 36dbd2cec): the checked-out branch is never named in the request — the engine always keeps HEAD on the command line, so the set sent is the ticks only (naming it from RepoInfo.branch is stale after a checkout and invalid when detached); an explicit empty set is ?ref= → HEAD alone, so "mode on, nothing ticked" never falls back to every ref; RunTimedWithStdin instead of a RunTimed overload (a string before params string[] would win every existing call); stdin is written while the readers already drain; RepoTree reads the store through useGraphRefs(repoId) (prop repoId), header/strip in RepoTreeHeader.tsx and the tick rules in repoTreeChecks.ts for the 400-line cap; App.tsx's diagnostic-snapshot block became hooks/useDiagnosticSnapshot.ts at merge time for the same cap; the count "n of N" counts the current branch once even when also ticked. Gates on the worker's harness (1442/7742): engine QueryTests|ApiTests|GitProcess 47/47 (13 new: two-branch fixture, tag, seven rejected names incl. -c/--all/short/HEAD/a..b, 1,000 refs, ref+path, command-log invariants, route 400, runner stdin); tsc, lint, prettier clean; unit 358/359 (the 10k-layout budget under load; 364/364 on the merged tree); graph-branch-filter.spec 1/1; shell, file-history, live-refresh-scope, context-menu, branch-highlight, recents, session-states 42/42; merged tree cross-checks 31/31 and 34/34 (see v0.18.4); the funnel button changes the left panel on every screen, so @grid/@bottom baselines were regenerated (a7f127fc2) and looked at with the mode on

### v0.18.6 — Save a commit as a patch — from the row menu, git's own file name, native Save dialog (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-15): "Being able to export a patch from a commit. Probably from the right click menu." Git Extensions has Commands → Format patch… (FormFormatPatch: a revision range and an output directory, `git format-patch --find-renames --find-copies --break-rewrites -o <dir>`); PowerGit puts it where the owner reaches for it, the row menu, one commit at a time. "Save as patch…" after "Create archive…" opens the platform's Save dialog with git's own name for the file (`0001-<%f>.patch`, the sanitized subject `git log -1 --format=%f` uses) and writes the mailbox-format patch of that commit (`git format-patch -1 --stdout` with GE's three flags, binary diffs included so `git am` applies it whole); the status bar says "Saved 0001-….patch" with Show in folder. On the Working directory and Index rows the same entry saves the pending diff (`git diff --binary`, `git diff --cached --binary`) as `<repo>-worktree.patch` / `<repo>-index.patch`, disabled when there is nothing to save. The engine stays read-only: it streams the patch text (`GET /commits/{id}/patch`, `GET /worktree/patch?scope=`), the shell writes the file where the user pointed (a small `write_text_file` Tauri command next to `diagnostic_snapshot`; no fs plugin); in the browser (dev, demo) the same request downloads through the browser with the same file name. A range ("from the other selected commit to this one", GE's dialog) and a native Save path for "Create archive…" are follow-ups in the backlog, not here.
- [x] Engine: GET /repos/{id}/commits/{sha}/patch streams `git format-patch -1 --stdout --find-renames --find-copies --break-rewrites <sha>` as text/x-patch with Content-Disposition attachment; filename=0001-<%f>.patch (GitHost.OpenPatch on the OpenArchive streaming pattern, so a large patch never meets the stdout cap; the sha verified with rev-parse ^{commit}; git's exit code and stderr surface as 400); GET /repos/{id}/worktree/patch?scope=worktree|index streams `git diff --binary` / `git diff --cached --binary` as <repo>-worktree.patch / <repo>-index.patch; xunit on a fixture with a rename, a binary file and a two-line body: the commit patch has From / Subject: [PATCH] / the body / the rename header and `git am` applies it whole in a clone at the parent (same tree, same subject, same author); the worktree patch applies with `git apply`; a bad sha and an unknown scope are 400; SRS-ENG row in docs/srs/SRS-git-engine.md
- [x] Shell: `write_text_file(path, contents)` Tauri command in src-tauri (fs::write, the parent directory must exist, the error message returned as the string the UI shows), registered next to diagnostic_snapshot; cargo check on Windows and in WSL (wsl-linux-shell-gate.md); the dialog plugin's `save` needs no new capability beyond what plugin-dialog already grants — verify in src-tauri/capabilities
- [x] Frontend: client.patchUrl(sha) / client.worktreePatchUrl(scope) (token in the query, like archiveUrl) and client.patch(sha) / client.worktreePatch(scope) returning the text; useOperationActions.savePatch(row): in the shell, fetch the text, `save({ defaultPath: <the name from Content-Disposition>, filters: [{ name: "Patch", extensions: ["patch"] }] })` from @tauri-apps/plugin-dialog, `invoke("write_text_file")`, then a status-bar note "Saved <name>" with a Show in folder action (revealInFolder from diagnostics/snapshot.ts) that the next selection clears; cancelled dialog = nothing; in the browser openExternal(patchUrl) so the browser downloads the file under the same name; errors → setEngineError("Save as patch: …")
- [x] Menu: revisionMenuModel.ts (+ unit test) gains { id: "ctx-save-patch", label: "Save as patch…", icon: "patch" } right after Create archive… in the same group; on the artificial rows the label is "Save changes as patch…", disabled when the row's count is 0 (unstagedCount / stagedCount already reach the model); a `patch` MenuIcon (DifferenceOutlined or SaveAltOutlined) in NodeMenu; RevisionContextMenu dispatches to actions.savePatch(row); the file history's menu (FileHistoryContextMenu) gets the same entry for its commits (GE's FormFileHistory offers Format patch there too)
- [x] Symptom e2e save-patch.spec.ts in the owner's words ("export a patch from a commit … from the right click menu"), browser path: fixture with `base` then a commit `change` that edits a.txt and adds b.bin (binary); right-click `change` → Save as patch… → Playwright's download event: suggestedFilename() is 0001-change.patch, the text starts with `From <sha>`, has `Subject: [PATCH] change` and `GIT binary patch`; `git am` of the saved file into a fresh clone at `base` succeeds and the clone's HEAD subject is `change`; on the Working directory row (an edited file) the entry reads "Save changes as patch…" and the download is <repo>-worktree.patch that `git apply --check`s against `base`; a clean working directory disables the entry
- [x] docs/agents/memories/git-extensions-map.md (GE FormFormatPatch → PowerGit Save as patch…, one commit, mailbox format, GE's flags, binary on); docs/agents/memories/save-patch.md (why the engine streams and the shell writes, the %f file name, the browser fallback, what a range would need); backlog lines: a range from the other selected commit (GE's dialog), a native Save path for Create archive…
- [x] Owner tick: on Windows and Ubuntu, right-click a commit → Save as patch… opens the native Save dialog with 0001-<subject>.patch, the saved file applies with git am, and the status bar's Show in folder lands on it
- [x] Deviations and gates (2026-09-15, merge of wt/v18-patch): EngineAuth accepts ?token= on /archive and /patch (AcceptsQueryToken) — the plan said "like archiveUrl", but only /events honoured a query token, so the browser download of an archive had been a latent 401 since v0.15.0; fixed for both, tested. A merge commit is a 400 (format-patch prints nothing for a merge and the streamed exit code is not observed) rather than an empty file; an empty commit yields an empty file (--always needs git ≥ 2.36). The file name matches git byte for byte: 0001-<%f> chopped at 57 bytes + .patch, pinned against git 2.38. "Disabled when the count is 0" is exercised as the reachable case — the menu is open on the Working directory row when the tree goes clean under it; RevisionMenuInput.artificial is false | "worktree" | "index". File history: fh-save-patch beside the difftools, disabled on the pending rows (their patch is the Browse menu's); GE's Format patch… is on FormBrowse's Commands menu, not FormFileHistory — the GE map says so. A status-bar note slot did not exist: hooks/useStatusNote.ts ({ text, action? }, cleared by the next selection or after 10 s) through GitActionsDeps.notes. The shell path (native Save dialog → write_text_file → Saved … / Show in folder) is covered by the cargo unit test and the owner tick, not by an e2e. Gates on the worker's harness (1444/7744): engine PatchTests|QueryTests|ApiTests|Sequencer|CommandLog 95/95 (6 new); cargo check + the write_text_file test on Windows and in WSL; tsc, lint, prettier clean; unit 374/374; save-patch.spec 1/1; context-menu, file-history-context-menu, pending-rows 8/8; status-bar, git-ops-extra, shell 27/27; merged tree: tsc, lint, unit 374/374 (e2e cross-check together with v0.18.7/8 below)

### v0.18.7 — Recent repositories as tiles — the picker from prototype C (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-15): "can we run /frontend-design on the Recent Repositories overlay? Show me visual prototypes to improve that UI" — three pickers prototyped in docs/prototypes/recents.html (A quick-open list, B grouped by folder, C tiles) beside today's dialog (D); the owner chose C ("ok for C"). The v0.4.6 dialog (a centred modal, two columns of outlined cards, no search, no keys, no sign of the open repository, the full path on every card) becomes a flat picker: a search box with focus on open that narrows by name, path or branch (match highlighted), a hairline grid of tiles three across (two under 700 px), each tile one bold element — an initials disc (the name's words, camel-case counted, PowerGit → PG) coloured by a stable hash of the path onto the six badge pairs (the author-disc device, so two repositories called api never share a disc) — the name at 500 with the branch chip (fork glyph) and "open now" on the repository that is open, the path's tail in the code face with the shared root dimmed, a key hint 1–9 in the corner that gives way to the cross on hover. Keys: ↑ ↓ ← → move the cursor, Enter opens it, 1–9 open that tile, Esc clears the filter then closes, Delete forgets the cursor tile; the cross forgets with a five-second Undo in the footer (the DELETE is delayed, no engine change) instead of a confirm; the footer keeps the count, the key hints and Open folder…; empty states in the interface's voice ("No repositories yet — open a folder and it will be listed here next time", "Nothing matches 'x'"). The dialog keeps its props (recents, onPick, onForget) and test ids (recent-card is the tile, recent-forget the cross); no engine change.
- [x] components/recentsModel.ts (+ vitest), pure: repoInitials(name) (words, camel-case boundaries, hyphen/underscore/dot; one word → its first two letters; uppercase), repoPalette(root) (the stable hash from graph/authorIdentity.ts onto the six badge pairs — reuse its hash, do not duplicate it), sharedRoot(roots) (the path prefix every listed repository shares, on a separator boundary, empty below two entries), matchRecent(repo, query) (case-insensitive on name, root, branch) and the match range for the highlight
- [x] RecentsDialog.tsx rebuilt on prototype C: MUI Dialog (fullWidth, maxWidth 760, PaperProps top-anchored at 80 px, no Card) with the search row (InputBase, autoFocus, data-testid recents-filter, Esc clears then closes), the tile grid (CSS grid, 1 px gap on the divider colour so the hairlines come from the gap, three columns, two below 700 px), each tile data-testid recent-card with the disc (30 px, 11.5 px 700, the pair's bg/fg), the name (500) + RefChips-style branch chip (fork glyph; reuse RefChips with fold off or a one-chip helper) + "open now" when root equals the open repository (a new optional `currentRoot` prop from App), the path in MONO_FONT with the shared root in text.disabled and the last segment in text.secondary, ellipsis from the left (direction rtl + bdi, as the prototype), the key hint 1–9 (mono, text.disabled) that hides when the cross shows; cursor tile = the selection band colours (grid-sel bg, 2 px sel-border on the left); the cross data-testid recent-forget visible on hover and on the cursor tile; the footer (sunken bg): "n repositories" or "n of N", the kbd hints (Kbd component), Open folder… (onOpenFolder prop → openFolder); empty states as in the goal; every colour a token, both themes
- [x] Keys and Undo: the dialog owns the keys while open (the browse layer is silent under a dialog): ArrowUp/Down move by a row (three), ArrowLeft/Right by one when the caret is at the input's edge, Enter opens the cursor tile (onPick + close), 1–9 open that tile unless the filter has text, Delete with an empty filter forgets the cursor tile; forgetting hides the tile at once and shows "Removed <name> · Undo" in the footer for five seconds, the real onForget fires when the timer ends or the dialog closes, Undo cancels it (a small useDelayedForget hook or local state; closing the dialog flushes pending forgets so nothing is lost)
- [x] Symptom e2e recents.spec.ts extended in the owner's words ("improve the Recent repositories overlay"): the picker opens with the filter focused; each tile shows a disc with the repository's initials and the branch chip; typing part of a path leaves only the matching tiles and highlights the match; ArrowDown then Enter opens the second tile (the status bar's repository name changes); pressing 1 opens the first; the cross hides the tile and the footer offers Undo — clicking Undo brings it back and the engine's /repos/recents still lists it; letting the five seconds pass (fake timers or a short test override via a prop) removes it for good; recents-restart.spec.ts stays green (same test ids); the visual @dialogs subset gets a recents-picker baseline (light) looked at
- [x] docs/agents/memories/visual-direction.md: a short "Recent repositories" paragraph (the picker is a flat grid, the disc is the one bold element, no cards); docs/agents/memories/recents-startup.md unchanged unless the startup fetch moves; the prototype file stays as the reference; deviations noted here
- [x] Owner tick: on Windows and Ubuntu, the picker opens with the cursor in the search box, PowerGit is found by its PG disc before reading, typing narrows the grid, 1–9 and Enter open, the cross offers Undo
- [x] Deviations and gates (2026-09-15, merge of wt/v18-recents): App.tsx untouched — AppDialogs already has repo and openFolder, so currentRoot and onOpenFolder are wired there; "open now" sits after the path (on the name line it crushed the name beside the chip in a 250 px tile, as prototype C's markup has it); ArrowDown moves by a row of three; the five seconds in the spec are Playwright's fake clock, forgetDelayMs exists but nothing sets it; the branch chip is a one-chip helper, not RefChips (with no remote names known any slash would have got the cloud); MUI 9 dropped disableEscapeKeyDown — Esc is handled in the picker's onKeyDown; a focus landmine found and fixed: autoFocus inside a Dialog loses the focus in dev (StrictMode double-mounts MUI's FocusTrap), a mount effect refocuses the filter, recorded in docs/agents/memories/mui-dialog-focus.md; RecentTile.tsx split out for the 400-line cap. Gates on the worker's harness (1445/7745): tsc, lint, prettier clean; unit 377/377; recents, recents-restart, session-states, shell, focus-management, toolbar-chrome 39/39; merged tree with v0.18.6 and v0.18.8: unit 386/386, the impacted set (save-patch, recents, wrap-lines, recents-restart, context-menu, file-history-context-menu, status-bar, diff-view, floating-bars, review-mode, blob-highlight, pending-rows, shell, focus-management) 49/49; the recents-picker baseline seeded on the main tree and looked at — the first take caught MUI's backdrop mid-fade, so the visual test now waits for the backdrop's opacity before the screenshot

### v0.18.8 — Wrap lines — one switch in the floating options bar for every code view (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-15): "whenever we display code (e.g. Commit view or Diff view): can we have an option to activate line wrapping in the floating action menu?" Today every code surface is set to no-wrap on purpose (diff-view.spec: "diff view does not wrap", "blob view does not wrap long lines"; a horizontal scroll with a sticky gutter). That stays the default; a "Wrap lines" toggle joins Context and Ignore whitespace in the floating diff options pill — the Diff tab, the commit window's diff column and the file history's diff share the one pill component, so they get it at once — and the File tree's blob pane gets the same pill reduced to that one toggle. It is one remembered preference (pg.diffWrap, off by default), not a diff option: wrapping is presentation, it never re-requests a diff or touches the commit cache key. Wrapped, a long line breaks inside the text cell (pre-wrap, overflow-wrap anywhere) while the gutter with its line numbers and the +/− sign stays put on the left and the row grows; the virtualized paths (a diff over 200 lines, a blob over 2,000) measure their rows the way the revision grid does since v0.18.3, so scrolling, Home/End, review marks, line selection and copy keep working on rows of different heights. Not a Git Extensions item (its FileViewer has no wrap); the copy is "Wrap lines" in both pills.
- [x] components/codeWrap.ts (+ vitest for the parse): a useSyncExternalStore store like graph/graphOptions.ts — getCodeWrap / setCodeWrap / useCodeWrap, persisted under pg.diffWrap ("1" / "0", tolerant), default off; not part of DiffOptions, so commitCache keys and the engine requests are untouched
- [x] DiffOptionsBar: a "Wrap lines" toggle (ToggleButton like the graph pill's Ring / Dim, data-testid diff-wrap-toggle, aria-pressed) after Ignore whitespace, reading and writing the store; a `wrapOnly` prop renders the pill with the Tune icon and that toggle alone; BlobPane mounts <DiffOptionsBar wrapOnly /> at the bottom centre of its pane (same floatingBar behaviour); the commit window's and the file history's pills need no change beyond the shared component
- [x] Wrapping in the rows: DiffView's scroll container and the blob <pre> get data-wrap="true" when the store is on; app.css: under [data-wrap="true"] .diff-row is width 100 % (not max-content) and .diff-row-text is white-space: pre-wrap; overflow-wrap: anywhere; min-width 0, the gutter unchanged (sticky, flex-shrink 0); the blob's pre and Shiki's pre/code get the same pre-wrap under the attribute; the plain (<200 lines) diff path and the small blob need nothing else
- [x] VirtualLines: a `wrap` prop — rows keep estimateSize 18 but get ref={virtualizer.measureElement} (data-index is already there), height auto, whiteSpace pre-wrap, width 100 %, and the inner track drops width: max-content; the virtualizer's getItemKey by index; toggling wrap calls virtualizer.measure() so cached heights are dropped; keyboard scrolling amounts stay CODE_LINE_HEIGHT; scrollToIndex (review mode, go-to-line) still lands on the row; DiffView's line selection and copy (selectedWithin / the .diff-row-sign exclusion) and the review marks are untouched by the height change — verify with the existing review-mode and diff-text-selection specs
- [x] Symptom e2e wrap-lines.spec.ts in the owner's words: a fixture file with one 400-character line in a commit; Diff tab: the bar's Wrap lines toggle is off and the row is wider than the pane (scrollWidth > clientWidth); toggle on → the row is no wider than the pane, the long line's row is taller than 18 px, the gutter's line number is still at the left edge, no new /diff request was made (count network requests); the setting survives a reload and applies in the File tree's blob pane (its own pill shows the toggle on; the long line wraps) and in the commit window's diff; toggle off → no-wrap again; the existing diff-view.spec no-wrap assertions stay green with the default; a >200-line diff (virtualized) with wrap on scrolls to its last row with End and the review marks land on the right row
- [x] docs/agents/memories/diff-review-view.md or a new code-wrap.md: the store, why wrap is not a DiffOptions field, the measured-rows contract shared with the grid, the data-wrap attribute; git-extensions-map.md: FileViewer has no wrap, PowerGit adds one; visual @bottom baseline with wrap on for the Diff tab, looked at
- [x] Owner tick: on Windows and Ubuntu, Wrap lines in the Diff tab's pill wraps the long lines with the numbers staying put, the commit window and the file viewer follow, and it is still on after a restart
- [x] Deviations and gates (2026-09-15, merge of wt/v18-wrap): virtualizer.measure() on toggle is ref-guarded so the mount does not re-render; the parse also accepts "true"; a pre-existing quirk found and backlogged, not fixed: the engine passes git's stdout with its trailing newline, so every diff has an empty last row and the review layer's End lands one below the last code line (the spec steps up with k before marking). Gates on the worker's harness (1446/7746): tsc, lint, prettier clean; unit 373/373; wrap-lines.spec 2/2 (every code view, the virtualized diff, zero /diff or /changes requests on toggle, survives a reload); diff-view, floating-bars, review-mode, diff-text-selection 8/8; blob-highlight, large-content, commit-partial-diff, file-history 7/7; merged tree: unit 386/386 and the 49/49 impacted set listed under v0.18.7; the bottom-diff-wrap baseline seeded on the main tree and looked at (a long header and a long line wrap, the gutter numbers stay put); smoke on the merged tree: Vite + engine, the row menu's Save as patch…, the picker and the Diff tab's Wrap toggle reached with zero console or page errors
- [x] Released as v0.18.8 (2026-09-15, tag on febf4bad6 "test(visual): baselines at v0.18.8"): preflight engine 175/175, full e2e 159/159, resolution 20/20, all visual baselines re-taken at 0.18.8 and looked at; Windows zip + installer packaged locally and smoke-tested (sidecar /health 0.18.8, the shell renders); release.yml green on windows / linux / manifest — assets zip, setup.exe + .sig, AppImage + .sig, latest.json; notes published; showcase screenshots regenerated and the feature cards updated (4c6266004), pages.yml green, test:live green. Per the v0.18.0–0.18.2 precedent the six iterations stay open on their owner-tick tasks (AGENTS.md: owner reports are closed by the owner), so the release skill's "close the iteration" step was not applied

### v0.18.9 — The picker you can read, the author disc whole (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-16): "The new recent repo had good style, but the scale and size of the overlay is not good. We need to rework that. Should be centered, and larger so that we can actually read the stuff on the cards." and "in the main view, the author col has the author icons truncated on the left — we should probably move the col left boundary by a few pixels to ensure we don't cut our author icons." The v0.18.7 picker is a 760 px panel pinned 80 px from the top (the command-palette anchor of prototype C) with 13 px names and 11 px paths in tiles 250 px wide, so on the owner's screen the tiles are small and every name of any length ends in an ellipsis. It becomes a centred window sized to the screen — width min(1120 px, 100vw − 96 px), height min(720 px, 100vh − 96 px) — whose grid is as many tiles as fit at 340 px or more (auto-fill; still three across at 1120), each tile taller (96 px) with a 36 px disc, the name at 14 px and the path at 12 px; a name gives up the branch chip before it truncates; ↑ ↓ keep reading the column count from the CSS. The author cell: the initials disc sits flush on the cell's left edge with overflow hidden, so the 3.5 px ring of the selected author's discs (v0.18.1) is cut on the left, and at narrow widths the disc itself; the Author and Date cells get the 8 px left padding their headers already have and the default Author width grows 154 → 162 so a 20-character name still fits (author-highlight.spec).
- [x] Recents picker centred and sized to the screen: the paper loses its absolute 80 px top (MUI centres it), width min(1120px, 100vw − 96px), height min(720px, 100vh − 96px), both divided by the zoom as today; the grid is repeat(auto-fill, minmax(340px, 1fr)) so a tile is never narrower than 340 px and columnsOf keeps reading the count from the CSS; the search row 48 px with 15 px text, the footer 38 px; recents.spec asserts the paper's centre is within 2 px of the viewport's at 1280×720 and 1920×1080 and a tile is ≥ 340 px wide.
- [x] Tiles you can read: 96 px tall, 36 px disc at 13 px initials, the name at 14 px / 500 and the branch chip one size up (11 px), the path at 12 px, 16 px side padding; the name keeps at least 12 ch and the chip shrinks first (a 20-character name shows whole in a 340 px tile — recents.spec asserts no ellipsis on the name at 1280); the cross and the key hint keep their corner.
- [x] Author and Date cells line up under their headers: .author and .date get padding-left 8 px (the header cells' value), so the disc and its selected-author ring (3.5 px, v0.18.1) are whole at every width; DEFAULT_WIDTHS.author 154 → 162 (and the < 1200 px value 128 → 136) so a 20-character name still fits beside the disc; author-highlight.spec asserts the disc's left edge is ≥ 8 px inside the cell and the ring is not clipped on a marked row (bounding box of the disc minus 4 px inside the cell).
- [x] Limited test set by impact (recents dialog, grid cells): recents.spec, author-highlight.spec, ref-chips.spec, shell.spec, the @grid and recents visual baselines re-taken on the main tree; engine untouched.
- [x] Owner tick: on the owner's screen the picker opens centred with tiles whose names and paths read whole; the author discs and their rings are not cut on the left.
- [x] Owner report (2026-09-16): "When I performed a merge, the overlay bugged at 'Merging' — I had to close it. Then I had an error message at the top 'merging ‘branch’ history: fetch is aborted'. Merge worked in the end." Read of the code, confirmed by a test that reproduces the timeline (useOperationActions.test.ts "the owner's timeline"): useOperationActions.merge ran `engine.merge` then `refresh(FULL)` inside withBusy("Merging x") and MergeDialog's submit awaited all of it — on a repository whose first history page takes seconds the merge was long done while the dialog still said Merging…; meanwhile git's writes reached /events, the deferred echo refresh fired reloadHistory, whose abortInflight() killed the merge refresh's own page-0 request, and that refresh failed with the browser's AbortError text under the merge label. Fixed: (1) reloadHistory coalesces — a reload requested while one is in flight waits for it and runs once after it, never aborting a same-filter page 0 (abortInflight stays for resetHistory and a filter change; a superseded run says "history reload superseded" in the diagnostics ring) — useHistory.test.ts +4; (2) withBusy(label, fn, { refresh }) is two-phase: the engine answer resolves the dialog, the refresh runs on behind the top bar under the same label, a refresh failure reads "Refresh after merging x: …", and an operation confirmed during the previous one's refresh waits instead of being dropped — merge, checkout, rebase, reset, cherry-pick, revert, stash all use it; (3) merge.spec "merge on a slow history": /revisions held 3 s, the dialog is gone ≤ 1.5 s after the merge response, no banner ever matches /abort/i. Unit 394/394; e2e merge, rebase, rebase-interactive, git-ops-extra, browse-reset, context-menu, error-banner, toolbar-chrome, live-refresh-scope, shell 55/55. Owner tick: the merge dialog closes when git is done; no abort error after a merge.

### v0.18.10 — Performance audit — the graph on a 100k-commit repository, the app under thousands of pending changes (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-16): "We need to have a go at performance. We need to evaluate the performance of the UI when the user is interacting with the graph. Also I've had cases where when there's a lot of pending changes, the app becomes unusable. We can use big open source repos like kernel or vscode to test our app on large repos." v0.8.0 and v0.10.0 made the graph paged, laid out in a worker and virtualized, with a synthetic 50k-commit fixture and budgets (npm run test:perf) — but the budgets were set against generated history, nothing measures the interaction itself (scroll, hover, select, expand, the highlight, the ref filter) on a real repository, and nothing at all was ever measured with a working tree full of changes. This iteration measures first and fixes nothing: real repositories (flutter, 97k commits and 2,146 refs, already on the machine; vscode, 130k commits, cloned; the Linux kernel, 1.3M commits, blobless, if the first two do not already show the ceilings) and a generated working tree with 500, 2,000 and 10,000 pending changes, driven by a Playwright + CDP harness that records what the user feels — time to first rows, long tasks and dropped frames during a scroll, the latency from click to the Commit tab, from hover to the canvas, from a status refresh to the Working directory row — beside the engine's own timings. Suspects to confirm or clear, not conclusions: GetStatus runs status -uall plus ls-files -v over the whole tree on every refresh (GitHost.Queries.cs, GitHost.Files.cs); the pending file lists (CompactFileList, CommitFileLists, CommitFileTree, DiffTab) render every file as MUI rows with no virtualizer; the live-refresh feed re-runs status on every .git/index write; hover and selection re-render the grid rows and redraw the canvas; RefChips measures text per row. The output is a report with numbers, a ranked list of bottlenecks by what the user feels, and the fix iterations to schedule (v0.18.11+), for the owner to pick from.
- [x] Test beds: C:\dev\perf\vscode cloned (130k commits), C:\dev\flutter as is (97k commits, 2,146 refs), scripts/perf/make-pending.mjs generates a working tree with N pending changes (N modified tracked files, N/4 untracked, N/10 staged; idempotent, under the OS temp dir like make-heavy-repo.mjs) and prints its manifest; the Linux kernel (blobless clone) only if flutter and vscode do not already show the ceilings. Every engine started for the audit sets POWERGIT_DATA_DIR (private-engine-data-dir.md) so nothing lands in the owner's recents.
- [x] Harness: frontend/scripts/perf-audit.mjs (Playwright + CDP, the e2e-harness.ps1 engine on its own port) opens a repository and records per scenario — boot to first rows and to 1,000 rows; a 6 s wheel scroll (long tasks > 50 ms, frames > 32 ms, from the Performance domain trace); a hover sweep over 40 rows (input to canvas repaint); 20 selections (click to the Commit tab's subject); +n expand/fold; Highlight ancestry on and off; a ref filter with 5 and 500 refs; a refresh (F5) — as JSON plus a Markdown table; engine timings from the command log (GitHost.CommandLog.cs) per git call; heap size before and after each scenario. Runs on the PowerGit repository as the baseline first.
- [x] Pending changes: the same harness on the generated tree at N = 500, 2,000, 10,000 — time for the Working directory row to show its count, the Diff tab's file list and the first diff, the commit window's open and its file lists, staging one file and staging all, the live-refresh storm (touch 200 files while open: status requests in flight, main-thread long tasks, time to quiet); the engine's status cost broken down by git call (status -uall, ls-files -v, rev-parse, ahead/behind).
- [x] Report docs/perf/audit-2026-09-16.md: the numbers per scenario and repository (PowerGit, flutter, vscode, pending N), the bottlenecks ranked by what the user feels with the code path each sits in and the evidence (trace, timings), the suspects cleared, and the fix iterations proposed for v0.18.11+ with an estimate and a budget each; frontend/tests/perf gains the new scenarios behind test:perf (opt-in, budgets set from the measured baseline × 1.5 so a regression fails, no fix implied).
- [x] Owner report: the audit's one-page summary — the numbers that hurt, the ranked list, the proposed iterations — for the owner to pick the fixes to schedule first.

### v0.18.11 — The operation dialogs quote the graph — Create branch and its family, Checkout of a remote branch (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-16): "The overlay popup for 'Create branch' on the main graph is rather bad visually. If there are other overlays like that, we should also rework them" and "when I hit 'checkout branch' on a remote branch, it should offer me the possibility to create a new local branch for that remote branch or to reset the current local existing branch and move it to remote ref" — "visual prototypes first". Today every small git-operation dialog shares OpDialog (MUI's 20 px h6 title, stacked content, an action row) and puts the commit's whole subject in the title, so "Create branch at 4c62660 (site: showcase screenshots at v0.18.8; feature cards …)" wraps to four lines, the field is a lone floating-label input with no check-out-after-create (Git Extensions has it), no validation and no sign of what the graph will show; Reset, Rebase, Cherry-pick, Revert and Delete repeat the pattern, and Checkout lists every branch as radios and treats a remote branch like a local one (git checkout origin/x: a detached HEAD). Prototyped in docs/prototypes/op-dialogs.html (tab 1 Create branch A "quote the row" / B "at the commit" popover / C "in place" ghost chip; tab 2 Checkout with GE's three ways for a remote branch — create a tracking local branch, reset the existing local branch to the remote, detached — and the Local changes group; tab 3 the family in the chosen shell: Reset, Delete branch, Merge, Create tag); the owner picks before implementation and the tasks are written then. Design rule from the prototype: the dialog quotes the graph — the commit or ref it acts on is rendered as its row or chip is in the grid, on a band under a 15/600 title; labels above inputs; a live "appears as" chip for a ref name with git's check-ref-format reasons; destructive primaries turn red and say the consequence.
- [x] Owner picks (2026-09-16): Create branch "1A" (the modal that quotes the row); Checkout of a remote branch whose local exists defaults to Create with a suggested name, red only when commits become unreachable; Merge at 600 px so nothing wraps. The shell: OpDialog becomes the A shell — a 15/600 title with Enter/Esc kbd hints at its right, a sunken band that quotes the subject (QuotedRow: the commit as RevisionRow draws it — lane-coloured node, mono SHA, ref chips via RefChips, the subject on one line, the author disc, the date; QuotedRef: the chip and its tip row; two rows with "into" for merge), a body with labels ABOVE fields (never MUI's floating label; DialogContent overflow visible so nothing clips), option rows 13 px with the explanation after an em dash on the same line or a 12 px meta line under it, a footer with a note at the left and Cancel + the primary at the right; `danger` turns the primary red (the head-ref pair) and the label says the consequence; width per dialog (520 default, 600 Merge/Checkout) — no label, option or field is ever clipped or wrapped. OpDialog keeps its props and adds subject, width, note, primary {label, danger, disabled}; every existing testid stays.
- [x] Create branch / Create tag on the shell: the quoted commit row; "Branch name" with the live "appears as" chip (refChipsModel's glyph, red with git's check-ref-format reason: spaces, "..", a trailing "/", ".lock", "@{", control characters, or "already exists" against the ref tree — refName.ts + unit tests), the primary disabled until the name validates so Enter never sends a bad name; "Check out the new branch" (on by default, the primary reads "Create and check out"; engine: checkout after create through the existing routes) and "Orphan — start a branch with no history" (engine: `git checkout --orphan`, GE's chkCreateOrphan; the clear-working-directory option stays out); Create tag adds "Message — a message makes an annotated tag" (engine: `tag -a -m`); the footer note counts the uncommitted changes that stay.
- [x] Checkout on the shell: the quoted ref (remote or local chip with its tip row); the branch picker is a searchable chip select (BranchPicker.tsx: a filter box with the focus, Branches / Remotes groups, chips with the tip SHA and subject, the checked-out branch excluded; shared with Merge); for a remote branch, "Check it out as": (•) Create local branch [name] tracking origin/x — the default, the name suggested from the remote's, red-chip validation as above; ( ) Reset local branch x to origin/x — offered only when a local x exists, its note from the engine's divergence ("x has 2 commits that origin/x does not; resetting makes them unreachable." in amber and the primary red "Reset and check out"; a plain fast-forward stays blue); ( ) Check out the commit — detached HEAD; "Local changes (n files)": Keep / Stash / Discard (Discard makes the primary red). Engine: POST /checkout grows {ref, as: track|reset|detached, name, localChanges: keep|stash|discard} (`checkout -b name --track`, `checkout -B name origin/x`, `checkout --detach`, an autostash pop after, `checkout -f` for discard) and GET /branches/divergence?local=&remote= answers {ahead, behind}; PowerGit.Engine.Tests cover the four paths; SRS-ENG entry added by the coordinator.
- [x] The family on the shell: Merge (two quoted rows with "into", the picker, "Fast forward" radios — Fast-forward when possible / Always create a merge commit / Fast-forward only — refuse otherwise, Squash, "Auto stash n uncommitted changes and restore them after", Specify merge message; the amber note when the tree is dirty and Auto stash is off), Reset (the band says "powergit moves from <sha> to" + the row; Soft / Mixed / Hard with their consequences; Hard → red "Reset and discard n changes"), Delete branch (the chip and its tip row; "Merged into powergit — nothing is lost." or "Not merged — n commits only on this branch become unreachable." with the red "Delete unmerged branch"; engine: merged-into and the count), Rebase, Cherry-pick, Revert (a single quoted row, their options unchanged), Delete tag. GE parity in the copy where GE has words (git-extensions-map.md).
- [x] Tests, light by the owner's call (2026-09-16: "just do very light tests on these features to make sure they work"): unit refName + BranchPicker model + shell (427/427 on the merged tree), engine 186 passed / 1 skipped (checkout track / reset / detached / stash / discard / keep, divergence), e2e merge.spec (5/5, incl. the picker label inside the dialog) on the main harness, a Playwright smoke on the merged tree with zero console errors: Create branch (invalid name → red "no spaces" chip and a disabled primary; valid → "Create and check out"), Checkout of origin/powergit with a local powergit (Create "powergit-2" default, Reset with the amber "14 commits … unreachable" note, Detached, Local changes), Merge at 600 px — screenshots compared with the prototype by the coordinator. NOT run: the checkout e2e cases for the three remote ways (Codex left them; the engine tests cover the paths), context-menu / git-ops-extra / browse-reset / rebase / error-banner / toolbar-chrome / git-console specs (updated by Codex, unverified); the visual @dialogs baselines do not picture these dialogs. Debt: run that subset before the next release.
- [x] Owner tick: on the owner's screen Create branch, Checkout of a remote branch and Merge read as the prototype; nothing clipped or wrapped in any of the family.

### v0.18.12 — Go to HEAD, parent and child — navigation controls on the graph (2026-09-16) (COMPLETE)
**Goal:** Owner (2026-09-16): "when we're in the main graph, I'd like to have some sort of control buttons for 'go to head', 'go to successor' and 'go to parent'" — "visual prototypes first". Git Extensions has the commands without buttons: Go to parent (Ctrl+P, the first parent of a merge, then the way back is remembered), Go to child (Ctrl+N, the first child in the loaded graph), Select current revision (Ctrl+Shift+C), and Alt+← / Alt+→ over the selection history. Prototyped in docs/prototypes/graph-nav.html: A a segmented cluster at the right end of the grid header (⌂ HEAD · ↑ Child · ↓ Parent, a "2" badge and a parent list on a merge), B a floating compass pill at the bottom-right of the graph — the twin of the options pill — with the three stacked, C chevrons on the selected node itself plus a HEAD button in the Graph header; each with the disabled states (first commit, no child loaded, already at HEAD) and the "target not loaded yet" case where the grid pages until the parent arrives the way a ref jump does. The owner picks before implementation and the tasks are written then; the three chords ship with any variant.
- [x] Owner pick (2026-09-16): "definitely the compass bottom right" (prototype B). Model first: graph/graphNav.ts — parentOf(rows, sha, which) (first parent by default, GE's way back remembered per row so Ctrl+N after Ctrl+P returns to the merge), childOf(rows, sha) (the first child in the loaded graph; the same-lane preference is an open question, default GE), headRow(rows), a selection history (back/forward, capped at 50) — unit tests on a synthetic graph with a merge, a root and a tip.
- [x] The compass: a floating vertical pill at the bottom-right of the grid body, the twin of GraphOptionsBar (same border, shadow, 45 % opacity at rest, full on hover), three 28 px round buttons ↑ child · ⌂ HEAD · ↓ parent with tooltips "Go to child Ctrl+N", "Go to HEAD Ctrl+Shift+C", "Go to parent Ctrl+P"; HEAD tinted primary while the selection is HEAD; disabled with the reason on hover ("No parent — first commit", "No child in the loaded history", the pending rows: no navigation); on a merge the parent button carries the "2" badge and a chevron that opens the parents list (SHA, subject, first parent marked); the pill never overlaps the graph scrollbar or the options pill (test ids graph-nav, graph-nav-head, graph-nav-child, graph-nav-parent, graph-nav-parents).
- [x] Keys: Ctrl+P, Ctrl+N, Ctrl+Shift+C in the browse scope (hotkeys/catalog.ts, GE ids GoToParent, GoToChild, SelectCurrentRevision), Alt+← / Alt+→ walk the selection history (NavigateBackward/Forward); the settings' hotkey table lists them; docs/agents/memories/hotkeys.md updated.
- [x] A parent not loaded yet: the grid pages until the commit arrives, the loading tail says "Loading history to febf4ba…", the selection lands on it and scrolls into view (the ref-jump path of v0.10.0-3); under the ref filter (v0.18.5) a parent outside the filtered set says so in the tooltip instead of paging forever.
- [x] Tests, light by the owner's call (2026-09-16): unit graphNav + hotkeys (in the 427/427 merged run); e2e graph-nav.spec 7/7 on the main harness (parent / child / HEAD clicks and keys, the merge's parents list, disabled states, Alt+← back, HEAD tint); the smoke on the merged tree shows the pill bottom-right with the "2" badge on a merge and the HEAD tint; the @grid selected-hovered visual baseline retaken with the compass in the frame. NOT run: highlight-ancestry / graph-branch-filter / shell specs on the merged tree (Codex's runs died at its 30 s cap); no dark-theme or hovered-state baseline. Noted for the owner tick: at rest the pill sits over the last SHA cells of a narrow grid (850 px crop) — a bottom offset above the loading tail, or a reserved strip, is the follow-up if it bothers.
- [x] Owner tick: on the owner's repository the compass reads at a glance, parent/child/HEAD land where expected, a merge offers its second parent.

### v0.18.13 — Pending lists that scale — virtualized file lists, a status that keeps its identity
**Goal:** From the v0.18.10 audit (docs/perf/audit-2026-09-16.md §4 #1–2), the owner's "with a lot of pending changes the app becomes unusable": the commit window with 12,500 pending files takes 21 s to open, 29 s until its lists are there, then a click on Stage does not react for 30 s+ (915 MB heap); at 2,500 files it opens in 3.7 s and staging one file takes 5.5 s; the Diff tab on the Working directory row takes 13 s at 12,500 files; with the window open every 10 s status poll freezes the UI ~3.5 s. Cause: CompactFileList renders every file as three MUI Boxes with no virtualizer (+68,070 DOM nodes), and every status answer is a new object that re-renders both lists and the Diff tab and re-fetches the worktree diff. Fix: CompactFileList on useVirtualizer in flat and tree modes with rows as plain classed elements; useRepoState keeps the previous status object when the new one is deep-equal; pendingOf memoised on the status' file arrays; usePendingDiff keyed by path + staged; useCommitFiles selections stable across status. Budgets (production build, harness): commit window open ≤ 500 ms and Diff tab ≤ 400 ms at 12,500 files; stage one ≤ 400 ms at 2,500; a status poll with the window open ≤ 50 ms of main thread; tests/perf/pending.spec.ts is the gate at N = 2,000 and gains N = 10,000. Estimate 2–3 worker-days. Proposed first.
- [x] CompactFileList on useVirtualizer in flat and tree modes, rows as plain classed elements (no per-row MUI Box), keyboard, selection scroll, context menu and every test id kept (Codex, 1e137f0e5).
- [x] Status identity: useRepoState keeps the previous status object when the new one is deep-equal (statusEquals + unit tests), pendingOf memoised, usePendingDiff keyed by path + side, selections stable (Codex, 7844ffd38).
- [x] Gate: tests/perf/pending.spec.ts expects a visible virtualized row (1cf65dfc9); measured after on the 2,500-file tree: Working directory row 1,862 ms, Diff tab file list 213 ms (was 2,300), commit window lists 219 ms (was 3,700 to open) — the N = 10,000 case and the render-count probe are not written (debt).
- [x] Verified by the coordinator: tsc, lint, unit 429/429, e2e commit-dialog-size / commit-file-menu / commit-partial-diff / commit-reliability / pending-rows / review-mode / diff-view / browse-reset 22/22 on the private harness, test:perf pending gate green. Memory docs/agents/memories/perf-pending-lists.md.
- [ ] Owner tick: on the repository where the app became unusable, the commit window opens and stages at once.

### v0.18.14 — Big lists reach git — paths on stdin, the ref filter as a body, fetch failures in the banner
**Goal:** Two defects the v0.18.10 audit met on the way (§4 #3–4, §6): "Stage all" above ~1,300 files on Windows never runs — GitHost.Queries.cs Stage() puts every path on argv (> 32 K) and the button does nothing visible; and the ref filter of v0.18.5 empties the graph above ~250 refs — GET /revisions?ref=… × 289 is 8,898 characters, Kestrel answers 414, the fetch rejection is unhandled and the grid goes blank with no error (on vscode a single tick on a remote folder does it). Fix: Stage, Unstage, files/delete, files/reset, ignore and patch take their paths through `--pathspec-from-file=-` on stdin (the RunTimedWithStdin shape of v0.18.5), "Stage all" / "Unstage all" use git's own whole-tree forms (add -A, restore --staged .) as GE does; the ref filter travels as POST /revisions with a JSON body (GET kept for ≤ 8 KB); a failed page fetch surfaces in the error banner. xunit with 3,000 paths; e2e with 400 refs ticked. Budgets: stage all of 12,500 files completes; Show all on 5,577 refs draws the graph. Estimate 1–1.5 worker-days. Proposed second.
- [x] Paths on stdin: Stage / Unstage / index bits / untrack take `--pathspec-from-file=- --pathspec-file-nul`, Stage all = `add -A`, Unstage all = `restore --staged .`; the command log counts the entries (Codex 48cf8f5bc). files/delete, files/reset and patch routes still take argv — debt for a follow-up.
- [x] The ref filter travels as POST /revisions with a JSON body (always, for a filter); the route sits outside the mutation gate so it never queues behind an operation nor answers 409 during a fetch (196f74c24 — caught by the coordinator, Codex had it gated); a failed page fetch reports "History: …" through the failure handler (no unhandled rejection).
- [x] Regression tests: ArgvLimitTests (3,000 paths staged/unstaged, a spaced/quoted/non-ASCII path, the stdin log line, POST with 400 refs, an unknown ref → 400), graph-branch-filter "Show all on 400 refs draws the graph", useHistory "a failed page reports History:". Show all is a no-op until the ref tree has arrived (f9ae93f7b: the spec clicked faster than /refs answered and got HEAD alone).
- [x] Verified by the coordinator: engine 190/191 on the merged tree, unit 432, tsc, lint, e2e graph-branch-filter 2/2 (main), commit-file-menu / commit-partial-diff / pending-rows / browse-reset / file-tree-worktree / status-bar on the private harness.
- [ ] Owner tick: Stage all on a tree with thousands of files stages them; Show all on a repository with thousands of refs draws the graph.

### v0.18.15 — Engine on big repositories — short ref names in C#, bigger history pages, a cheaper status
**Goal:** From the v0.18.10 audit (§4 #5–6): F5 takes 3.8 s on vscode and 2.0 s on flutter, 2.6 s of it `git for-each-ref … %(refname:short)` (2,578 ms of the 2,700 ms /refs on 5,577 refs; 130 ms without it); boot to 10,000 rows takes 17 s on vscode and 11.7 s on flutter because every 1,000-row page is a fresh `git log --date-order` over thousands of tips at 1.0–1.5 s regardless of skip; GetStatus runs six processes of which rev-parse and the branch name are constant. Fix: GetRefs drops %(refname:short) and shortens in C# (strip refs/heads/, refs/remotes/, refs/tags/); history pages 1,000 → 3,000 under the same eager ceiling, or one streaming git log per session if the fixed cost dominates (the measurement says it does); GetStatus caches the git dir and reads the branch from the refs it already has. Budgets: F5 on vscode < 1.5 s; boot to 10k rows on vscode < 8 s; a status on the 18,930-file vscode tree stays fs-bound (core.fsmonitor is the follow-up). Estimate 1.5–2 worker-days. Proposed third.
- [x] GetRefs drops `%(refname:short)`: for-each-ref asks for %(refname) (+ the fields it already asks for) and the short name is derived in C# (strip refs/heads/, refs/remotes/, refs/tags/; anything else keeps refs/…); ambiguous cases git would have disambiguated (a branch and a tag of the same name) keep their full prefix; xunit: the short names of a repo with heads/remotes/tags and a same-named branch and tag; a timing test on 5,000 generated refs asserting /refs under 400 ms.
- [x] History pages: PAGE 1,000 → 3,000 in useHistory with the same EAGER_CEILING and HARD_CEILING (the layout worker appends in one go; paging/jump/filter specs unchanged) — or, if the audit's fixed-cost finding holds on re-measure, one long-lived `git log` per session streamed in 3,000-row chunks; either way boot to 10,000 rows on flutter/vscode within the budget below; history-paging.md updated.
- [x] GetStatus loses two processes: the git dir is resolved once per session (RequireRoot cache) and the branch comes from the ref tree the session already holds (HEAD symbolic ref read from the file, as the watcher does), so a status is `status -uall` + ls-files -v + ahead/behind; xunit on the status DTO unchanged; the command log shows four calls per status.
- [x] Budgets, re-measured with the audit harness on vscode (dev build, 2 runs, medians): F5 3.8 s → 1.65 s (for-each-ref 2,578 → 195 ms; what remains is one `git log` page of 3,000 rows at 1.4 s) — 10 % over the 1.5 s budget, the streaming log is the follow-up; boot to 10k rows 17.0 s → 7.4 s settle (12,000 rows: the eager loop fills whole pages past the 10,000 ceiling) — within the 8 s budget; status on the vscode tree 176 ms. tests/perf/interaction.spec.ts got a heavy-fixture boot budget (Codex 7a7b40b5c).
- [x] Verified by the coordinator: engine 194/195 on the merged tree (a GetDiff-of-a-rename inconsistency the suite met for the first time is fixed — GetDiff gives git both paths so it matches the whole-commit patch; the 5,000-ref timing budget allows a loaded box), unit 435, tsc, lint; e2e on the branch: graph-branch-filter, live-refresh-scope, status-bar, shell, graph-nav, ref-chips, highlight-ancestry, history-paging 39/39 (the paging fixture now spans two 3,000-row pages); on main: graph-nav, selected-row-graph, graph-branch-filter, status-bar, live-refresh-scope 17/17; smoke on the real repository: first rows 1.6 s, 12,001 rows loaded, zero console errors. Numbers in docs/agents/memories/perf-engine-refs-pages.md.
- [ ] Owner tick: on the biggest repository at hand, F5 and the first screen feel immediate.

### v0.18.16 — Grid render hygiene — graphWidth memoised, rows memoised, ancestry incremental
**Goal:** The graph itself passed the v0.18.10 audit (hover → canvas 32–34 ms dev on 10,000 rows of a 165k-commit repository, click → Commit tab 140–220 ms, no long task in a 6 s scroll on flutter), but the traces name three app-owned costs (§4 #7–8): graphWidth(rows) walks all rows on every RevisionGrid render (143–216 ms of self time per 6 s scroll, the largest app function in both profiles); RevisionRow is not memoised so a hover re-renders ~45 rows and their RefChips; markAncestry re-walks 10k rows on every page append. Fix: useMemo graphWidth on rows; React.memo(RevisionRow) with stable callbacks; incremental markAncestry on append. Budgets: hover → canvas < 16 ms production; no long task during a 6 s scroll on vscode. Estimate 0.5–1 worker-day; after the three above, not before.
- [x] graphWidth(rows) memoised on rows in RevisionGrid (useMemo), and the derived autoWidth/overflow with it; a unit test on the pure function stays, a render-count probe (the project's createRoot pattern) proves a hover re-render does not call graphWidth again.
- [x] React.memo(RevisionRow) with stable callbacks (useCallback on onClick / onContextMenu / onMouseEnter / onRefContextMenu / onExpand / onFold in RevisionGrid) so a hover re-renders only the two rows whose class changes; RefChips memoised on (refs, budget, expanded, current); selected-row-graph.spec and ref-chips.spec unchanged.
- [x] markAncestry incremental on append: the ancestry marks of the already-marked prefix are kept and only the appended rows are walked (a merge into an older row can only add marks below it; the highlight root's marks are recomputed when the root changes); unit tests prove full-run and incremental give identical marks on the synthetic graph with merges and on a 10k-row generated history.
- [x] Measured with the audit harness on flutter after (dev build, 2 runs, medians; 12,000 rows loaded): hover → canvas redraw 23 ms median / 30 ms p95 (was 32–34 ms), no long task in the hover sweep; a 6 s scroll shows 2 long tasks (172 ms total, max 110 ms) that are the 3,000-row page appends of v0.18.15 landing mid-scroll (the audit's 1,000-row appends were ~75 ms each), script 1,602 ms. tests/perf/interaction.spec.ts rejects long tasks > 50 ms during the scroll on the heavy fixture (Codex 77405e29c). Before numbers in docs/agents/memories/perf-grid-render.md.
- [x] Verified by the coordinator: unit 435/435, tsc, lint; e2e on the branch: selected-row-graph, ref-chips, highlight-ancestry, graph-nav, shell, author-identity 48/48; on main (merged with v0.18.15): graph-nav, selected-row-graph, graph-branch-filter, status-bar, live-refresh-scope 17/17; the @grid selected-hovered baseline retaken (the repository's own rows changed; the rendering did not — checked by eye); smoke: hover sweep, select, F5, zero console errors.
- [ ] Owner tick: hovering and scrolling the graph on the biggest repository feels the same or better; nothing looks different.

### v0.18.17 — Operations that work — merge, reset, rebase, cherry-pick, revert on a real working tree, never dropped, never silent
**Goal:** Owner (2026-09-16): "It's really important that the operations in the repo are working. Whenever I do a merge or a reset, usually it fails. Simple operations like that are failing. App looks good, but we need to ensure the operations are working properly." Scheduled before the perf fixes (v0.18.13–16) and run with full tests, not light ones. Two causes read in the code before any measurement, both to be proven by a failing test first: (1) the engine refuses a merge, rebase, cherry-pick and revert outright whenever any tracked file is modified (GitHost.Sequencer.cs HasTrackedChanges: "The working tree has uncommitted changes. Commit or stash them first, or merge with autostash.") — git itself merges over a dirty tree unless the merge touches the same files, Git Extensions never pre-refuses, and a developer's tree is always dirty, so the default Merge (Auto stash off) fails every time; (2) useJobs.withBusy returns silently when the app is busy (a background fetch on its interval, a job, an engine call in flight) — the dialog's submit resolves, the dialog closes, nothing ran, no message; and the engine answers 409 Busy for a mutation during a job, which the dialogs do not handle. Reset itself runs `git reset --<mode>` and should work; whatever the owner meets there is to be found by the campaign (the busy drop is the prime suspect) — the owner's exact message is asked for. Fix: the engine lets git decide (drop the four blanket refusals; git's "would be overwritten" answer becomes a typed error the dialog turns into "Stash and retry", i.e. autostash for merge/rebase, a stash push/pop around cherry-pick/revert); an operation requested while a job runs waits for it, with the top bar saying so, and never vanishes; a 409 is waited out the same way; every operation error stays in its dialog with git's own stderr and the command, fields intact, and lands in the console. A campaign of e2e specs runs every operation on a fixture whose tree is dirty the ways a developer's is (modified tracked files that do not overlap, ones that do, staged files, untracked files) and during a running fetch.
- [x] Symptom first — done, and it corrected the plan's assumption: tests/e2e/operations-dirty-tree.spec.ts (Codex 15fb3c314, then the coordinator's fixes) on a fixture dirty like a developer's (fileB, fileC modified; fileD STAGED; two untracked). What git really does: Merge and Rebase refuse a staged file (git-merge(1): "changes registered in the index relative to HEAD"; rebase: "cannot rebase: You have unstaged changes"), Cherry-pick and Revert refuse a staged file ("your local changes would be overwritten"), Reset soft/mixed/hard and a Checkout of an untouched branch go through. So the spec asserts: each refusal stays INSIDE its dialog in git's words with "Stash and retry", which succeeds and brings the dirt back (git's own --autostash for merge/rebase leaves the staged file modified but unstaged; the engine's stash around cherry-pick/revert pops --index and keeps it staged); reset mixed unstages fileD (git); checkout keeps the dirt; a reset confirmed during a fetch waits ("Waiting for Fetching…") and lands; the error banner never appears. Engine xunit twins in SequencerTests / CheckoutTests. 7/7 on the ops branch and on the merged main tree.
- [x] Engine (Codex 9befda411, 9d13988c8; coordinator fc237a55d): the four HasTrackedChanges refusals are gone and so is Checkout's — git runs and decides; every wording of git's dirty refusal (would be overwritten by …, cannot rebase: You have unstaged changes, your index contains uncommitted changes, Please commit or stash them, commit your changes or stash them to proceed) throws DirtyTreeException → 409 {code: "dirty", message, files}; the busy 409 carries code "busy"; Cherry-pick and Revert gain Autostash (stash push, pop --index; a pop conflict is the error, the stash kept); Rebase/Merge keep git's --autostash. Engine 190/191 on the merged tree.
- [x] Frontend (Codex 9befda411, 8c45f26d5; coordinator): useJobs.withBusy waits while another engine call runs ("Waiting for <label>…") instead of returning silently; every operation dialog keeps a failed engine call inline (rebase's used to reach the banner) and a {code: "dirty"} answer shows "Stash and retry" (merge / rebase / cherry-pick / revert; checkout retries with Local changes = stash); Rebase pre-ticks Auto stash on a dirty tree; a dirty local checkout from the tree opens the dialog (its Local changes group is where git's refusal can be answered). Unit tests useJobs.test.ts, useActionDialog.test.ts (Codex d61648042).
- [x] Reset: soft / mixed / hard on the dirty fixture and a reset confirmed during a fetch all pass; nothing to fix in ResetTo itself — the owner's reset failures were the silent busy drop (withBusy) and, on the tree, checkout's guard. The owner's exact message, if it recurs, still wanted.
- [x] Full verification: engine 190/191 (merged tree), unit 432/432, tsc, lint; e2e on the ops branch: operations-dirty-tree 7/7, merge / rebase / rebase-interactive / browse-reset / git-ops-extra / context-menu / error-banner / live-refresh-scope / shell / checkout 52/52; on the merged main tree with the default harness: operations-dirty-tree 7/7, merge, checkout, pending-rows, commit-file-menu, graph-nav, rebase, browse-reset, git-ops-extra, shell 25/25, graph-branch-filter 2/2; smoke on the real repository: 31 rows, compass mounted, Merge dialog opens, zero console errors.
- [ ] Owner tick: on the owner's repository with the usual uncommitted changes, a merge and a reset from the graph just work; when git refuses, the dialog says why in git's words and offers Stash and retry; nothing ever closes without doing anything.

## v0.19 — Review mode, continued — the file, the commit window, whole-review progress, comments
> The four review-mode iterations designed as v0.17.1–v0.17.4 (docs/design/review-mode.md §6), renumbered on 2026-09-11 because v0.18.0 (the settings page) shipped between v0.17.0 and them: a release tag is the last closed iteration, and a v0.17.1 after v0.18.0 would read as older to the updater. Content unchanged; the old headings under v0.17 are closed stubs that point here.

### v0.19.0 — The review survives — the file, the engine routes, the Review file pane, Export
**Goal:** A review is one JSON document per key (commit sha, or HEAD-worktree / HEAD-index for the pending rows) at <repo>/.powergit/reviews/<key>.json, written by the engine, hidden from git through .git/info/exclude, loaded when the row is selected and saved debounced as marks change. The Review file button shows the file as written with Copy; Start over deletes it; Export at the left of the bar copies the review as Markdown or JSON, or saves it. A reload keeps the review. This file is what an agent will read over MCP later. (Was v0.17.1.)
- [x] Engine GitHost.Reviews.cs + Program.cs: GET / PUT / DELETE /repos/{id}/reviews/{key}; key validated by ^[0-9a-f]{40}([0-9a-f]{24})?(-(worktree|index))?$ and the final path still passed through ResolveInRoot; PUT writes temp + rename; the first write ensures /.powergit/ is in .git/info/exclude (EnsureExcluded factored out of ExcludeFiles, exact-line check) and bumps the status; the reviews routes bypass the mutation gate so a save never 409s during a pull; the directory is one constant (app-data fallback is a one-line switch, owner call 3). xunit ReviewsTests: round trip, file at .powergit/reviews/<sha>.json, git status --porcelain empty afterwards, a ../x key refused, DELETE removes, GET 204 when absent (was 404 in the plan: a missing review is the ordinary case for every row, and a 404 logged a console error per selection — 87e27bb3b). SRS-ENG-054. Done: Codex Sol 5299910fb (M1 worker wt/review-engine), merged e6436433d; the PUT reads its body with ReadToEndAsync and the exclude check runs once per session; ReviewsTests 6/6, the whole engine suite 202 passed / 1 skipped / GetRefs' 1,000 ms budget missed at 1,420 ms on this machine today (1,386 ms on the pre-merge engine; raw for-each-ref 100 ms — the box, not the change).
- [x] review/reviewKey.ts (+test): commit row → full sha; Working directory row / unstaged file → <HEAD>-worktree; Index row / staged file → <HEAD>-index; disabled while headId is null. engine/reviews.ts client functions over EngineClient.request (files.ts pattern). hooks/useReview({engine, key}): load on key change (latest wins), debounced save with saved-at, the document merged into reviewState so both surfaces see it. Done: Codex Sol 54eb6df36 (wt/review-file), merged e6436433d; the save debounce is 400 ms with the loaded/saved instance as the baseline, "Saving…" from the first unsaved change and a pagehide flush with keepalive (review-persist.spec caught a reload inside the window losing the last mark).
- [x] components/ReviewFilePane.tsx beside the diff: the JSON exactly as written (version, commit, head, reviewed, changed, status, files.lines, files.comments), Copy, saved-at line; Review file and Start over buttons in the bar; the pane also serves as the debug view of the format for the MCP major. Done: 54eb6df36 / e6436433d — review/reviewFile.ts serializeDoc fixes the key order, the pane's text is what PUT sends (byte-equal to the file on disk, asserted); BottomTabStrip.tsx split out of BottomPanel for the lint cap.
- [ ] Owner (2026-09-11): "left of the REVIEWING banner we need something to export the review." An Export button at the left of the review bar, an outlined button with the tabs' weight, disabled until a line is marked, opening a menu: Copy as Markdown (default: per file, the rejected lines with a few lines of code around each, the comments once v0.19.3 adds them), Copy JSON (the document as written), Show review file (the pane), Save as… (Markdown or JSON through the shell's save dialog); unit test for the Markdown digest, e2e: mark a line, Export → Copy as Markdown, the clipboard holds the file path and the line. Implemented e6436433d (reviewFile.ts reviewMarkdown: per file the rejected and commented lines with two rows of diff around each, the comments quoted; reviewFile.test.ts; ReviewExportMenu.tsx; Save as… through the shell's save dialog, a browser download in the harness; review-persist.spec asserts the clipboard holds f.txt and +second), awaiting owner verification.
- [x] Symptom e2e review-persist.spec.ts: mark two lines on the Working directory row, reload the page, the marks are still there, the JSON on disk at .powergit/reviews/<HEAD>-worktree.json equals the pane text, and git status shows no untracked .powergit; Start over empties the pane and removes the file. Done: e6436433d, green on the main harness.
- [ ] Owner ticks: a review made in the evening is there the next morning; git status in the terminal stays clean; Export → Copy as Markdown pastes something an agent can act on.

### v0.19.1 — Review mode in the commit window
**Goal:** The commit dialog gets the same review mode on its diff: the button in the files bar left of the mode toggle, the bar in the dialog frame, the same keys, the same marks. The key follows the selected file (unstaged → HEAD-worktree, staged → HEAD-index), so a line marked in the dialog is marked in the Diff tab of the Working directory row and vice versa. (Was v0.17.2.)
- [ ] CommitDialog.tsx is at the 400-line lint cap: split first (CommitDiffColumn.tsx for the diff side and its option bar, CommitDialogConfirms.tsx for the confirmations) with no behaviour change; the existing commit specs stay green.
- [ ] headId reaches the dialog (App.tsx → AppDialogs.tsx prop); the review key follows selected.staged; useReview and the review layer (over the commit layer, thanks to the v0.17.0 fall-through) are wired; the button in CommitFilesBar.tsx before the mode toggle; the bar in CommitWindowFrame's children slot; Enter / Shift+Enter walk the unstaged then staged lists.
- [ ] Symptom e2e review-commit-dialog.spec.ts: Ctrl+Space → review on → mark a line in f.txt (unstaged) → close the dialog → the Working directory row's Diff tab shows the same mark; stage the file, the mark moves with the key to HEAD-index (the unstaged diff no longer has that line).
- [ ] Owner ticks: reviewing in the commit window before committing feels like the Diff tab; the marks are the same in both.

### v0.19.2 — Whole-review progress — file pills, the meter over every file, "Review complete"
**Goal:** The review is about every file, not the one on screen: each file row carries reviewed/changed (a small check when complete, rejected count in red) and an amber or blue left stripe, the bar's meter and counts cover all files, n and Enter cross into the next file with work, and the label turns to "Review complete" when nothing is left. Changed-line counts come from git so files not yet opened have a denominator. A reviewed file row stays quiet, like the ok mark after the v0.19.0 design pass. (Was v0.17.3.)
- [ ] Engine: added / removed line counts per file (git diff --numstat) on the commit file list (/changes, ListFiles) and on the pending rows' file lists and the commit dialog's status lists; binary files report null; FileChangeDto / StatusFileDto and engine/types.ts gain the two fields; xunit on a fixture with an added, a modified, a deleted and a binary file.
- [ ] CompactFileList.tsx: optional per-row trailing content and a data-review attribute for the stripe; reviewModel gains the cross-file progress, nextUnreviewed across files, and the 'complete' status written to the document; the bar shows d / n lines · r rejected · f of F files and "Review complete"; n and Enter move to the next file with work.
- [ ] Symptom e2e review-progress.spec.ts: two changed files; pills read 0/2 and 0/1; marking both lines of the first turns its pill to ✓ 2/2 and its stripe blue; Enter lands on the second; marking its line turns the label to "Review complete" and the file's status to complete.
- [ ] Owner ticks: from the file list alone you can tell what is left to review.

### v0.19.3 — Comments and the slash command line
**Goal:** Press / on a line and a command line opens under it: /comment <text> attaches a review comment to that line (shown as a note row, editable, several per line), /ok /reject /clear set its state, Enter runs, Esc cancels, an unknown command gets a hint; a "+" on hover in the gutter does the same as /comment. Comments are in the review file with their line, in the Markdown export, and counted in the bar. Finish review saves and shows the summary; sending it to an agent is the next major. (Was v0.17.4.)
- [x] review/reviewCommands.ts, pure and unit-tested: parse and run /comment (/c), /ok, /reject (/rej, /x), /clear; errors for a missing text, a marking command on a context line, an unknown command; the command never fires from inside a note textarea or the command input. Done: Codex Sol e9356f306 (M1 worker wt/review-comments), merged e6436433d — the typing guard is the hotkey layer's `editing` rule (an INPUT/TEXTAREA target never resolves the review scope) plus stopPropagation on the rows; reviewCommands.test 4, reviewModel.test 34, useDiffReview.test 11.
- [x] DiffView.tsx / VirtualLines.tsx: a row model {line | note | cmd} so comment rows and the command row sit under their line in both the plain and the virtualized path (per-index sizes, measureElement on note rows); they are not .diff-row so copy and selection skip them; components/ReviewRows.tsx renders the note row (textarea, line label, delete) and the command row (input, hint); the '+' hover affordance in the gutter. Done: e9356f306 / e6436433d — DiffView builds the item list only with the review prop (the non-review DOM stays byte-identical, DiffView.test 9), a line-to-item map for the scroll; VirtualLines takes estimateSize / measure / itemKey (stable keys so measured sizes follow an inserted note); docs/agents/memories/review-comments.md.
- [x] Comments in the document ({line, text} per file), in the pane, in the Markdown export, the count in the bar; Finish review saves, marks the document complete when nothing is left, and shows the summary (lines, rejected, comments) — no agent call yet. Done: e6436433d — withComment / withCommentText / withoutComment / commentsOf / commentCount in reviewModel, the count in ReviewBar ("· 1 comment"), the quoted comments in reviewMarkdown, Finish review flushes the pending save and opens ReviewSummaryDialog (lines, rejected, comments, files, the file path); `recount` marks complete when reviewed ≥ changed.
- [x] Symptom e2e review-comments.spec.ts: / on a changed line → type /comment needs a guard → Enter → the note row is under the line with that text, the bar says 1 comment, the pane JSON contains {"line":"+5","text":"needs a guard"} (the fixture's +second is new line 5); /reject marks the line; /bogus shows the hint and leaves the line alone; Esc closes the command line; the + in the gutter opens /comment; Finish review's summary reads 1 / 2 lines reviewed · 1 rejected · 1 comment. Done: e6436433d, green on the main harness.
- [ ] Owner ticks: a review with a few rejected lines and comments reads back from the file the way it was made.

> **Release v0.18.17 (2026-09-17)** — tag `v0.18.17` on `ac612ad0a` (the baselines commit); release workflow run 35207383576 green (windows / linux / manifest); assets `PowerGit_0.18.17_win64.zip`, `_x64-setup.exe` (+.sig), `_amd64.AppImage` (+.sig), `latest.json` (manifest version 0.18.17, both platforms); notes published; Pages showcase re-captured at v0.18.17 and the feature cards updated (compass, dialogs that quote the graph, git deciding on a dirty tree, the big-repository numbers), deploy green, `test:live` green. Preflight: engine 194/195, full e2e 180/180 (two git-console cases updated to the v0.18.17 rule that a failed operation stays in its dialog), resolution 20/20, visual 11/11 retaken, packaged zip smoke (engine 0.18.17, the repository renders, Merge dialog, 150 %, zero console errors). As for v0.18.0–v0.18.8, iterations v0.18.13–v0.18.17 stay open on their owner-tick tasks; the ACTIVE marker is on v0.18.17. Seen on the way: the perf audit's heavy fixture had landed first in the owner's real Recent repositories (2026-09-16 11:21, before the harness moved to a private data dir) — removed from disk so the store prunes it; and the Graph column takes its 35 % cap on the 1280 px baselines now that 12,000 rows are loaded (the deepest lane in 12k rows), which narrows the Message column — a follow-up if it bothers.

### v0.18.18 — A reactive UI — one amplifier, four costs, from the 2026-09-17 review
**Goal:** Owner (2026-09-17): "I have a feeling like it's slow, sluggish, lagging. Not very smooth and reactive." A five-finder review with a skeptic per finding (46 raw, 39 distinct, 13 confirmed, 11 refuted; docs/perf/reactivity-review-2026-09-17.md) found one amplifier and four independent costs. The amplifier: App.tsx passes `onSelect={(i) => setSelectedSha(…)}` inline, RevisionGrid puts it in the deps of clickRow/contextRow, and those are props of every memo(RevisionRow) — so every App render re-renders the ~60 rows of the virtual window instead of the 2 whose selection changed; and App renders twice per click, twice every 10 s (the status poll flips `refreshing` even when nothing changed), twice per action, once per splitter pointermove, every 400 ms during a fetch. The costs: (1) Shiki tokenises the whole diff synchronously on the main thread on every arrival, no cache — 0.1 ms/line measured, 760 ms at 7,200 lines, plus ~300 ms first-run grammar compile per language; (2) every in-app action refreshes twice — the watcher echo is deferred past the mute, never dropped, so the full sweep (3,000-row log, refs, four status processes) runs again 1.2 s later (useOperationActions.test pins three page-0 fetches for one merge); (3) scroll — react-virtual's flushSync render on every 28 px range change, the canvas bitmap reset and fully redrawn per step, a non-passive wheel listener that makes the compositor wait for the main thread; (4) the splitter drag sets App state per pointermove (whole tree per frame, one emotion rule per pixel), and any reload whose page 0 changed resets the layout worker (all rows re-laid-out, ~12 MB structured-cloned for 10k rows). Also: RenamedFrom (v0.18.15) spawns a whole-commit diff-tree on every per-file diff. Ruled out by the skeptics: GetStatus's four processes, BottomPanel's missing memo, the 3,000-row JSON parse, EngineProvider's fresh context value. Each task carries its measurement; a second pass on the graph's own interactions and the bottom panel's rubber-band resize follows as its own tasks.
- [x] A — The amplifier (Codex): App.tsx gives HistoryPane, StatusBar and the grid stable callbacks from the `chrome` useStable bag (`selectRow` reads rows at call time) and RevisionGrid keeps onSelect in a ref; useRepoState.refresh does not flip `refreshing` for a status-only scope; GraphOptionsBar is memo'd; the bottom-panel splitter and the files SplitHandle write `--pg-bottom-height` / `--pg-files-width` while dragging and commit state on release. Measurement: RevisionGrid.test.ts renders 60 rows, rerenders with a new `selected` and a new onSelect identity — 2 row renders, not 60.
- [x] B — Scroll and hover (Copilot): `useFlushSync: false` on the grid's useVirtualizer (no nav path reads the range synchronously — checked useGraphNav, jumpToRef, graphNav, focusGrid); the canvas effect assigns width/height/style only when the pixel size changed (drawRows clears itself); the Shift+wheel listener is passive (Chromium and WebKitGTK deliver Shift+wheel as deltaX with deltaY 0, the vertical scroller has no horizontal overflow). Measurement: audit on the 50k-commit fixture, hover → redraw 17 → 16 ms median, p95 25 → 23 ms; a 6 s scroll 0–1 long task either side.
- [x] C — The Diff tab (Copilot): highlight.ts keeps a 64-entry / 8 MB LRU keyed by language + mode + text (highlight.test.ts: the second call never reaches the highlighter); `@shikijs/langs-precompiled` 4.4.3 with `createJavaScriptRawEngine` replaces the first-run grammar compile, same language list, pure JS; GetDiff memoises the rename map per (root, full commit id) — QueryTests: two GetDiff calls on a rename commit byte-identical, the second served from the cache.
- [x] D — One refresh per action (Codex, bound reworked by the coordinator): every /repos/{repo} response carries `X-PowerGit-Change-Version` sampled at request start (exposed through CORS); the client keeps a scope of the stamps its requests carried and useRepoState drops a watcher event only when it is at or below the LOWEST stamp of the completed sweep's own requests, tracked per scope (full vs status-only) so a concurrent mutation or a status poll can never mark an unseen refs event as observed; useOperationActions.test asserts two page-0 fetches for one merge, not three. The layout worker keeps its previous rows and posts geometry patches on a reset, useHistory reapplies them by SHA keeping unchanged GraphRow identity — measured 9 of 10,000 rows change for one new top commit (0.09 %).
- [x] E — Second pass on the graph's own interactions and the bottom panel's resize (owner: "when I resize the bottom panel it's jumping or rubber band jumps"): four finders, a skeptic per finding, six named hypotheses for the jump — 25 raw, 18 distinct, 10 confirmed, 8 refuted. The jump: the height is React state, a pointermove's setState renders a frame late and the App-wide render overruns the frame gap, so Chromium coalesces the moves and the panel advances in multi-frame steps (task A's CSS-variable drag removes it); the pointerdown is not default-prevented so a text-selection drag runs alongside (task H). Not the cause: zoom units (already divided), a height transition (none), a moving clamp, pointer capture (present). The graph: the page append's 3.35 MB worker reply and three O(rows) rebuilds in one task (F), uncoalesced key repeats (G), zoom-blind drags and a blurry canvas at every zoom but 100 % (H), the viewport shifting when a reload inserts commits on top (I). docs/perf/reactivity-review-2026-09-17.md §Second pass.
- [ ] Owner tick — click and scroll through the graph on your biggest repository, drag the bottom panel, open a large diff: smooth, no jump, no rubber band.
- [x] F — Page append off the hot path (Codex Sol): the worker's replies carry no `rev` (re-attached from the revisions kept per seq), land in 500-row chunks and are published once through startTransition — 27,306 → 18,306 objects per 3,000-row page (−33 %), 3.69 → 3.02 MB; the navigation index extends its Maps over engineRows on append instead of rebuilding; withArtificialRows keeps a last-call cache so extendAncestry takes its incremental path with a dirty tree (ancestry.test.ts counts the full walks). Measurement: audit boot long-task total 1,038 → 883 ms on the fixture.
- [x] G — Keys, clicks and the compass (Copilot Fable 5.1): useHeldKey coalesces held ArrowUp/Down and PageUp/Down to one selection per animation frame, the first press immediate (RevisionGrid.test.ts: 10 repeats in one frame → one onSelect with the final index; 11 calls before); useStatusNote guards clearNote on a ref (useStatusNote.test.ts: 2 renders per selection, 3 before) and owns the clear-on-selection effect; compass buttons prevent mousedown so a click leaves keyboard focus on the grid (GraphCompass.test.ts).
- [x] H — Zoom-correct drags and a crisp canvas (Copilot Fable 5.1): column-resize and SplitHandle deltas are divided by getZoom(); the graph canvas backing store scales by devicePixelRatio × zoom with `ctx.setTransform` and the size guard comparing the scale; the bottom-panel splitter and SplitHandle ignore non-primary buttons, prevent default on pointerdown and set user-select none. The @grid visual baseline is retaken at the next release (it photographs the repository's own rows).
- [x] I — The viewport keeps its row (Codex Sol): useViewportAnchor remembers the first visible item's key and start and, in a layout effect after rows change, compensates scrollTop by that key's start delta — unless scrollTop is 0 or a selection scroll is pending (RevisionGrid.test.ts: five rows prepended keep the same row at the same offset; the top stays at the top).

### Release v0.18.18 — 2026-09-18

Tag `v0.18.18` on `ee7194bd1`, from iteration v0.18.18 (A–I delivered; the owner tick stays open, as for v0.18.17). Shortest path at the owner's request: no local packaging, no suite re-run, no showcase re-capture — the tag workflow (run 35333805110) builds and signs the Windows zip + installer, the Linux AppImage and `latest.json`. Verified on the merged head before the bump: tsc, eslint, prettier, vitest 451/451, engine 196/196, e2e 180/180, smoke with zero console errors, perf A/B on the 50k fixture neutral-to-better. Not done: the `@grid` visual baseline (photographs the repository's rows; retake before the next tag), the Pages showcase screenshots (v0.18.17's stay).

### v0.18.19 — Launch-readiness from the Grok audit — storefront, cold start, one zoom clip, stale docs
**Goal:** Grok Bot (the owner's COO agent) audited v0.18.18 in AGENTS_IPC.md (local, gitignored) and called marketing NO-GO on four P0 and seven P1 items. A verifier plus a skeptic per finding against HEAD (24 agents) found 4 legit, 2 partial, 1 by design, 6 stale — the P1 table is the 2026-09-05 internal audit re-listed, and five of its seven items were fixed in v0.13.17/v0.13.18/v0.18.0. Legit: the GitHub repository metadata is still Git Extensions' (description, homepage gitextensions.github.io, no topics, Issues off) and nothing in the tree owns it; a packaged cold start lands on the empty "Open a repository…" screen because the engine's last-open is in-memory only and recents[0] is never read back, and `powergit <path>` is ignored end to end (no env::args in the shell, no CLI plugin, the engine takes only --urls/--parent-pid, the Linux .desktop advertises %U nobody reads). Partial: the Linux freeze is closed on the owner's tick of 2026-09-11 (native Wayland default, "No issues at all", Ubuntu features ticked since) but docs/ubuntu-freeze.md, README:131 and two PLAN lines still say open; the audited 150 % zoom clip is fixed and guarded, but JobPanel.tsx sizes the Operations drawer `min(70vh, 560px)` undivided by zoom, which by the tree's own arithmetic puts its header — the only Close — 76 px above a 1280×800 viewport at 150 %. Owner (2026-09-18): "inform grok and fix what can be fixed". The command palette (P1-4) and the Git-console rest label (P1-7) are deferred as separate calls.
- [x] A — Storefront (P0-1): `gh repo edit CynaCons/PowerGit` with the README tagline as description, the Pages site as homepage, topics (git, git-client, git-gui, git-extensions, tauri, react, dotnet, windows, linux), Issues enabled (Discussions left off); a "GitHub storefront" checklist in .claude/skills/release/SKILL.md next to the release-notes step so the next release re-checks it; README gains a "Report a bug" line pointing at Issues. Done 2026-09-18: `gh repo view` shows the PowerGit description, homepage https://cynacons.github.io/PowerGit/, the nine topics, Issues on; step 4b in both release-skill copies and the README line (Codex Sol 6dd4f9a94/c136d7884, merged 3223e4dfb).
- [x] B — Cold start, auto-open (P0-4): useEngineSession.resolveRepo, after /repos/current answers 404, opens recents[0] through POST /repos/open when the new Behaviour setting "Open the last repository on start" is on (default on, Git Extensions' "Open last working dir on startup"); a failed open (root gone, 400/404) falls through to no-repository with a warn in the log, never a banner. Settings row behaviour.openLast in settingsCatalog + BehaviourSection; recents-restart.spec keeps its no-open assertion by mocking the open as gone, and cold-start.spec asserts the reopen: mocked 404 current + one recent → the grid shows that repository. Done: Codex Sol 9fedcb3fa, merged 12e4569a7 — hooks/resolveStartRepo.ts (launch path → pin → current → recents[0] when on; 6 vitest cases), cold-start.spec 2/2 × 2, recents-restart 2/2 × 2, the full e2e 183/183.
- [x] C — Cold start, `powergit <path>` (P0-4): lib.rs .setup reads std::env::args_os().skip(1), takes the first argument that is not a flag, strips a file:// scheme (the Linux .desktop's %U, percent-decoded), makes it absolute and checks it is a directory (not fs::canonicalize — a verbatim \\?\ path on Windows is no directory to git), keeps it on EngineState; engine_config returns it as openPath; bootstrap.ts keeps it (bootOpenPath) for useEngineSession, which opens it ahead of the pin and the current/recent fallbacks (a directory that is not a repository logs a warn and falls through). Cargo unit tests for the argument parsing, vitest for the resolution order, README usage line. A second launch while running (single-instance forwarding) is a follow-up, not this task. Done: Codex Sol 1af866c3e + coordinator 517565c4d; cargo test --lib 37/37.
- [x] D — Operations drawer at zoom (P1-1 sibling): JobPanel.tsx reads useZoom() and sizes the drawer `min(calc(70vh / z), 560px)` with `maxWidth: calc(90vw / z)`; OpDialog.tsx maxWidth `calc((100vw - 32px) / z)` and GitFailureCard likewise; shell-zoom.spec gains a case at 1.5 on 1280×800 that opens the Operations panel (a fetch, then topbar-progress) and asserts job-panel y ≥ 0 and job-panel-close inside the viewport. Codex Sol 435f34c28/7c3a2da00, merged 378738883: the spec on the unfixed tree measured the panel at y = −66 px (the skeptic's arithmetic said −76), 10/10 after.
- [x] E — Stale docs (P0-3, P2): docs/ubuntu-freeze.md header and §7 restated as closed on the owner's tick 2026-09-11 (native Wayland default; H1 unconfirmed because no freeze recurred to probe; X11 sessions unmitigated by default — POWERGIT_NO_FRAME_SYNC=1 is the fallback); README:26 "Windows today, Linux target" → Windows and Linux, README:125-131 drops "still under investigation" and frames the freeze-dump paragraph as the X11 fallback; a "superseded by v0.13.17/v0.13.18/v0.15.0/v0.18.0" note on the fixed items of docs/agents/context/release-ui-audit.md and frontend-ux-next.md so the next external reviewer does not re-raise them; the PLAN lines on the freeze pointed at the v0.15.6 tick (powerplan). Done: Codex Sol 3ca32d3d9/c136d7884/9ea54d5c0/904a98421, merged 3223e4dfb; the v0.15.4 owner line carries the pointer, its tick and the backlog line "whenever the window loses focus on my ubuntu" stay the owner's to close.
- [x] F — Verification on the merged tree: tsc, lint, vitest, engine tests, e2e (foreground shards), the two new specs, a packaged smoke that launches `powergit.exe <path>` and `powergit.exe` alone with a recent present; then the Claude reply appended to AGENTS_IPC.md with the corrected facts and what shipped. Done: tsc / eslint / `npm run format:check` clean; vitest 457/457; engine 198 passed, 1 skipped; e2e 183/183 (three foreground shards) plus cold-start, recents-restart, shell-zoom, session-states × 2; cargo --lib 37/37; packaged 0.18.18+ zip: `powergit.exe C:\dev\public-repo\pg-wt-zoom2` showed that repository, then `powergit.exe` alone from %TEMP% reopened it from the private recents (captures smoke-1-with-path.png / smoke-2-no-path.png, no console error beyond the first health probe); CI run 35350627589 green on both runners; reply 2 appended.
- [ ] Owner tick: cold-start the installed app with a recent present and see the last repository; `powergit <path>` from a terminal; zoom 150 %, start a fetch, open the Operations panel and close it from its header; the GitHub page reads as PowerGit.
- [x] G — The push CI has been red since 2026-09-15 and the README badge shows it (Grok's P0-2 "any workflow footgun worth fixing anyway?" — yes): App.tsx crossed the 400-line max-lines tripwire with the v0.18.18 prettier pass (410) → the browse hotkey map moves to hooks/useBrowseHotkeys.ts; SequencerTests.Rebase_todo_capture_leaves_no_rebase_state fails on both CI runners because git ≥ 2.45 writes `pick <sha> # <subject>` under rebase.instructionFormat → ParseTodo strips the prefix (+ test); FilesTests.Terminal_editors_are_recognised fails on Ubuntu because Path.GetFileName ignores backslashes → IsTerminalEditor splits on both separators (+ a /usr/bin/vim case); the format gate is `prettier --check .`, not src + tests — scripts/perf/scenarios.mjs (v0.18.18) was unformatted; and three specs written after the last green run (2026-09-08) had never met the runners: recents.spec and commit-file-menu.spec compared the engine's long root with os.tmpdir()'s C:/Users/RUNNER~1 (the fixtures resolve the long name now), ref-chips assumed Windows chip metrics (the Ubuntu font folds the tag into +1; the spec expands it). Done: d0822e312, 8aa3c351a, d5f86cad7 — CI run 35350627589 green (Ubuntu once re-run past the grid-columns auto-fit flake, recorded in the backlog).

## v0.20 — The agent review bridge and the start pane
> Owner via Grok Bot, approved 2026-09-18 as written (option A) — plan at C:\dev\grok\powergit\MCP-AGENT-REVIEW-PLAN.md (Grok's, outside the tree; mirrored in AGENTS_IPC.md). Local coding agents change many files; most of that is noise the owner does not want to review, but a small critical patch must surface inside PowerGit and wait for him. Three modes: Silent (plain git, nothing in the UI), Notify (an inbox item and the diff, the agent goes on), Wait (the same UI with a blocking badge; the MCP tool blocks until Approve / Request changes / Cancel). v1 is the review gate only on the same machine — no clone/push/remote tools, no cloud, no GitHub PR sync, no multi-user; PowerGit must be running for Wait; a protect list (globs that upgrade Silent/Notify to Wait) is v1.1. The review file of v0.19.0 (.powergit/reviews/*.json) is the format and v0.19.3's line comments are what "Request changes" carries back: M1 of Grok's sequence IS v0.19.0 + v0.19.3 — landed 2026-09-18 as e6436433d + 87e27bb3b (the file, the engine routes, the pane, Export, the comments and the slash command line; the owner ticks stay open); then M2 engine + inbox (v0.20.0), M3 the MCP server (v0.20.1), M4 modes, docs, Linux (v0.20.2). Hosting, locked by the owner 2026-09-18: PowerGit starts and hosts the MCP server; agents connect as clients over a private pipe / local socket with no token on disk — v1 is NOT "edit mcp.json with a localhost port and a token file" (option B rejected); clients reach it through a command PowerGit provides or an endpoint it registers while running. Rough size 8–10 worker-days including v0.19.0 and v0.19.3. Marketing (Show HN, Linux + local agent review) only after M4. Started 2026-09-18 on the owner's "keep going, skip the smoke approval" relayed by Grok Bot (v0.18.19 F and G green, its owner tick open).

### v0.20.0 — M2 — Agent review sessions: the engine keeps them, the inbox shows them, the owner resolves them
**Goal:** Grok's M2: a session is one JSON per review (version 1; mode, title, why, repo root, base/head SHA or worktree, files with optional patches, status pending / approved / changes_requested / cancelled / expired, a summary note, line comments aligned with the v0.19 review JSON so Export and the Review file pane stay one format) under .powergit/agent-reviews/ with an index for the inbox; engine routes create/update, list, get, resolve (approve / request changes / cancel / ack); a Wait wakeup the MCP server can long-poll. UI: an "Agent reviews" rail entry with a count badge (pending Wait + unread Notify); rows with title, branch/worktree, mode chip, file count, age, status; click opens the existing review / diff surface on that session's file set (reuse v0.19 review mode, no second reviewer); Approve, Request changes (comments from the review file + summary), Cancel, Ack. Gate: a fake session written by hand appears in the inbox, opens, and resolves each way. Sized after v0.19.0 lands.
- [x] Engine GitHost.AgentReviews.cs + Program.cs: a session is one JSON at .powergit/agent-reviews/<id>.json (id = 40 random hex, which is also the review key of its marks and comments at .powergit/reviews/<id>.json — one format, Export and the pane unchanged); fields version 1, id, mode notify|wait, title, why, agent, branch, base, head, worktree, files[{path, status, patch?}], status pending|approved|changes_requested|cancelled|expired, unread, createdAt/updatedAt/resolvedAt, resolution {summary, comments[{path, line, side, body}]}, reason; routes POST /agent-reviews (create), GET list (newest first, summaries + the badge count), GET /{sid}, PUT /{sid} (pending only, else 409), POST /{sid}/resolve {action approve|request_changes|cancel|ack|expire, summary?, reason?} (request_changes copies the review file's comments into the resolution), GET /{sid}/wait?timeoutMs= (long-poll, clamped 1–120 s, {session, timedOut}, never an auto-approve), GET /{sid}/diff?path= (the session's patch, else git diff base head -- path / the worktree); temp + rename, /.powergit/ excluded, outside the mutation gate; xunit AgentReviewsTests; SRS-ENG-055. Done: Codex Sol ffd3962fc (wt/agent-engine), merged 56fe90378; the wait takes its wake-up signal before reading the session (a resolve between the two was a missed wake-up); Open() excludes a .powergit/ that already exists (5e47d93f8, a session written by hand listed as untracked until the first save); AgentReviewsTests 4/4, engine review/agent/api/files/host/session suites green.
- [x] Frontend: engine/agentReviews.ts + types; hooks/useAgentReviews polls the list (5 s while the app runs) and derives the badge count (pending wait + unread notify); an "Agent reviews" entry with a count badge in NavRail and CommandRail; components/agentReviews/AgentReviewsView.tsx in the file-history slot — the list (title, agent, branch, mode chip, file count, age, status) and, on click, the session: title, why, mode and status, the file list + diff (DiffTab / DiffPane over GET /{sid}/diff) with review mode on and the bar (marks and comments go to the review file under the session id), Approve / Request changes (a summary dialog; the engine takes the comments from the review file) / Cancel / Ack; a resolved session drops back to the list with its status chip. Done: Codex Sol 5a4008ae8 (wt/agent-ui), merged 56fe90378 — useAgentReviewsPage owns the page state, the badge is the engine's count, the session view reuses DiffTab read-only + ReviewBar under the session id; useReview.finish() resolves once the save is on disk and a resolve awaits it (a /comment inside the 400 ms debounce reached the engine late); the bar's meter shrinks so the tab strip keeps its labels at 1280 px (5e47d93f8). Looked at: the inbox row, the session with the why, the actions and the marked diff, zero console errors.
- [x] Symptom e2e agent-reviews.spec.ts (Grok's M2 gate): a fake session written by hand into the fixture repo's .powergit/agent-reviews/ appears in the inbox with the badge at 1, opens on its patch, a line rejected and a /comment added, Request changes with a summary → the session file on disk reads changes_requested with the comment's path, line and body; a second session Approve, a third Cancel, a notify one Ack clears the badge. Done: 56fe90378, green on the main harness (the badge is NavRail's Badge or the command rail's pill, both accepted).
- [ ] Owner tick: a session an agent opened shows up in the rail, reads like a review in the Diff tab, and Request changes carries the comments back.

### v0.20.1 — M3 — The MCP server: agent_review_open / wait / get / list / cancel over stdio
**Goal:** Grok's M3, hosting as the owner locked it on 2026-09-18: PowerGit itself starts and hosts the MCP server while it runs (in the engine sidecar, which already owns the sessions and the HTTP API), listening on a private per-user endpoint — a named pipe on Windows, a Unix domain socket under the app data dir on Linux, owner-only permissions — so no token ever lands on disk; when PowerGit is not running there is nothing to connect to and the client says so. Agents connect as MCP clients: a thin `powergit mcp` stdio shim (the same executable, or a sibling) that Claude Code / Codex launch from their MCP config and that bridges stdio to the pipe — the documented config is one command line, never a localhost port plus a token file (option B rejected for v1). Tools: agent_review_open (mode, title, why, repo_path, base_sha?, head_sha? or worktree paths, files[] with optional patch, protect_check?) creating or updating a session; agent_review_wait blocking in 30–120 s long-poll slices until a terminal status or timeout_ms — on timeout it returns pending + timed_out: true, never an auto-approve; agent_review_get (status, summary, the review file path); agent_review_list (pending and recent for the repo); agent_review_cancel. Notify = open with mode notify; Wait = open + wait; Silent = no tools. Return payload: review_id, status, mode, summary_note, comments [{path, line, side, body}], review_file, export_markdown. PowerGit quitting mid-Wait ends the session cancelled/expired with reason host_gone. docs/agents/mcp-agent-review.md with the Claude Code and Codex config lines. Gate: from Claude Code, open a Wait review on a toy patch, see the diff in PowerGit, Request changes with a line comment, the agent receives the comments and continues.
- [x] Engine McpHost: the sidecar hosts the MCP server on a private per-user endpoint (Windows named pipe, Linux socket) with the five agent_review_* tools. Done c79f56cbc — McpHostTests 7/7, engine suite 216 passed / 1 skipped.
- [x] `powergit mcp`: the stdio-to-pipe shim Claude Code and Codex launch from their config; Windows needs overlapped I/O or the shim deadlocks. Done 4ecd83a12 + 93a080515 — cargo --lib 40/40.
- [x] docs/agents/mcp-agent-review.md: the one-line config for Claude Code and Codex, the tool payloads and the three modes; README and AGENTS.md point at it. Done 7be444bfc + 5dbd2bb3e.
- [x] Gate: a Wait review opened through the shim shows in the inbox and comes back changes_requested with the line comment, then approved. Done 2026-09-18 with mcp-probe.mjs and a real Claude Code session.
- [ ] Owner tick: with PowerGit running, `claude mcp add powergit -- powergit mcp` and a Wait review from a real agent session lands in the inbox and comes back with the comments.

### v0.20.3 — The start pane (current) (ACTIVE)
**Goal:** Prototype B of docs/prototypes/recents-v2.html, picked by the owner on 2026-09-22: opening the recent repositories stops covering the app. The picker becomes the main pane — the same one a cold start shows instead of "Open a repository to see its history." — with the list on the left and the chosen repository on the right: its path, working tree, what is to push and to pull, its recent history, and Open / Terminal here / Copy path / Remove. The three you live in can be pinned to the top. The modal dialog goes; Escape puts the grid back. The engine learns when each repository was last opened and can peek at one it has not opened, without starting a session. Gate: the e2e suite green on the rewritten specs, and a look at the pane in both themes and at 150 % zoom before it goes to the owner.
- [x] Engine: recents.json gains lastOpened (written by Remember) and pinned (toggled by PUT /repos/recents/pin); both survive the prune. RecentsStoreTests cover them.
- [x] Engine: GET /repos/peek — one or many roots, read-only, cached: branch, ahead/behind, changed count, and the last 8 commits for a single root. Never opens a session.
- [x] Frontend StartPane: the list on the left (filter, Pinned / Today / This week / Earlier, keys, Delete with Undo) and the chosen repository on the right (path, state, recent history, Open / Terminal here / Copy path / Remove).
- [x] Frontend wiring: the rail button and the cold start both show the pane, Escape puts the grid back, RecentsDialog and RecentTile go; recents.spec.ts rewritten for the pane.
- [x] Gate: tsc, eslint, prettier, vitest, the engine suite and the e2e suite green, plus a look at the pane in light and dark and at 150 % zoom.
- [ ] Owner tick: open the app cold, land on the pane, pin two repositories, open one from it.
- [ ] Owner on the released build: "The 'xx min ago' is on the same line as the branch name. They overlap. Better split." Fixed in dee69ecf5 — the row's first line is the name and the time, the second the branch and the state; his sentence is a test at three widths. His to tick.
- [ ] Owner on v0.20.4: "I would like to have the branch names longer if possible, so separate row for the rest would be good." Done 339578512 — the chip takes the row's width and wraps onto a second line, two at most. His to tick.

## v0.21 — The agent bridge, finished — modes, the agent convention, Notify hygiene and the Linux path
> Everything the bridge still owes, moved here on 2026-09-22 so the start pane ships first in the 0.20 line. The iteration below keeps its old number (v0.20.2) because powerplan cannot renumber one.


### v0.21.0 — The start pane
**Goal:** Empty on purpose. The start pane was renumbered to v0.20.3 on 2026-09-22 at the owner's ask — it ships in the 0.20 line, and what is left of the agent bridge moved here. powerplan can neither renumber nor delete an iteration, so this heading stays until it can; nothing is planned under it.

### v0.20.2 — M4 — Modes, docs and the Linux path; the protect list stays optional
**Goal:** Grok's M4: the agent-side convention documented (large refactors and generated noise → Silent, or Notify for a paper trail; engine, release scripts, auth, IPC → Wait; always a one-line why and the base SHA or branch so the UI anchors the diff); README gains a short "Agent review (MCP)" section; docs/agents/mcp-agent-review.md carries the tool schemas, the modes and "PowerGit must be running"; a protect-list note in Settings even while the list itself is v1.1 (globs that upgrade Silent/Notify to Wait, or prompt the agent to re-call with Wait); the Wait round-trip demonstrated on Ubuntu Wayland from the AppImage, not only on Windows; Notify sessions persist without the GUI focused; rate-limit identical Notify sessions. Gate: the owner uses Wait on one real critical patch. Marketing (Show HN draft, Linux + local agent review) comes only after this closes.
- [ ] Docs and the agent convention: docs/agents/mcp-agent-review.md carries the modes (large refactors and generated noise → Silent, or Notify for a paper trail; engine, release scripts, auth, IPC → Wait; always a one-line why and base/head or the patches), the tool schemas and "PowerGit must be running"; README's "Agent review (MCP)" section; a protect-list note in Settings → Behaviour ("Paths that always wait for review — coming in v1.1", read-only, with the glob idea) so the setting has a home before the list exists.
- [ ] Notify hygiene in the engine: a Notify session identical to a pending one (same repo, title and file set within 10 minutes) updates it instead of adding a row (rate-limit identical Notify sessions); Notify sessions persist without the GUI focused (they are files — assert it: a session created while no window is open lists after the next launch); AgentReviewsTests for both.
- [ ] The Linux path: McpHost's Unix-socket branch and the shim's UnixStream branch checked on Ubuntu — CI's Linux engine job runs McpHostTests over the socket; the shim's cargo tests in WSL (cargo test --lib, the endpoint rule with XDG_DATA_HOME / HOME); the Wait round-trip demonstrated from the AppImage on Ubuntu Wayland is the owner's tick (the coordinator cannot run the AppImage here).
- [ ] Gate (Grok's M4): the owner uses Wait on one real critical patch; marketing (Show HN draft, Linux + local agent review) only after this closes.
- [x] Polish from the coordinator's look after the review/agent/MCP work (2026-09-18, not M4): the graph compass at rest is 70 % with a hairline frame and the paper behind it — at 45 % on a transparent box it sat over the SHA column's text and the owner never found it (Grok relay: "make it a bit more visible at rest"); the review bar wraps onto a second row when the strip is narrow — the agent session view at 1400 px and the Diff tab at 1024 px cut "Finish review" off the right edge; the graph-options pill keeps its 45 % until the owner says.
- [x] CI hygiene from the same look: grid-columns.spec's scrollbar case runs on a made three-lane repository instead of the checkout's own graph, whose lanes grow while pages load — on the Ubuntu runner the auto-fit at the end met the 35 % Graph cap and rightly kept the scrollbar (red on 3 of the day's 5 runs). Known and left: graph-nav's "a parent below the loaded window" case fails once in ~4 runs under load (the second page's 30 s poll), and the @grid visual baselines are the checkout's own rows and drift with every commit — they want a made fixture too.

Released as v0.20.3 on 2026-09-22 with the owner tick above still open, at the owner's explicit call ("Can we release 0.20? If possible quickly" — and, asked about the two open ticks, "tag now, tick after"). The v0.20.1 owner tick is open too. Anything either of them turns up goes into v0.20.4.

## Backlog
- Drop leftover 2021 origin branches
- Component/UI test coverage: stash flow, gitignore preview dialog, commit-dialog multi-select semantics, remote config dialog, blob viewer content
- Reproduce fullscreen clipping in Tauri window: need owner screen resolution + DPI scaling %; CSS overflow hardening already in place
- [ ] Hotkeys Slice 2–4: commit pane-focus/stage-all chords, grid parent/child/go-to, remapping UI (parked from v0.11.0) [agent: grok]
- Reopened from v0.11.0 (ticked but absent): Alt+Up/Down parent/child navigation, Ctrl+Shift+F find, Ctrl+G go-to. Deferred to next iteration. (deferred from v0.12.2: duplicate of the Hotkeys Slice 2–4 backlog entry; v0.12.2 was already marked complete) [agent: claude]

### From the former “Later” list
- Fetch / Pull / Push **dialogs** (buttons are wired to engine `/fetch`, `/pull`, `/push` since v0.6.4; richer dialogs later)
- Design-demo step/pause bar (opt-in `npm run test:demo`)
- Visual screenshot suite (`npm run test:visual`, owner-triggered)
- Restore parallel Playwright workers on CI (fullyParallel) and run the Linux harness once as the acceptance test; lands after v0.13.0 so auth sits before session resolution (deferred from v0.13.6: engine is multi-repo safe now, but the UI boots from the engine-global /repos/current, so a spec opening a temp repo still leaks into concurrently booting specs; needs UI repo pinning (e.g. ?repo=id) first — see docs/agents/memories/engine-sessions.md) [agent: claude]
- Graph parity: 33 of 37 Git Extensions RevisionGraph fixtures diverge because GE's post-passes are not ported — StraightenLanes (6 fixtures), StraightenDiagonals (18), ReduceGraphCrossings when mergeGraphLanesHavingCommonParent=false (9). PowerGit matches GE's pre-straightening layout lane-for-lane, so the gap is only the look-ahead passes; porting them needs a revisable-rows worker protocol (rows re-emitted after look-ahead). See tools/ge-parity/README.md and frontend/src/graph/layout.ge-parity.test.ts KNOWN_DIVERGENCES. [agent: claude]
- Lint debt from v0.13.4: eslint.config.js exempts src/engine.ts (561 lines) and src/graph/* (draw.ts 407 lines, two no-useless-assignment) from max-lines/no-useless-assignment/preserve-caught-error, and .prettierignore skips them; drop the exemptions the next time those areas are touched [agent: claude]
- Lint: adopt the React Compiler rules shipped with eslint-plugin-react-hooks 7 (set-state-in-effect, refs, incompatible-library) — ~20 deliberate existing patterns flagged (dialog reset-on-open effects, latest-value refs in Host.tsx/SplitHandle, react-virtual); its own iteration [agent: claude]
- First CI run: .github/workflows/ci.yml has never executed (nothing pushed yet); expect first-run fixes (Windows shell quoting in the engine-start step, Playwright browser cache on ubuntu, cargo cache) [agent: claude]
- powerplan 0.8.0 has no update_iteration title tool; v0.13.10–v0.13.13 headings still contain the literal (PLANNED) after COMPLETE. Strip it when a retitle mutation exists. [agent: grok]
- worktrees [agent: grok]
- Owner-approved test:visual / design walkthrough and canonical website/public/assets screenshots (parked; owner-triggered). Resolution assertions for zoom, themes, toolbar overflow and pane minimums landed in v0.13.13. [agent: grok]
- History streaming owns the main thread: on a 17k-commit repository each 1000-row page append costs a 150-400 ms long task for ~15 s after boot (dev; less in production), which delays clicks made during that window (diff-latency.spec measures steady state only). Move the append/layout merge off the main thread or chunk it (requestIdleCallback / smaller batches) and assert with the spec run before settle. [agent: claude]
- Large first diffs render slowly: DiffView renders plain rows below VIRTUALIZE_MIN_LINES=2000, so a 1500-line first diff (PLAN.md) costs a 400 ms render in dev / ~120 ms in production on selection. Lower the threshold (~300 lines) once the specs that read the full diff text are adapted, and measure with diff-latency.spec's worst-case row. [agent: claude]
- Engine: details + changes on one selection spawn three git processes concurrently (show -s, diff-tree, show -p) and each takes 120-200 ms under contention vs 80-100 ms solo. Options: fold the details into /changes (one request, still concurrent), or a long-lived `git cat-file --batch` for commit metadata. [agent: claude]
- Owner 2026-09-08: freeze while idle — window moves/resizes, content black and not redrawn, buttons dead, only the native Open-repository picker works, snapshot button unresponsive. Watchdog did not catch it: the script heartbeat keeps beating when only compositing/painting dies. Next: paint heartbeat (rAF) + platform crash hooks (WebView2 ProcessFailed, WebKitGTK web-process-terminated) + shell-side recovery (webview reload, native dialog with snapshot + restart) + Linux WEBKIT_DISABLE_DMABUF_RENDERER workaround.
- Owner 2026-09-08: Working directory / Index pseudo-rows break the other branches' lines through those rows — lines must pass through continuously.
- Owner 2026-09-08: resizable columns in the main graph grid.
- Owner 2026-09-08: graph column wider than its space — discreet horizontal scrollbar at the column bottom, Shift+wheel scrolls it.
- Owner 2026-09-08: syntax highlighting with language detection in the diff and commit views (public library).
- Owner 2026-09-08: top-bar search in the graph view, find or filter (design later).
- Owner 2026-09-08: upgraded right-click menu; merge, rebase and conflict resolution as Git Extensions does natively; enhanced settings.
- [ ] Owner 2026-09-09: "whenever the windows loses focus on my ubuntu, it usually end up in a freeze." v0.15.2 adds investigation tools only; root cause and fix awaiting Ubuntu reproduction and owner verification. [agent: codex]
- [ ] Owner 2026-09-09: on Ubuntu AppImage, "open recent repos" works only until app close/reopen. Reproduction/fix under v0.15.2; awaiting owner verification after release. [agent: codex]
- [ ] CI has been red since at least 2026-09-09 on two tests that pass locally, and it stayed red through v0.15.5. Neither is caused by the commits that surfaced them. (1) SequencerTests.Rebase_todo_capture_leaves_no_rebase_state fails on both ubuntu-22.04 and windows-latest: the captured todo's subjects come back as '# topic-change' instead of 'topic-change', so ParseTodo is reading a comment prefix the runners' newer git emits and my local git 2.38.1 does not — GitHost.Sequencer.cs:232 already sets rebase.instructionFormat=%s for exactly this and it is no longer enough. Needs the raw todo from a runner (or a newer git locally) to see what git actually wrote. (2) merge.spec.ts 'fast-forward only refuses a diverged branch and says so' fails on ubuntu only, 125/126. Until both are fixed CI cannot tell us anything, so every push looks the same whether it broke something or not. [agent: claude]
- [ ] Temp is carrying ~100 leftover e2e fixture directories (12 pg-commit-reliability-*, plus pg-ui-audit-*, pg-settings-* and others). commit-reliability.spec.ts does not remove its repositories the way repoFixture.removeRepo does — they also fill the dev engine's own recents list. Give it the same close-session-then-retry cleanup. [agent: claude]
- Updater: "Could not fetch a valid release JSON from the remote" during the hour between the release workflow creating the GitHub release and its manifest job attaching latest.json (owner hit it on 2026-09-11 while v0.16.0 was building). Fix: create the release as a draft and publish it from the manifest job, or attach latest.json before the release becomes "latest". [agent: claude]
- Diff tab blank after the File tree tab: select a commit while the File tree tab was last used, switch to Diff — the pane renders nothing (DiffPane.tsx returns null for the file-set / no-diff case) until a file row is clicked; it should show its "Select a file." empty state (seen 2026-09-12 during the v0.18.2 look)
- Save as patch (v0.18.6) follow-up: a range — "Save patches from the other selected commit to this one" (GE FormFormatPatch's from..to into a directory, git format-patch -o with git's own 0001-, 0002-… names); the engine route streams one mbox today, a range needs a directory picker and either one mbox with several patches or n files
- Create archive… could take Save as patch's native path (plugin-dialog save + write_text_file, or a streamed write for a binary zip) instead of opening the browser on the /archive URL; the query-token 401 it had since v0.15.0 was fixed in v0.18.6
- Save as patch on a merge commit answers 400 (git format-patch has no patch for a merge); GE has the same gap. A --first-parent diff (git diff-tree -p -m --first-parent) could be offered as "Save the merge's changes as a diff…"
- The engine passes git's stdout with its trailing newline, so parseGutterLines yields an empty last row on every diff and the review layer's End lands one row below the last code line (found by the v0.18.8 wrap spec, which steps up with k before marking; recorded in docs/agents/memories/code-wrap.md). Trim the final newline in the parser and adjust the specs that count rows.
- Click → highlight is ~68 ms median and click → Commit tab ~160 ms on the 50k fixture (dev build), unchanged by v0.18.18 A–I: the audit's select profile (2026-09-17, `perf-audit.mjs --scenarios select`, now profiled) puts element creation on top — createElement + jsx + ReactElement ≈ 45 ms of self time per click, i.e. a large subtree is rebuilt per selection — beside three sequential git processes per click (`show --format=…`, `diff-tree --root`, `show --find-renames`, ~250 ms). A third pass on the click frame: which subtree, and can the three git calls become one.
- Owner via Grok Bot (2026-09-18, AGENTS_IPC.md): the agent bridge should also show agent changes inside PowerGit for direct review, with a filter — large noisy agent work needs no review; small critical patches must surface in PowerGit and wait for the owner. Grok's shape, to be sized after v0.18.19 as part of the MCP iteration: an agent opens/updates a Review session bound to a worktree/branch (files, hunks, a short why); modes notify (show, don't block), wait (block the agent until approve / request changes), silent (plain git); an "Agent changes" inbox in PowerGit reusing the review UI (open diff, comment, approve, request changes); critical = the agent chooses wait, or a policy of always-wait on a protected path list. Not before v0.18.19 closes.
- CI flake (2026-09-18, Ubuntu runner only, once in two runs): grid-columns.spec "a graph wider than its column gets a scrollbar that Shift+wheel drives" — after the dblclick auto-fit the graph-scrollbar stayed visible. The checkout's own graph is the fixture: its lane count grows while pages load, so the auto-fit at dblclick time can hit the 35 % Graph cap (the v0.18.18 known item) and the bar rightly stays. Pin the fixture (a made repo with a known lane count) or wait for the tail before the dblclick.
- graph-nav.spec "the grid pages until the commit arrives" flakes under load (2026-09-18, three times while parallel Playwright runs and cargo/dotnet builds shared the box; 11/11 green quiet, on both the pre-M1 and the M1+M2 frontends): after the held skip=3000 page is released, a full refresh ("refresh: 2", revisions+refs+status, no watcher event — the change version stays put) fires right after the parent commit's /commits + /changes land and re-issues page 0; when it wins the race the scrollHeight stays at 3000 rows for the 30 s poll. Find who calls refresh() there (the focus/visible sweep or a watcher echo) and make the eager tail survive a reset, or hold the reload until the nav intent has landed.
- Owner 2026-09-22: "I don't like the current UI for the 'select a recent project'" — three prototypes in docs/prototypes/recents-v2.html beside the shipped picker: A the list (one aligned row per repository, a time column, working-tree state, Today/This week/Earlier groups), B the desk (no dialog at all — the picker becomes the main pane and the cold-start front door, list on the left, the chosen repository's path/state/recent history on the right, pinning), C the board (today's cards at two columns, each carrying the last commit, the state and a two-week activity bar, the initials disc replaced by a colour spine). Waiting on the owner's pick; A about half a day, C a day, B two or three. All three want two engine additions: a lastOpened field written in RecentsStore.Remember, and a cached batch peek (git status --porcelain=v2 --branch, git log -1) over the listed roots that paints after the list and never blocks it.
- The release workflow's Linux job lists dist deb/*.deb for upload but no .deb has been attached since at least v0.18.18 — the bundle is not produced or lands elsewhere. Check tauri.conf's bundle targets, or drop the glob and say AppImage only.
- Engine flake on the Ubuntu runner (2026-09-22, once): ReviewsTests.Round_trip_uses_the_review_file... expected the change version to stand still after a second identical WriteReview and saw 5 -> 9. The watcher sees the .powergit write asynchronously on Linux, so the second assertion races it. Windows is green. Settle the watcher before asserting, or compare kinds rather than the counter.
